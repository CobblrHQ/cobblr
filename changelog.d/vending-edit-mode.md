---
type: feature
scope: views
date: 2026-08-23
docs_target: docs/USER_GUIDE.md#3.15 Apps (`core-apps`, stock)
---
**The vending board can now be tapped.** It has always shown what you have and never let you change it. A "Use / restock" button turns on a plus and minus on every tile, so you can record what you used while standing in front of the cupboard rather than going back to a laptop. Undo sits next to the toggle, because a mis-tap on a screen you walk past is going to happen.

## docs

### Using the vending board

The board shows what you have as a wall of tiles: a quantity, a status light, and an empty-looking slot when something has run out. It is meant to be readable from across the room.

Tap **Use / restock** and each tile gains a large plus and minus.

| | |
|---|---|
| **−** | you used one. Always takes the oldest one you have, so the warning moves on by itself |
| **+** | another one arrived. Dated from today using how long the item keeps |

**Undo** appears beside the toggle after any tap, naming what you just did. It stays in one place rather than on the tile, so it does not move when the board re-sorts underneath it.

Tap **Done** to go back to the plain board. It starts plain every time, because a board covered in buttons is a worse thing to read.

This is meant for a tablet left somewhere useful. Recording what you used at the moment you use it is the only way the rest of Cobblr can tell you anything about what you are running out of.
