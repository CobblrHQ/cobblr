---
type: improvement
scope: platform
date: 2026-07-14
docs_target: docs/USER_GUIDE.md#4.5 Custom fields
---
On the Fields page, the fields you added yourself now sit at the top under "your fields", and the ones that came from bundles or modules sit below a divider. Before, a field you just added could be buried under a wall of bundled fields, so you had to scroll to find it.

## docs

The `/fields` list is split into **your fields** (the ones you added) at the top, then a divider, then **from bundles & modules** below. So a field you just created is right where you'd look for it, not lost among the fields a bundle shipped.
