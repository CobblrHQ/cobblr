---
type: improvement
scope: platform
---
Managed-app signups ("Cobblr for Yarn") now load the app's bundle from the official registry **server-side** — the consumer's browser no longer supplies it. The new workspace always gets the current published version of the app, and the signup page is simpler.
