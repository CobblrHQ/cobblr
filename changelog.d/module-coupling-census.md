---
type: internal
scope: tooling
date: 2026-08-16
---
A nightly census counts where modules name each other and where the kernel names a module, so the architecture's drift is a tracked number rather than an impression. It never fails a build.
