# Changelog

User-facing changes, newest first. Dates are release dates.

## 2026-08-13

### Features
- Scan a screenshot of a listing and Cobblr crops the item's photo out of it. Upload a marketplace or shop screenshot and the item's own picture is cut out of the page, away from the chrome, price, buttons and seller, and used as the catalog photo. It is a picture of the actual item you are getting rather than a stock shot of some other unit of the same product, it appears instantly because nothing has to be searched or downloaded, and it cannot break later the way a hotlinked image can. The internet options are still one tap away in the photo strip, the crop stays there as a "From your screenshot" pane so you can go back to it, and Revert undoes it like any other picture change. Costs no extra AI: the read that identifies your item answers this at the same time.

## 2026-08-12

### Features
- Four new field roles describe how something came to be yours: where you got it, when, what it cost you, and who sold it. Mark your own fields with these and a receipt can fill them in for you, whatever you happen to have named them. "Acquired from", "Bought from" and "Source" are the same idea, and saying so is what lets Cobblr work on all three.
- A dropdown field can carry a one-off note beside its value. Pick "eBay" from the list and note the specific seller next to it, and the record reads "eBay · detroitaxle". The value stays "eBay", so grouping, filters and saved views still gather every eBay purchase together however many sellers you have noted. Useful when a detail is worth keeping but does not deserve a permanent spot in your choices list.
- A custom field can say what it means, not just what it is called. Fields you create yourself can now carry a role from a short standard list (identifier, expiry, category, packaging, assignee), the same list bundles use. A field named "Best before" and one named "Use by" are the same idea, and saying so lets Cobblr fill it in for you, match it against a field someone else made, and warn you before you end up with two fields for one thing.
- A confirmed receipt fills in your own fields. If you have a field marked as "acquired from", "acquired on" or "acquired for", a receipt fills them in for you: where you bought it, when, and what it actually cost after any discount. It matches on what your fields MEAN, not what you named them, so "Acquired from", "Source" and "Bought from" all work. On a marketplace the individual seller is kept alongside the shop rather than replacing it.
- A forwarded receipt now keeps the whole purchase, not just the item. Alongside the line items, Cobblr reads who sold it, what it actually cost you after any discount, and when it is due to arrive. Marketplace orders keep the shop and the individual seller as separate facts, because you shop at one again and probably never deal with the other twice.
- Replay now means "re-apply the latest processing", and it can no longer make an item worse. What the AI works out about an item (its name, brand, category, what the photo shows) is stored on the item. Everything after that (which table it belongs in, which fields fill, the pack size, a decoded serial, the catalog photo) is worked out from that stored answer by Cobblr's own code. Replay redoes the second half with the code as it is today and never touches the first, so it costs nothing, returns immediately, and has no new information to overwrite anything with. Previously it re-asked the AI for a copy of an answer it already had, and when that copy could not be found, degraded to a keyword guess that could overwrite a good name with a worse one. Use it to bring an item you scanned last month up to date with an improvement that shipped since. Re-run AI is still the one that takes a fresh look. Scan names that an earlier replay truncated are restored automatically.
- You can paste a screenshot straight into the scan inbox's search box, instead of saving it to disk first so you can drag it back in. Pasting text still searches as it always did.

### Fixes
- A receipt's price is read as what you actually paid, not the sticker. An order with a coupon used to record the pre-discount price, so "what did I pay for this" came back wrong by the discount with nothing to indicate it. Cobblr now reads the subtotal, discounts, tax and shipping separately, works out what the item cost you, and checks that those add up to what was charged. When they do not, it records the parts it is sure of and leaves the price for you rather than guessing.
- A re-run can no longer rename your item to a worse name. When the AI was unreachable, scan fell back to its keyword heuristic, and that fallback's guess was written back over the name something better had already produced. A "Voron 0.1 3D Printer (partially built)" came back as "Voron 0". Two faults, both fixed: the keyword fallback read the decimal in a model number as the end of a sentence, so "0.1" truncated to "0", "1.75mm" lost its leading digit and "v2.4" became "v2"; and nothing stopped that fallback from renaming a row it had no better information about. The keyword fallback now routes and fills fields as before, but it never renames. Only a real reconciliation does.
- The order number field on a receipt session closes on Escape or by clicking away again, and it is wide enough to show the whole number.
- Pasting a product link into the scan box adds it again. It had been rejected before it reached the code that handles links, and the failure was reported as a green "Added 0 URLs", so it looked like nothing had happened rather than like an error.

## 2026-08-11

### Features
- Fields can now hold a person. Pick a Member field in the field builder and the field becomes a dropdown of everyone in the workspace, showing their name rather than an id, with Unassigned as a real option. Renaming someone updates it everywhere, and a person who has left still shows on records they were on.
- You can now crop your own scan photo into the item's catalog picture, instead of taking whatever the web turned up.

### Improvements
- You can now create a relation field, the one that links a record to another record, from Configuration then Fields. The type existed and worked, but it was only reachable by installing a bundle, so it never appeared in the field builder.

### Fixes
- Inventory tables showed a raw internal id for fields that point at a person or another record, while the same field read correctly everywhere else. The module's own list was a second way of reading the same records that skipped the step which turns ids into names.
- Creating a Member field failed with a database error. The type was accepted everywhere in the application and rejected by the database itself, so the field could never actually be saved.
- A Member field showed a raw internal id instead of the person's name everywhere the value was displayed: table cells, list rows and the API. The picker was right, but everything that only reads the value was not.
- Creating a relation field now asks which kind of record it points at, and refuses to create one without it. You could previously pick the type but never say what it linked to, so the field stored a reference nothing could resolve and always displayed a raw id.

## 2026-08-10

### Features
- A bin of fifty jumbled things can say so. Instead of pretending you will one day type in every adapter in the box, record it as "assorted adapters, roughly 50", and break it into kinds later only if you want to. Anything you do count keeps its identity, including its printed label.
- Scanning more of something you already have now asks what kind of buy it was (as usual, a one-off, a stock-up, or going quicker lately), and when the system thinks you should still have some left it asks what happened to them: still have them, used them faster, or they went bad. That last answer is what stops food you threw away being counted as food you ate.

### Improvements
- One upload button for photos and receipts. Adding a receipt used to be a separate menu item from adding a photo, which meant answering "which kind of upload is this?" before you had even chosen a file. Now a single control on the header row takes both, and the file itself decides: a PDF or spreadsheet is only ever a receipt, so it goes to the line-item parser, while pictures go to the photo pipeline. Importing a previous export stays its own menu item, sitting next to Export where it belongs, because an export file is neither a photo nor a receipt. The scan page also drops its duplicate camera button on desktop, which gives the search box noticeably more room to show what you typed.
- Uploading a photo from your device is a button again. It had ended up inside a menu, behind a paperclip, inside the search box - which on a phone collapses to a magnifier, so one of the main ways things get into the inbox was effectively hidden. It is now a single-purpose control on the header row: one tap, straight to your picker, no menu in between. On a desktop it sits next to the camera deliberately, since the camera opens the live scanner and the picture icon takes photos you already have; on a phone this row has no camera at all, because the app's own top bar already carries one. To make room on a row that has to stay one line at phone widths, Live Sort moved into the ⋯ menu, where it gains a readable name instead of relying on a tooltip that a phone cannot show.
- The scan header uses the whole width on a phone, and search takes the row when you tap it. The controls used to bunch against the left edge with dead space trailing after the ⋯ menu; spare width is now shared evenly between everything on the row, so it breathes and the actions finish at the right edge. Tapping search no longer squeezes a stub of a field in beside everything else: it expands across the row, with its own close button. Search also sits to the left of upload now, matching the order you read them in, and the placeholder is shorter so it stops being cut off mid-word ("Search, paste a UPC or link" - dropping a file still works, and uploading has its own button).
- The item history says whether the AI actually answered. A re-run is recorded the moment it starts, so the timeline read "Re-ran the lookup with AI" even for runs the AI never answered, which is exactly when you most want to know. The most recent entry now says so plainly when no model replied and the keyword fallback produced the result. Replays are unaffected: those never ask the AI in the first place.

### Fixes
- Re-buying something that had already gone off now actually gets recorded as waste. The system correctly stopped asking what happened to it, then filed nothing, so the waste never counted and it would go on suggesting you buy the same amount again.
- Two fixes to the predictive half of consumption tracking. The run-out signal now actually reaches your shopping list (it was publishing the wrong key, so the wire matched nothing and quietly never fired), and the buy-less insight, when most of something keeps going bad, is now sent to you instead of being emitted into thin air.
- The buy-context chips on a re-scan never appeared. They were shown only when the system had learned a rate, but the check read a field name the server does not send, so the answer was always no.
- Re-buying something that had already gone off is now recorded as waste without asking you, because the scan can see the expiry date. The vending view's wording also matches its status dot again, instead of reporting an expiry date on an item flagged as running low.
- Buying a replacement for something that had already gone off now records it as waste instead of as consumption. Recording it as consumption made the system think you were getting through the item faster than you were, and recommend buying more of the thing you kept throwing away.
- The first time you scan something into a table now counts as a purchase, so the system starts learning how fast you get through it from day one instead of from the second time you buy it.
- The header menu holds still and fits the screen. Opening it on a phone used to leave the page free to scroll underneath, and because the panel kept re-aligning itself to a button that was scrolling away, the whole menu appeared to slide up the screen with it. The page is now held while a menu is open, and a menu taller than the room below it scrolls inside itself instead of running off the bottom. On a phone the panel also widens to use the space it has, so hints stop wrapping onto extra lines - capped so a strip of the page stays visible beside it and it still reads as a menu rather than a sheet. Tidier with it: section headings sit closer to what they head, the "waiting 2d+" filter is a single quiet line at the top rather than a switch it never was, and copying the receipt address is a link beside the label instead of a third line under it.
- Uploading from your phone is findable again. Consolidating the scan header into one row put photo upload, receipt upload and import behind a paperclip inside the search box, which a phone collapses to a magnifier: the only route to "upload a photo from this device" was through an icon that reads as Search. All three now sit at the top of the ⋯ menu. In the same menu, rows that are switches look like switches instead of spelling out "on" and "off", and the receipt drop-box address is shown before it is copied rather than landing on your clipboard unseen. Separately, an installed home-screen app no longer paints a pale strip behind the status bar in dark mode.
- Scanning more of something you already track now offers to add to the quantity even when the scan also has details worth merging in. Previously the add option, and with it the question about what happened to the ones you had, was hidden whenever there was anything to merge, which is most of the time.
- The scan inbox now offers to add to the quantity when you scan more of something you already track, along with the question about what happened to the ones you had. Those controls existed but only appeared on a card most people never reach, so in practice they could not be used.
- Rotating a photo no longer clones it, and landscape shots stop wasting space. Each tap of rotate used to file the previous orientation into the item's photo strip, so straightening a sideways label left two or three near-identical photos behind; a rotation is now treated as an edit of the same photo rather than another photo of the item. The big photo panes also size to the picture instead of always being square, so a landscape shot no longer sits in a band of empty black. Two staleness fixes as well: the "the AI didn't answer" note now stays attached to the run it describes instead of jumping to the newest history line, and the catalog-photo search follows a rename immediately rather than needing a page reload.
- Correcting an item from its photo now clears the wrong product's details too. When a photo showed the lookup had identified the wrong thing, Cobblr renamed the item but kept the rejected product's category, type and description on the record, so a monitor correctly renamed off its own label was still filed as a gas and flame detector. A correction that replaces the identity now replaces everything derived from it, keeping only what the correcting photo itself observed.
- A serial number is no longer mistaken for a product code. Scanning the label on the back of a monitor could name the item after a completely unrelated product: the serial happened to be ten characters long, which is also the shape of an Amazon product code, so it was looked up as one and a web search for the bare code returned something whose part number merely shared a few characters. Codes like that are now recognised as serials, and separately, a web guess that nothing has verified is kept as a hint rather than written as the item's name whenever there is no way to check it. Blank, with the guess offered underneath, beats confidently wrong.
- The What's on hand view never showed anything about expiry. Items going off tomorrow looked the same as items with months left, because the view read only the built-in fields and expiry is one a bundle adds. It now reads both, so the status dot means what it says.

## 2026-08-09

### Features
- Cobblr can now learn how fast you go through something and put it on your shopping list before you run out, instead of waiting for you to cross a reorder line. Turn on the new Cadence capability and the Groceries bundle starts learning from ordinary shopping: every time you check an item off the list it quietly records the restock, and once there is enough history it predicts the next run-out. It also notices the opposite problem, telling you to buy less of something that keeps going bad rather than reordering it on schedule.
- The published container images now say where they come from: each one carries a link to the public source repository, a description and its licence, so the GitHub package page shows a README and a link back to the code instead of a bare list of tags.
- Say "I'll get it" on a shopping list so nobody comes home with two of the same thing. Every open line gets a claim toggle, your name shows on it for everyone else, and the list refreshes itself while it is open. Checking the line off clears the claim.
- Put something where it actually belongs. A book on your Bookshelf that is really just inventory, or a kitchen item that wants its own "Cooking Stuff" list, can now be moved instead of deleted and retyped. The record keeps everything: its history, its tags, its photos, and the QR label already stuck on the thing.
- The Groceries bundle now installs a What's on hand app: a vending-machine view of the kitchen with a quantity badge and a status dot per item, and a use-it-or-lose-it list sorted by what expires first. Bundle-authored apps can finally use view and stat blocks, by naming a view the bundle provides instead of pasting an id that only exists in one workspace.

### Improvements
- Bundles in the catalog are now named for the thing they track, all the way down. Groceries, Pets, Plants, Vehicles, Documents, Warranties and Filament used to carry older internal names behind the scenes (Groceries was filed as "food-cluster"), which made them hard to find when searching. Anything you already installed keeps working and updates itself; you may see a version bump on those bundles. Three titles also dropped the phrasing that described what they do rather than what they are: Household Supplies, Home Maintenance, and Grocery Spend.

### Fixes
- The automated nightly release works when it runs on the deploy box itself. Every script that needed the box reached it in a way that only worked from a developer's machine, so the scheduled release refused to run.

## 2026-08-08

### Improvements
- Ready-made setups are now named for the thing they track rather than the chore: Plant Care is Plants, Pet Care is Pets, Kitchen & Groceries is Groceries, and Warranties & Receipts, Medications & Refills, Subscriptions & Recurring Bills and Important Documents & Renewals lose their second half. The table each one creates gets the same name, so what you pick in the catalog is what appears in your nav. Setups you already installed keep the names they have.
- The community forum now carries the full release history: a thread per month, with one post per day of changes, going back to the project's first commit. Earlier weeks that predate the changelog are summarised so the timeline starts where the code does.
- Less scrolling on an expanded scan item. The "Add to ..." commit buttons no longer sit on a full-width row of their own beneath the card; they move into the bottom of the details column, which always ran shorter than the photo column beside it, so the row they used to occupy is simply gone. They are joined there by a collapse arrow in the card's bottom-right corner, so you can close a long item from where you finished reading instead of scrolling back up to the one at the top.
- Closed scan items are as short as their photo. Two standing lines are gone from every closed card: the routine "Identified via ..." provenance (it lives in the expanded Source data panel) and the untaken "Not right? Fix the name" offer (now in the card's ⋯ menu; the amber double-check warning still shows on low-trust reads, and the inline editor still appears right on the card once summoned). The catalog-photo strip in the expanded view now scrolls sideways only, so it no longer wobbles vertically under a thumb, and and the add buttons read in action order as a matched pair: "Add to ..." first, "add somewhere else..." after.
- The scan inbox header is one clean row. "Inbox" now sits on the same line of type as the counts beside it, rather than being centred independently and landing a couple of pixels off. The standing "Drive this screen with scans" offer, which occupied a whole line under the header whether or not you use second-screen scanning, moved into the header's ⋯ menu under Capture setup; once it is switched on its live status pill still appears, because status earns its place where an untaken offer does not.
- Release notes now arrive as one post a day. Whatever shipped in that night's build is announced once each morning, in the same form on Discord and the community forum, grouped into features, improvements and fixes. Previously the two carried different content on different schedules: the forum could publish a full day of work while Discord stayed silent, because Discord only ever carried features and roughly a third of days ship none.

### Fixes
- A queued print can no longer be dispatched onto a printer that is already busy. When two assignment passes overlapped (creating a job kicks one off, and there is a regular re-tick), each could read the same printer as free and hand it a different job, which in the real world means a plate landing on an occupied bed. Freeness is now checked by the database at the moment the job is claimed rather than from a snapshot taken earlier in the pass, so the second pass simply finds nothing to claim and the job waits its turn.
- Floor plans now show you when two things are in the same place: a drawer or item that overlaps another is outlined in amber, on screen and on paper, instead of one silently hiding the other. Printed plans also stopped turning every label sideways, so a rack's bin names read across the page when there is room for them.
- Workspace names with accented or non-English letters now make sensible web addresses. A name like "Muellers Werkstatt" or "Asas Verkstad" used to have every accented letter replaced by a dash, and a name that STARTED with one lost that letter completely, so the address quietly misspelled the name back at you. Accented letters are now folded to their plain equivalent.
- A background job can no longer make a request fail by closing a database connection out from under it. Cobblr keeps one connection pool per workspace and lets background sweeps close pools they are finished with, so a job that visits every workspace does not hold hundreds of connections open. The check for "is anyone still using this?" could not see a request that had just been handed the pool but had not yet run its first query, so a sweep could occasionally close it underneath and the request would fail with an internal error. The check is now correct, and as a second line of defence a request whose pool disappears quietly reconnects instead of failing.
- A workspace is no longer offered record types belonging to modules it never turned on. Scanning or searching used to probe every record type the server knows about, including ones your workspace does not have, which produced errors in the log and wasted work on every scan.

## 2026-08-07

### Features
- Scan a storage tote, turn it into a bin - one step. When a scan looks like a storage container, its inbox card offers Turn into a bin (always available in the card's More menu too). One action creates a container location - named for you as the next free "Bin N", editable - with the product's photo, barcode and brand kept on the bin's record as its identity. Create & scan into it also makes the new bin the standing file-location, so the next things you scan land inside it. Your bin's name is never overwritten by the product name.

### Improvements
- Cobblr now upgrades its own database across major PostgreSQL versions. Previously a new major refused to start on an existing data directory, so someone had to stop the stack, export everything, wipe it and import again, which is not something a self-hosted instance should ever be asked to do. The database image now carries the previous version too and migrates the data itself on first start, keeping the old copy untouched as a fallback, so pulling a new image is all an instance has to do. This release moves to PostgreSQL 18.
- An automatic update can no longer leave an instance with a database that will not start. The database image now upgrades clusters left at the pre-18 mount layout in place (no compose edit needed), and when a major upgrade cannot proceed safely it keeps serving the previous Postgres version from the untouched data and alerts the admin (log plus superadmin email) instead of failing. Operators who want to schedule major upgrades themselves can set COBBLR_DB_MAJOR_UPGRADE=hold.
- The public source tree grew its release paperwork: a LICENSE and a security policy (how to report a vulnerability privately) now live in the repository itself, the published changelog ships with the source so a clone builds without stubs, and the experimental-modules switch is validated at boot and documented in the self-hosting guide.
- Self-host releases grew their machinery: every nightly now also gets a frozen `nightly-YYYY-MM-DD` tag you can pin or cite in a bug report, a stable release is cut by re-tagging the exact nightly that soaked (no rebuild, bit-identical), and `/healthz` plus the admin Health page now report which version tag your instance runs.
- A calmer scan review card that is honest about its guesses. Expanding an inbox item now shows just the review surface; the full add-to-a-table form (with every field of the destination) appears when you actually pick a destination, so a wrong suggestion no longer buries you under the wrong table's empty boxes. Rarely used tools (Split into items, Retake for catalog, Add a photo, Box state) moved into one ⋯ menu on the card's action rail, where re-run and discard already live, so they no longer cost a row under the photos and you can reach them without expanding the card at all. Photo controls now live on the photos themselves: rotate and "use as catalog" on your photo's caption, "back to original" on the catalog's. Extra photos appear as a strip directly under the big images once an item has any, so the catalog-photo picker sits right where you're looking instead of below two near-empty rows. The card also stops dressing keyword guesses up as AI matches: a route based only on incidental keyword hits renders as an outlined tentative chip with no one-tap Add, File all skips it, and when your workspace has AI but the model failed to answer, the card says so and offers a one-tap retry.

### Fixes
- Hourly background sweeps (maintenance due-soon, expiry, stock burn-rate) on instances with many workspaces no longer exhaust the database's connection slots: each sweep now closes a workspace's connection pool the moment it finishes with it, instead of quietly holding one pool per workspace until the next hour.
- Deleting a workspace now removes its database user as well as its database. Postgres users are cluster-wide, so they were left behind on every delete, and instances that reap expired trials accumulated one per reaped workspace forever. Existing leftovers are swept automatically on the next start.
- Labels printed by an automation (a wire firing labels:print) now carry the same full, phone-scannable QR URL as labels you print by hand, reusing the item's existing code where one was already printed. Previously they encoded a bare path that a phone camera read as plain text. If neither a label base URL nor a public instance address is configured, the automation now refuses with a clear message instead of printing a label that can never scan.

## 2026-08-06

### Features
- Self-hosted instances can now turn a feedback note into a ready-made bug report for the public issue tracker, with the version, browser and enabled modules already filled in.

### Improvements
- Your sidebar-or-top-bar choice follows your account now, so signing in on another desktop keeps the layout you set instead of starting over on the top bar. Phones are unaffected and keep their own menu.
- Signup gains opt-in anti-abuse controls, all off by default so nothing changes for an existing instance: a captcha on the signup form (Cloudflare Turnstile), a block on known disposable/throwaway email providers, and an option to require a verified email before an account can sign in. The trial tier also gains a humane reaper that emails an expiring workspace a heads-up, waits out a grace period, and never removes a workspace it could not warn first.

### Fixes
- Self-hosted images now know which commit they were built from, so Health and a bug report show a real version instead of a blank one.

## 2026-08-05

### Features
- If you use the Cobblr desktop or iPhone app, its own settings (which Cobblr it loads, and app updates) are now reachable from Configuration then Connections then Devices, instead of only from the launch screen.
- Rolling back to the previous release is now a supported, tested path: migrations are enforced additive, so the older image runs on the newer schema, and a nightly job proves it by booting yesterday's build against today's database.
- Scanning in the dark now turns the flashlight on automatically, and off again when the scan lands, the lighting improves, or you close the scanner. It waits out passing shadows, never flickers (once lit it stays lit until a real reason to stop), and backs off for the whole session if you turn it off by hand. The top-left flash button is now the auto-flash switch, and a small manual flashlight button sits next to the shutter.
- The scanner zooms in by itself when a barcode is too small in view to read, so a code across the room or on a low shelf no longer means walking closer. It uses the position it already works out while trying to read the code, steps gently rather than snapping, holds still while codes are reading, and widens back out when there is nothing in view, since a narrow view makes finding a code harder. Setting a zoom by hand in the URL turns it off.

### Improvements
- On a phone the top bar now stays with you: it slides away as you scroll down a long list and comes straight back the moment you scroll up, so the workspace switcher, scan and the menu are always one flick away instead of a scroll back to the top.
- A resolved feedback item now posts its outcome inside that item's own Discord thread and marks the original report with a check, instead of adding a separate card to the channel. Closing several items at once no longer floods the channel, and those items get their check mark too, which batch closes previously skipped.
- Barcodes now read at any angle. On a missed read the scanner measures which way the code is pointing from the stripe pattern itself and reads along that axis, replacing the earlier quarter-turn retry that only covered sideways codes. It also guards against a diagonal read of a long barcode masquerading as a valid short one, which briefly produced wrong scans; short codes now must repeat identically a few extra times before they count.
- Barcode scanning is about three times faster per attempt again. Reading codes at any angle had made every frame without a code in it do the maximum-effort work twice over, which cut the number of attempts per second on a phone by more than half. The routine sweep now runs lean and the expensive last-resort guess happens periodically instead of on every frame, with no loss in what gets read: all fifteen angle and clutter cases still decode.
- Barcode scanning tries roughly twice as often as it used to. Measurements from a real phone showed two thirds of every decode cycle was deliberate idle, so the pause between attempts came down. The scanner URL also accepts scandelay, lens and zoom settings for trying alternatives on a real shelf, and the diagnostics readout reports which ones are active.

### Fixes
- A printer now waits for you to clear the bed no matter how the print ended. Cancelling a running print, removing a connection, or a failed handoff to the machine used to skip the bed-clear step that a normal finish triggers, so the next plate in the queue could be sent to a printer that still had the abandoned one stuck to it. A cancelled print also gets its own wording on the printer card, and does not ask whether the part came out good, because there is no finished part to judge.
- Homebox imports no longer lose labels while reporting a clean success. Items import in parallel, so two rows arriving with the same label could collide, and the losing attach was discarded without a word: the item came back missing tags even though the import said everything worked. Colliding attaches are now retried, and anything that still fails is counted and listed in the import result.
- Sideways barcodes now scan. A code held with its bars running vertically never read at all, no matter the lighting or distance, because the rotated retry was handing the decoder a corrupted image. The scanner now turns the frame itself and reads either orientation.
- The camera no longer files anything. The capture drawer's button used to create the entity and route the item to whichever destination the matchmaker guessed, which is the same act as the destination chips that were removed earlier, just under a different word. It now reads "Looks right" and simply clears the drawer, and the drawer's "files into" lines are honest routing hints instead. Everything you scan waits in the scan inbox, where routing decisions are made with the whole picture in front of you.
- Two field fixes on the scanner. The catalog picture now takes over the main image the moment it arrives, marked "checking" until it is confirmed against your photo, instead of appearing in the strip seconds before the main image caught up; if the check concludes it is the wrong product, your own photo steps back in. And the auto-flash no longer lights up while the phone dangles at your side: a flash burn that reveals nothing scannable ends after a few seconds and stays off until you actually raise the phone to something.
- The auto-flash now turns itself off when the room gets brighter. It compares the scene against how bright the flash itself made things, instead of waiting for a near-blinding level that normal room lighting never reaches, and it learns from mistakes: if turning off proves wrong (a reflective surface, not better lighting), the bar rises so the same surface cannot blink the light again.
- The auto-flash turns off when the room lights come on. Phone cameras adjust their exposure constantly, so a lit room and a flash-lit dark room look equally bright to the software, which is why the previous attempt kept the light on. It now watches the shape of the light instead: a flashlight makes a bright middle with dark edges, while room light is even, and that difference survives the camera's exposure adjustments. As a backstop the light also pauses briefly every 20 seconds to re-check the room with the flash out of the way, coming straight back if it is still dark.
- The camera scanner no longer comes back frozen after minimizing the app or switching away. A watchdog now watches for frames actually arriving (instead of trusting what the camera claims) and restarts the stream within a few seconds whenever the picture stalls.
- The scan result's photo strip and main image now always agree on which picture is showing. The ring sits on the picture actually on display, and catalog art still being checked against your photo is marked "checking" rather than silently claimed as final. Every other place that shows a scanned item's picture, from the inbox list to the capture drawer, now follows the same rule, so a picture never means two different things on two screens.
- Three fixes from reviewing the new scan tuning: the lens=wide override now selects the plain wide camera instead of accidentally matching a Wide-named composite lens, the diagnostics readout reports the settings actually in force rather than echoing the raw URL, and decode timing stats no longer count pauses as attempts.

## 2026-08-04

### Improvements
- Adding ?diag=1 to the scanner URL shows a readout of what your device is actually doing: which decode engine is running, which camera lenses it exposes, whether continuous autofocus was accepted, whether zoom and torch are available, and live decode timings. One tap copies it. It is off unless asked for, and the scanner now warns instead of staying silent when a browser refuses continuous autofocus.

## 2026-08-03

### Features
- New Configuration to Use from a script page: copy-paste code for creating and reading records from your own scripts, in curl, Python, Node.js, or TypeScript, generated from your workspace's own kinds and fields. Answer a few questions and it mints a scoped API token for you (limited to one record type and action), and the snippet loads it from an environment variable so the token never lives in your code.
- API tokens can now be scoped to a single record type and one action (create, read, write, or delete), and carry a provenance label recording where and why they were minted. A script or integration gets exactly the access it needs and nothing more, and a leaked token can only do what its scope allows.
- The barcode on a pending scan is now editable, a hint that names the correct barcode actually fixes it, and an added photo can drive a re-identify.

### Improvements
- A barcode result now appears in the same place as a photo capture: a sheet above the shutter rather than a card floating over the middle of the viewfinder. Same information, same buttons, one surface whatever you point the camera at.
- The camera's capture drawer now works the way the design intended: swipe up (or tap the grip) for the full sheet, swipe down to put it away, and the viewfinder dims while it is open so a stray barcode cannot interrupt an edit. A barcode result opens that same sheet and collapses back to the mini drawer instead of vanishing. Scanning a serial or asset code no longer shows a random web guess as the name; it stays blank and the photo does the identifying.
- One item, one sheet: swiping a scanned barcode item back up now reopens the same Scanned sheet it arrived in, instead of a different photo-shaped one. Swiping the mini drawer down dismisses it. The Scanned sheet got the platform quantity stepper (big thumb buttons, no tiny number box), a cleaner action row (photograph-it and Save & next side by side, Discard and Nice photo tucked below), and the type-a-UPC row hides while any sheet is open. When a photo can't be identified the drawer now says so instead of silently reading "Captured item".
- The Ask Cobb button now shows Cobb rather than a generic sparkle, and when no AI is connected he turns up resting on the notice that says so.
- The capture drawer now slides between its tall and short sizes instead of snapping to the new one in a single frame. The scanned-result sheet grows the same way as its details arrive.
- Scan actions no longer wait for the lookup: press Discard, Not it, or the plus shutter the instant a result sheet appears and it closes right away, with the action running the moment the scan lands. The Nice photo button is gone because the plus shutter now knows where a shot belongs: on a barcode item it becomes the display photo, on a photo item it joins the record.
- Pending receipts on the Purchases page now show their order/invoice number, so three receipts from the same vendor are told apart by what identifies them rather than by a line count.
- While a scan sheet is open the camera now truly freezes behind it, and the shutter becomes a plus: pressing it puts the sheet away, wakes the camera, and your next shot attaches to that item instead of starting a new one. The paperclip icon is gone; the drawer says what the next shot will do in words.
- The Purchases header is one row that never wraps, and the receipt drop-box address is the same one-press chip the scan inbox uses instead of a wall of text on its own line.
- Nice photo and Not it now arm the camera shutter, so you frame the shot full screen with the real shutter instead of a small capture window stacked over the viewfinder you were already looking through. The paperclip on the shutter means the same thing everywhere: your next shot attaches to the item you are looking at. The buttons on a scanned result are also all proper thumb targets again.
- A scanned barcode result now asks for a decision: Save & next, Discard, or one of the camera actions. The ambiguous X and the swipe-away are gone, so a result never disappears without you choosing what happened to it. All of the sheet's buttons are also there from the moment it opens, lighting up when the lookup lands, instead of appearing one at a time.
- On the Use-from-a-script page you can now scope one token to several record types at once. "Which kinds can it touch?" adds a "Pick kinds" option with a checklist of everything in the workspace, so a single script that logs computers and updates locations gets one token with exactly those kinds and nothing more.

### Fixes
- The Live box now sits below anything that covers the page, so modals, panels and the camera scanner are no longer floated over by it. It never appears over the viewfinder.
- Floating chrome now gets out of the way of every full-screen surface, not just some of them. The Quick Access pill and the feedback button no longer sit on top of the command palette, an image lightbox, the guided tour, a confirm dialog, or a mobile nav drawer.
- Changing the quantity on a scanned item now saves however you leave the sheet. Setting 2 down to 1 and swiping the sheet away used to revert it, because the count was only written by the Save and next button.
- Four scan fixes from shelf testing. Committing an item with a very long catalog title no longer fails with bad request body; the name is trimmed to fit instead. The scanned-result sheet no longer offers to file the item anywhere, since scans always land in the inbox and routing happens there. The sheet gained a photo strip showing every picture on the item, so a shot added with the plus shutter never vanishes into a different surface, and the drawer thumbnail no longer goes blank after adding one. The scan inbox session header also stops overlapping its End control on phones.
- A scanned barcode result no longer scrolls. Long catalog titles are marketing copy and one of them ran to thirteen lines, pushing Save and next off the bottom of the sheet; the name is now capped at three lines so everything fits.

## 2026-08-02

### Features
- Add a second photo to something you just captured, without it becoming a second record: ＋ Photo in the capture drawer arms the shutter so your next shot joins the same item. Take the informative shot first (the label, the writing on the box), then the nice one. Retake replaces the photo and reads it again.
- Tap the capture drawer to open it: the shot full-size, every photo on that item with a tap to pick which one is the cover, plus its name, quantity and location. The location starts from where you are standing and can be changed for just that one item.

### Improvements
- The capture drawer now has a Confirm button where your thumb already is: it files what you just captured and clears the drawer, so you can deal with an item and move on without leaving the camera. Undo moved down to the small print where it cannot be hit by accident, and an ✕ hides the drawer without filing anything.
- After every camera capture, a drawer above the shutter now shows the photo you just took, the name as soon as it is identified, a quantity stepper, and Undo, replacing the three little notices that used to take turns down there. You can check the shot came out, set "I have 3 of these", and keep scanning without ever leaving the camera. A photo that fails to upload stays in the drawer with a Retry.

### Fixes
- Scanning a barcode no longer shows you the result card and then the capture drawer for the same item. The drawer belongs to the shutter, so barcodes get the result card alone and photos get the drawer alone.
- The barcode scan result no longer takes over the whole phone screen. It is a card over the live viewfinder again, so you can keep the item in frame while you read the match. Same for the bin picker and the "which one is it?" prompt.

## 2026-08-01

### Features
- New workspaces get a short guided tour on first load, once per user and only while the workspace is still empty. It welcomes you, asks whether you want the navigation across the top or down a sidebar (and switches the whole app live as you try each), then spotlights the key parts of the dashboard one at a time. Skippable, and replayable any time from the account menu.

### Improvements
- The Build page no longer opens with a wall of chips asking which things your change touches. Describe what you want and Cobblr works out what it touches from your own words and field names, shows it back to you, and keeps the full picker one click away under "Change" for the rare miss.
- Cobb now shows you what he is doing on the Build page. He is at the bench while a prompt is being written, a paste is checked, a build is applied or a repair prompt is drafted, where before those steps showed nothing but a greyed-out button. When something you typed matches a ready-made setup, he turns up to point at it.
- The words for adding things to a workspace are settled: modules and bundles, everywhere. The same objects were called trackers, recipes, presets, specialisations, or starter packs depending on which screen you were on; those are gone from headings and buttons. A named copy of a module is a category (the "+ New thing" dialog is now "+ New category"), and a machine's Specialisation picker is now Machine type.
- An inventory list's tabs now read as tabs rather than a path, and the first one is named after what the list actually holds, so a Yarn list says "Yarns" instead of "List". Its Settings tab is one panel instead of two: categories sit with the other fields, you can rename a field by clicking its name, and the copy says plainly what you can change here and where to go for the rest.
- A brand-new workspace's dashboard no longer opens with "Pin a saved view here" and "no activity yet" placeholders, and the "New thing" dialog now offers every kind of thing you can track, turning one on for you as part of Create instead of telling you to go enable it elsewhere. Small dead ends fixed too: the bundles page's empty list points at the marketplace above it, a deleted category's page links straight to creating a new one, and the machine-type hint opens the marketplace already filtered.
- The dashboard's "More ways to start" browser and the Bundles marketplace now look like one catalog: the same tiles and the same two sections (ready-made bundles, then full setups) on both, instead of two different grids showing the same list.
- Scanning a barcode shows its name and picture noticeably faster: the scanner no longer waits for the catalog image to be archived before answering, and the scan card checks for late-arriving results more often.

### Fixes
- Assets can be created and updated with flags again; setting any flag previously failed with "Malformed identifier or value". Bulk scan imports also no longer drop the occasional photo when the database is busy.
- Opening a bundle's details from the Build page, Configuration, a field or a wire now shows the bundle right where you are instead of throwing you onto the Bundles page. Anything you had typed is still there when you close it.
- Ask Cobb now addresses the person actually chatting. When a workspace runs on an AI connection someone else shared in (for example a Claude bridge), Cobb used to greet everyone by the connection owner's name; it now uses the signed-in user's name and ignores any name the underlying connection's environment suggests.
- A pool no longer sends a second print to a printer that is already spoken for. A printer counted as free while its file was still uploading, while the manager had the job but had not started it, and while a print was paused, so two plates could go to one machine.
- The floating Live pill (and the Quick access button) now hide while Ask Cobb or the notifications panel is open, instead of floating over the message box.
- Every modal now fills the screen on a phone instead of floating as a cramped card, and the Live pill, Quick access and Feedback bubble step out of the way while one is open rather than covering its buttons. The scan inbox session row also stopped overlapping itself: it wraps when it needs to, the location button reads "Set location", and the longest labels shorten on small screens.
- Ask Cobb and the notifications panel now open as a proper full-width sheet on a phone, starting below the navbar and ending above the home indicator. They were sized with the browser's full viewport height, which on iOS is taller than the screen you can actually see, so the message box at the bottom was cut off.
- Typing a plain word like "yarn" on the Build page now offers the ready-made Yarn bundle instead of nothing, in both build modes. It only stayed quiet before if you happened to phrase it as "track my yarn stash" in eight characters or more. Notifications about an emailed receipt now open your scan inbox instead of dropping you on the dashboard, and a reply to your feedback just gets marked read rather than navigating away.
- A scan session header no longer claims a filing location the whole session does not have, and says how many items still need one. Categories are shown using your own word ("Books", not "Book"), and a session only offers to file under a category when the destination can actually store one. Replaying an item no longer destroys the snapshot that powers "Put it back", so a hand-picked catalog photo can be recovered.
- Undoing a scan import now works when the import replaced existing items; repeat syncs no longer store duplicate copies of unchanged photos; imported sessions keep their original origin (an emailed receipt still reads "emailed"); and assets whose flags were corrupted by the earlier bug are healed automatically.

## 2026-07-31

### Features
- You can now turn AI off for a single workspace. On Configuration then AI, owners and admins get a "Use AI in this workspace" switch: off, that workspace runs in basic mode and Cobblr makes no AI calls on its behalf, even when the server or your plan has AI available. A member's own personal AI key still works there. The status banner now also says where an "on" workspace's AI comes from (your plan, your personal connection, or a workspace key) so it stops contradicting the "nothing connected" state below it.

### Improvements
- On Configuration then AI, a job you can't pin yet (because AI comes from your plan and this workspace has no provider of its own) now shows a small info hint inline instead of an edit pencil that opened a dead-end modal. The pencil only appears when there's actually a provider to pin to. If a pinned job's provider is later removed, opening it now offers a Clear pin action rather than a wall of text.
- When AI is turned off for a workspace, the page now says plainly that AI is available from your Cobblr plan (or a connected workspace provider) and that flipping the switch back on will use it, so "off" reads as a choice rather than a dead end.
- Cobb turns up where he has something to say. Ask Cobb shows him at the bench while he's thinking instead of a grey dot, holding out the result when a build lands, and pointing at what he's proposing while it waits on your confirm. The Build page greets you with a proper hello: he stands beside a speech bubble with the mode picker tucked in next to him, in place of the small icon in a tall empty box.
- Cobb has a new look. The assistant who greets you on the Build page and heads up Ask Cobb was redrawn by an illustrator: a proper faceted cobblestone head with a sprout, a leather work apron, and a friendlier face. His four moments now read at a glance, and the "working" one finally shows the work: he's shaping a stone on his stump while your build runs, instead of the old snoozing pose that looked like he had dozed off on the job.
- "More ways to start" on the dashboard now opens one browse surface with a single search box and three clear sections (ready-made trackers, full setups, start from a blank slate) instead of two columns. Kinds you already track are offered as another category of the same thing, so the blank-slate list no longer shrinks to a handful of leftovers as your workspace fills up.
- Moving a scan inbox between Cobblr instances now carries the whole item, not just its current state: each item's history, your typed hints, its routing suggestions, the manufacturer, and the scan session it belongs to (with its label, order ref and original receipt) all come across, alongside the photos that already did.

### Fixes
- Fixed backups still piling up in Google Drive: a large backlog made the cleanup take so long the run was lost before it finished, so retention never took effect. Cleanup now runs in parallel and no longer blocks recording the backup, so Drive is trimmed to your retention count reliably.
- Items whose only picture is a catalog link now keep that picture when a scan inbox is imported into another workspace, instead of arriving blank.
- Importing a scan export no longer fails partway with a database error; routing suggestions are written in the format Postgres expects.

## 2026-07-30

### Features
- Configuration is five sections instead of 34 tiles. The hub opens to a card per section (Workspace, Build, People, Connections, System, plus Cloud on a hosted instance), nothing hides behind "Show advanced settings" any more, and you only see settings whose module is switched on and that your role can use.
- If the Cobblr desktop app is running on your computer, it now appears under Configuration, Connections, Devices with its Bluetooth printers and a connection test.
- The switch for letting AI pick your catalog photos automatically now also lives on the AI settings page, next to the connections that pay for it, instead of only as a chip in the scan header. With it on, re-running AI on an item re-picks its photo when your correction actually changed the question, so fixing a colour gets you a new photo instead of the old one.
- Scanned items no longer end up in near-duplicate categories. Cobblr now recognises that "apparel", "Apparel" and "clothing" are the same thing and can agree on one label for a whole scan session, so three shirts scanned together belong in one section instead of two. Items whose name was stored cut off mid-word also display correctly now, without needing a re-run.
- The colour now shows up in the item's name, not just in the photo search. An identified shirt reads "…T-Shirt, Black", and if you correct the colour the title is corrected too instead of keeping the old one. Brand names that contain a colour, like Red Heart yarn, are left alone. Separately, every AI job on the settings page now has a plain English name instead of falling back to its internal id.
- Filing a whole scan session now asks where the things go instead of quietly saving them with no location. If every item already has a spot, or you have an active bin set, it files straight away as before. Otherwise it shows the rooms and bins, and filing without a location is still one tap if that is what you want.
- A correction you type now sticks to the item. Tell Cobblr "color: black" once and every later re-run still knows it, instead of forgetting and re-answering with the thing you already corrected. Corrections given before this are recovered from the item's history, so you never have to type one twice.
- Corrections now build on each other instead of replacing each other. Tell Cobblr "color: black" and later "it's the loose fit" and it keeps both. Tell it two different colours and the newer one wins. The AI is shown everything you have said about the item, in order, and told that the most recent correction is the strongest.
- Scan a batch of similar things and they now land in ONE section instead of two near-identical ones. The session header reads "File all 3 into Clothing", and filing files them all under that. Cobblr also shows the identify which categories your workspace already uses, so it reuses "Clothing" instead of inventing "apparel" in the first place.

### Improvements
- Backup destinations now have a "View backups" button that lists the backups actually living in the destination right now (Google Drive folder, NAS folder, or bucket) with their real date, time, and size, instead of only "last: ok". And once Google Drive is connected the button reads "Reconnect Google Drive" rather than the confusing "Connect Google Drive".
- Settings pages have a way back. Every one now shows a breadcrumb to the section you came from, Modules reads as a page instead of a dialog stranded on one, Wires lives at `/wires` rather than `/bindings`, and activity feeds stop growing forever: they cap and scroll like the one on your home page.
- Settings pages line up. They shared no content column, so moving between two of them shifted the layout sideways and changed how wide it was. There is one column now, and the pages that hold a table get a wider one on purpose.
- Cleared every em dash from the app's own writing: bundle descriptions, label sizes, field help, error messages and what Cobb says back. The copy reads plainer and sounds less machine-written.
- AI call details are readable now. Prompts and responses render as labelled sections with the chat turns broken out, one click from the raw JSON, and any image the call carried is shown. Token counts read "tokens not reported" when a provider did not send them, instead of a zero it never claimed.
- A scan session's header now shows where its items are headed and lets you set one location for the whole session before filing, and a session's category reads the same on every card, chip and note instead of drifting between "Clothing", "clothing" and "apparel".
- Every settings page now wears the same header: same size, same weight, same sentence case, with the description written in one place instead of two. Permissions no longer prints its own title twice. On Integrations, the four blocks share one card shape rather than three. The activity log, the background queue and the OpenAPI browser fill the window and scroll inside it instead of running down the page.

### Fixes
- AI call details show the whole prompt and the image that was sent. Long prompts were being cut to their first 200 characters, and vision calls stored no picture at all. Prompts are now kept in full and images are stored as a small thumbnail.
- The AI settings page no longer claims "No AI providers configured" when your workspace is running on a personal connection someone shared with it. It now shows whether AI is on, what is powering it, and lets the owner pick between shared connections again.
- Google Drive backups no longer pile up forever: retention now applies to Drive (it keeps the newest N and deletes the rest, like the NAS and S3 destinations already did). Redundant backup runs are also suppressed, so a deploy or restart can't fire a burst of near-identical backups, and backup filenames now include seconds so two runs in the same minute stay distinct.
- Settings pages sit where every other page sits. The content column was pinned to the left of the window instead of centred, which left an empty band down the right on a wide screen.
- A settings page's main action sits at the top right, beside the title, the way the other pages do it. On Apps the New app button had dropped below the heading on the left, and Member portal's preview link had done the same.
- Settings pages all use the same width now, apart from the bundle catalog. Scan rules rendered its heading one word per line with the description on top of it; that is fixed, along with the same fault on Apps, General and Member portal.
- Ask Cobb stops failing with "edge request timed out" on longer answers. When the AI runs tools against your workspace a reply can take a few minutes; Cobblr was giving up after two and discarding an answer the bridge had already finished.
- On a phone, a location's name gets the full width and its buttons sit underneath, instead of the name being squeezed into a narrow column and drawn on top of them.
- On a phone the layout planner's size boxes no longer run off the edge of their card, and a location row shows its full name instead of trimming it to make room for the word "container". The tags and files headings now match the other section headings on the page.
- Creating a role no longer offers capabilities from modules your workspace has not enabled. The role dialog is bigger, groups capabilities by the module they come from, and shows the ones you have picked the way the finished role shows them.
- The same category now reads the same way everywhere. Cards were showing "Clothing" on one item and "clothing" on the next in the same session, because each chip printed whatever wording that item happened to be given. The File all button also stops promising to file when some items still have no location: it now says "Place and file all 3" and tells you how many need a spot.
- The category Cobblr shows on a scanned item is now the one that actually lands on it when you file it. The tidied-up value was being worked out and then dropped, so the raw wording from each item's own AI pass is what got saved, which is how three shirts scanned together ended up under two near-identical categories.
- Telling Cobblr an item's colour now actually changes the photos it finds. Before, a colour was only known if the thing you were filing into happened to have a colour field, which most do not, so typing a hint like "color: blue" changed nothing. Now your hint wins, the AI reports the colour it can see, and either one goes into the image search and the ranking. The photo strip also shows you the exact phrase it searched, and refreshes after a re-run instead of showing the previous results for five minutes.
- Closed three gaps left by today's scan-photo fixes. Four photo-gallery endpoints were still returning an item without composing the colour into its title, a truncated name was only being cleaned up on one of the paths that can produce one, and nothing would have noticed if the server image lost the fonts that make the AI photo picker's tile numbers readable.
- Fixed a re-run silently throwing away the correction you typed. If a barcode re-run ended up naming the item from your photo, that last step wiped your research hint, so any colour you had given it disappeared and the photo search reverted to a phrase without it, mid-run. Your hint is yours now: no automatic pass can delete it.
- Fixed "Pick best (AI)" failing with "no vision provider is configured" on workspaces whose AI runs through an edge bridge or a local model. The feature only ever worked on two of the five AI providers. It now sends the candidate photos as one numbered contact sheet instead of ten separate images, which works on every provider, costs a fraction of the AI usage, and lets the model compare colours side by side. When it does fail, the message now tells you which of the three possible causes it was.
- Fixed the colour disappearing from an item's title at the end of an AI run. The colour is now composed into the title when the item is read, instead of being written into the stored name where any of the nine passes that rewrite a name could drop it. Replaying an item shows it too.
- Fixed items ending up with a name cut off mid-word, like "…SS T-Shirt (Men's" with no closing bracket. When an AI reply gets cut short, Cobblr still rescues what it can, but it now trims the name back to its last complete part instead of keeping a visibly broken one.
- Wires and the activity log use the same column width as the rest of settings. Both were rendering wider than their content needed. Recent wire activity now scrolls within a fixed height instead of running down the page.

## 2026-07-29

### Features
- Edge bridges now lists the bridges Cobblr reaches directly, such as one on your own computer or a Pi on your network, alongside the ones that dial in.
- The Live box now shows the printers your edge bridge is holding, from any page: whether each is connected, idle or printing, its loaded roll and battery, and buttons to connect, disconnect or check it.
- A Floor Plan tab on the Locations page. The visual layouts were always there, but buried one click into each location. Now the Locations page opens straight onto a Floor Plan tab: pick a room to see its top-down plan, or a container to see its drawers and shelves as a front elevation at true scale. The old tree is still there under a List tab. Nothing drawn yet? It opens on List so you are not staring at a blank canvas, and the tab is right there when you want to draw.
- The scan inbox can now pick your catalog photo with AI. Tap "Pick best (AI)" on an item's photo options and a vision model looks at every candidate and chooses the cleanest one: the product by itself, in the right colour, with no people in the shot. The free photo ranking also got better at colour, so an item you know is black stops surfacing a red one.
- You can now have Cobblr pick catalog photos with AI on every scan instead of pressing the button each time. Turn on "Auto-pick photos" in the scan header and each newly identified item gets the cleanest product shot chosen for it. It stays off until you turn it on, never replaces a photo you chose yourself, and never pays twice for the same item.

### Improvements
- Labels whose shape leaves the caption almost no room beside the QR now print turned by default, so a name like "Thumper" reads across the label instead of wrapping to three letters a line. Working the turn toggle yourself still overrides it.
- The printer battery now shows as a gauge that fills with the charge actually left, instead of a three-step icon that showed the same picture for 76% and 40%. It turns amber before a run is at risk, and a charging printer is never flagged as a problem.
- When adding a printer on an edge bridge you now pick the bridge from a list and ask it which machines it serves, instead of typing its address and instance name from memory.
- The Labels page now says when the media size came from the printer itself ("as loaded"), and warns with a one-click fix when your pick disagrees with the roll the printer reports. The Printers settings page shows what each printer last reported it had loaded.
- "Pick best (AI)" now uses what the scan already knows about the item. If it's clothing, the AI is told to pick the garment alone with no person and the right colour; if it's a food or boxed product, it knows the front of the package is the correct photo instead of rejecting it as clutter. The heuristic also hands the AI a tidier shortlist now, dropping obvious junk and duplicates first, so the AI is choosing between good options rather than rescuing a bad strip.
- Developer tooling: the browser-Bluetooth printing path can now be exercised against virtual printers, including the failure modes that are hard to stage on real hardware (a printer that accepts every command and prints nothing, one that is paired but unreachable, picking the wrong one of two identical units).

### Fixes
- The Labels page keeps the roll a printer last had loaded, even when the printer reports nothing loaded, and says so instead of asking you to pick the size again.
- Locations no longer drops you into Configuration. Opening a room or bin from the navbar used to jump to a second copy of the page inside the settings shell; the place tree now owns one URL (`/locations`) and stays where you started. Old `/configuration/locations` links redirect.

## 2026-07-28

### Features
- Test a bridge-connected printer from Cobblr and see the roll it has loaded and its battery level.
- Connecting a printer now asks one question, whether you want to install a helper app, and finds printers on a bridge for you instead of asking for an address.
- Print to a label printer attached to an edge bridge on your own computer, straight from the browser, with no relay token and no pairing to the cloud.

### Improvements
- A label printer connected through an edge bridge now says "via edge bridge" in the printer menu instead of "Network", is no longer offered US Letter as its media, and drops the transport note the bridge appends to its name.
- When a printer will not connect through your browser, Cobblr now says why and offers the way that does work, instead of showing the browser's raw error.

### Fixes
- Adding a printer found on an edge bridge works, the printer list no longer offers lasers and 3D printers, and if no bridge is running you get the command to start one.

## 2026-07-27

### Fixes
- Fixed a fully charged label printer being shown as "charging" when it was not plugged in, and stopped presenting its battery reading as a voltage it never was.

## 2026-07-26

### Features
- Any part you have bought before now shows a Price history panel on its own page: what you paid last time, how much it moved since the purchase before, a chart of the unit price over time, and every purchase with its date, vendor and order. Needs Inventory and Purchases both on; parts you have never bought show nothing.
- Label printers now report the roll they have loaded and how much battery is left, so you can check before starting a run instead of after wasting labels. Printers connected through an edge bridge are asked automatically, and the Labels page sets the media to the roll they report.
- The Purchases list got a real overview: a stat strip across the top (orders, in transit, how many came from receipts, and total spend for the current filter), vendor chips, a receipt icon on receipt-imported orders, an item-count column, a running total in the footer, and sortable columns.
- Purchase orders got a real detail view: line items now link to the inventory item they created (with a thumbnail, and a clear flag if the part was deleted), receipt-imported orders show the actual receipt inline with a View button, and the vendor, order number, date, and total are filled in automatically at import instead of leaving the order blank. The footer reconciles the line items against the order total so a mis-read receipt is obvious.
- Purchases now shows the "Email receipts to <address>" forwarding chip (the same one on the Scan page), and a "N receipts pending confirmation" banner for receipts you've forwarded but not yet turned into orders, linking straight to the scan inbox to confirm them.
- Forwarding or uploading a receipt you already imported (same store and order number) no longer silently duplicates every line. Cobblr tells you it's already in your scan inbox and offers "Import anyway" if you really want a second copy. A receipt you'd discarded re-imports normally, and a receipt with no order number is never treated as a duplicate.
- Receipts that aren't a neat table now read without AI. A PDF or emailed receipt with no ruled line-item table used to cost an AI call; it's now read line by line for free. The reader only accepts a result when the line items add up to the receipt's own subtotal, so it can't hand you a confident wrong price: if the arithmetic doesn't reconcile it passes the job to AI instead. On a test corpus of eight store layouts it read every amount correctly, and invented nothing.
- Connecting a serial label printer now detects the loaded roll size automatically, so there is nothing to fill in.
- Label printers that connect as a serial port, including Bluetooth Classic models a browser cannot reach over Bluetooth, can now print from Cobblr.
- Two printers of the same model are now told apart, so a job cannot print on the wrong machine, and network label prints get the same edge protection against paper drift as Bluetooth ones.

### Improvements
- The label size and printer pickers now lead with what you actually print, ordered by most recently used.
- Connecting a label printer is now a single "Connect a printer" step that finds your printer for you, instead of asking you to choose between Bluetooth and serial, which are two ways Cobblr reaches the same kind of printer rather than two kinds of printer.
- The printer picker now shows the roll size and battery your printer last reported, instead of a bare "Bluetooth" line, so you can see what is loaded before you print.
- The Purchases page now shows an insights panel: your spend over the last six months and your top vendors by spend, both computed live from your orders.
- Viewing a receipt now shows a clean, itemised receipt built from the parsed order data instead of a raw email/text dump, with a "View original" link to the untouched photo, PDF, or email. A Tax / other line fills any gap between the line items and the total so the figures always reconcile.
- Click a vendor to open its record: total orders, total spent, and typical lead time, plus contact details and the full list of that vendor's orders (click one to jump to it).
- Receipt lines now recognise the part you already have when the receipt abbreviates its name. A line reading "SHRD MOZZ 8Z" finds your "Shredded Mozzarella" and offers to add to it, instead of quietly creating a second part every shopping trip (which also split that part's price history). Works both ways and on anything, not just groceries: "GALV WSHR" finds "Galvanized Washer".
- Forwarding a receipt that couldn't be split into line items now says so honestly ("saved as one note to sort") instead of a vague "we've got your receipt", and the exact same email delivered twice no longer re-imports and re-notifies. Re-forwarding a receipt is still treated as a new action.
- Added a setting that asks a label printer not to feed to the tear bar between prints, aimed at plain rolls with no size code costing a blank label per print, and the printer now says when it cannot read a roll instead of leaving the readout blank.

### Fixes
- A printer that cannot report its media size now says so and asks you to set the label size, instead of quietly assuming 40 x 30 mm and printing that guess onto real labels.
- Fixed the printer connect buttons disappearing once you had one printer, made "Connect a printer" in the labels queue open the Bluetooth chooser instead of navigating to settings, and replaced the browser's "Unsupported device" error with an explanation and the route that actually works.
- Fixed connecting a serial label printer failing at the final save step, made the roll and battery queries take turns so the battery reading actually arrives, and a printer with a plain uncoded roll now connects with sensible defaults instead of being reported as silent.
- Fixed a serial printer being reported as "didn't identify itself" when it had actually answered, by waiting long enough for a Bluetooth link to wake and matching each reply to its question by content rather than by the order it arrived in.
- Fixed connecting a serial label printer failing with "the port is already open" when that port had been opened earlier in the same visit.
- Fixed a serial-connected label printer being treated as a sheet printer, so it defaulted to US Letter with 20 labels per page instead of its own roll, was skipped by auto-print, and was ignored when a module asked to print something.
- Fixed a label printer failing to report its roll size whenever it was plugged in to charge.

## 2026-07-25

### Features
- Pairing a Bluetooth label printer now lists printers instead of every Bluetooth device in range, with a fallback for printers that do not announce themselves.
- Each printer now remembers the label size loaded in it, saved with your workspace, so it is right when you come back or print from another computer.

### Improvements
- Groundwork for one Cobblr account across every surface: a surface can now verify a central identity token and link its existing users to that global account by email. Off by default (unset IDENTITY_URL keeps each surface owning its own accounts, unchanged).
- Groundwork for time-boxed demo workspaces: an operator can provision a blueprint-seeded demo into an existing account with its own expiry and per-demo feature unlocks (so a demo can use what the free trial locks), and the owner can convert a trial or demo into a kept workspace. Backend only for now; the expiry banner and Keep button come next.
- The printer panel on the Labels page now shows print position and darkness, and reads the label size from the page's own picker instead of asking for it twice.

### Fixes
- Committing a receipt or scan no longer drops item images that were still being fetched, and you can now send a whole committed session back to the inbox in one tap (plus an Undo right on the "Confirm all" toast) instead of reverting items one by one.
- Sending a receipt's committed items back to the inbox now also removes their purchase-order line items and deletes the order once it is empty, so undo leaves no orphan order behind. Committing a big receipt is faster too: the item image is downloaded in the background instead of blocking the commit. "Send whole session back" now reverts the entire session even when it is larger than the recently committed list shows.

## 2026-07-24

### Features
- Forward a receipt email even when it has no file attached, and always hear back. Lots of store receipts arrive as an email body, not a PDF. Forwarding one to your `receipts+...@` address used to do nothing: the app only looked for a file attachment and silently dropped the body. Now, when there's no usable attachment, the email body is captured too. A body that reads like an item list ("2x PLA, 3 rolls tape") parses into one inbox row per line; anything else lands as a single note, so a forwarded receipt never just vanishes. Either way you get a reply telling you what landed ("Imported 4 items into your scan inbox") or that nothing usable was found, so "I emailed it and nothing happened" can't happen anymore.
- Forwarding a store receipt email now works, and the reply talks back to you. Most receipts arrive as an email you forward, with the receipt in the message body or its html, not as a file. That used to come back empty because the receipt content was being stripped before we ever read it. Now the full forwarded body and its html are read, so a forwarded receipt turns into inbox items like an attached one. The reply you get quotes what you sent and comes with Reply-To set to your receipts address, so if we could not find a receipt you can just reply with it attached and it lands straight in your inbox.
- An emailed receipt is never lost, even if we hiccup processing it. You send an email once. If something on our end fails to turn it into inbox items, that's ours to fix and replay, not yours to resend. Every message to your `receipts+...@` (or `reply+...@`) address is now stored raw the moment it arrives, before anything tries to parse it, and the outcome is recorded alongside it. So a message an older build would have dropped, or that hit a transient error, can be reprocessed from the backend through the current pipeline without asking you to send it again.
- Pick where a label prints, right from the toolbar. The printer chip on the Labels page is now a target selector: choose a saved printer or System print (the browser ⌘P dialog), and the whole toolbar follows your choice. The media list, the print button, and the preview all match the active target. So a Bluetooth label printer that is off, put away, or just not what you want right now is no longer a dead end: switch to System print and every size is available again (US Letter, 4x6, Avery, any roll). Your saved printer stays saved. Adding a printer, its settings, and Forget all live in the same menu, with Forget kept separate and behind a confirm so you never lose a printer by accident.
- Print a landscape label in portrait. A new Rotate 90° toggle on the Labels page turns a label's content a quarter turn, so a wide face like a 50 × 30 mm roll reads portrait when you stand the sticker on end: the title runs up the long side with the QR beside it. The toggle only shows for non-square labels, since a square turns into itself, and it is remembered for next time. The on-screen preview and the browser (⌘P) sheet both reflect the turn, so what you see is what prints. Bluetooth and direct-to-printer output do not rotate yet; that follows in a later update.
- Custom label sizes in mm, and a way to un-print a batch. The New label size dialog now has an in / mm toggle, so you can enter metric stock (50 x 30 mm) without converting by hand. It switches in place, keeping the physical size, and remembers your choice. And when a Bluetooth print does not come out right, the toast now offers Return to queue instead of a blind reprint: it puts those labels back where they were (reusing their codes) so you can fix the size and try again, rather than reprinting the same thing.
- You can now set or fix the order number on a receipt session. The parser reads an order or invoice number when the receipt states one, but sometimes it is missing or wrong. A receipt session header now has an editable PO number: add one so two receipts from the same store are distinct, or correct the parsed one, and the session title updates to match.
- A receipt in your inbox now keeps its original, and you can re-parse it or re-run any line. A receipt session remembers the exact thing it was read from, so "Original" opens the source (the PDF, the photo, or the emailed receipt) full-screen, and "Re-parse" re-runs the reader on it and replaces the still-pending lines when the first read was weak. The per-line re-run button also works now for lines that have only a name (a receipt line), so you can fetch a product image or re-identify it instead of being stuck.
- An emailed or uploaded receipt now lands as its own labeled group in the scan inbox. Before, a receipt's line items dropped into the inbox under a plain timestamp and could get mixed in with whatever else you were scanning at the time. Now each receipt is its own session with a clear header: "Receipt and the vendor" when the vendor is detected, otherwise just "Receipt", and an emailed one adds "emailed" with the time. The line items are grouped under that header, so a whole receipt reads as one thing you can triage together.
- The reply to an emailed receipt now comes from your receipts address and threads under your email. When you email or forward a receipt, the reply you get back now comes from the same `receipts+...@` address you sent to, nests under your original message in your mail client, and quotes what you sent. So it reads like a real reply in the same conversation, and hitting reply with the receipt attached drops it straight into your scan inbox.
- A receipt session now shows its order number, so two receipts from the same store are easy to tell apart. When a receipt states an order, invoice, or confirmation number, the parser reads it and the inbox header shows it, for example "Receipt and KC Tool #384602" instead of just "Receipt and KC Tool". If there is no number, nothing changes.

### Improvements
- One image viewer everywhere, and every photo opens full screen. Enlarging an image used to look different depending on where you clicked: the scan inbox had one viewer for web photo results and a separate, more cluttered one when you tapped the catalog or your own shot, and item galleries had no zoom at all. They are now a single viewer: click any photo (a scan's catalog or your capture, a web search result, or a photo attached to a part, asset, vehicle or machine) to open it big on a dark backdrop, click it again to zoom in to actual pixels so a VIN, plate or label reads, and flip between the related shots with the thumbnail strip or the left and right keys. Escape or a click outside closes it.
- When configured, barcode scanning can consult a shared hosted intelligence source (BIdb) so a fresh install resolves more products without its own lookup keys. Off by default, opt-in for self-host.
- Printing to a Bluetooth label printer got a lot smoother: the media and label size pickers now show right in the toolbar (funnelled to the rolls that printer can feed) and drive the print, instead of being hidden in the printer settings. The printer connection is reused so it stops asking you to pick the device again each time, and after a print you get a reprint option to the side in case it did not come out. The preview header now reads the clean size (like 50x30mm) instead of a long decimal.
- Multiple pending receipts collapse into one purchase-order prompt. Each parsed receipt used to add its own "Confirm as purchase order" banner, so a few receipts stacked up a wall of them. Now, with more than one, you get a single line ("3 receipts to confirm as purchase orders") with Confirm all, and you can expand it to confirm each on its own. A single receipt looks exactly as before.
- Expired trial workspaces are now swept automatically (they were stamped with an expiry but never cleaned up). Off by default; an operator turns it on with a dry-run first, then live.

### Fixes
- Picking a web-search photo for a scan item now only says "updated" when it actually worked. Some image results can't be pulled in (the site blocks hotlinking, the link is dead, or it isn't a real image). Before, those still popped a success toast while the catalog photo silently didn't change. Now the pick fails cleanly with a "couldn't be used, try another" message and leaves your current photo alone.
- A forwarded order-confirmation email now parses into its individual line items, and the reply is from a clean address. Emailing in a real store order (which has no "2x"/"qty:" shorthand) was being flattened into one useless catch-all item instead of one row per product. Emailed receipts now always run through the receipt parser, so each line becomes its own inbox row under the receipt session. Separately, the reply you get now shows a tidy "Cobblr" sender instead of a long signed address, while replies still route back to your workspace.
- Two fixes for viewing and fetching receipt images. Picking a web-search photo now works far more often: we fetch it the way a browser does (a real user agent and a matching referrer), so images a site would only serve to a browser stop coming back as "couldn't be used." And "View original" now renders the receipt in place, showing a photo as an image, a PDF inline, and an emailed body as text, instead of downloading a file with no name.
- Bluetooth prints now match the on-screen preview. The thermal renderer was a separate, cruder path: it put the QR on top with a single unwrapped line of text below, drew no center code, and ran edge-to-edge so wide media clipped. It now lays each label out the same way the preview does, by shape, name on top (wrapped) with the QR below for tall and square labels, QR-left/name-right for wide ones, draws the short code in the QR center, and leaves a whitespace margin so nothing prints off the edge. Tiled 2-up media also prints a solid cut line between the labels. A cross-package test pins the thermal layout to the same rule the preview uses, so the two can't drift again.
- Label captions now size themselves to the name. A short name like "Office" prints large and fills its space, while a long one like "2019 Honda Civic" shrinks and wraps to two lines, instead of every caption sharing one fixed size (which made short names look tiny and forced long ones to wrap awkwardly). The QR is already at its max, so the name is what adapts. The same fit rule drives the on-screen preview and the Bluetooth print, so what you see is the size that prints.
- Label queue rows show the instance name, not the raw kind. A 3D printer added to the label queue now reads as "3D Printers" (its instance), the way you see it everywhere else, instead of the internal "machines/machine". The row prefix is resolved the same instance-aware way the caption already was, so anything filed under a named instance (vehicles, laser cutters, a custom parts bin) labels with the name you gave it.
- Renaming a label in the queue now updates the preview right away instead of needing a refresh, and there is an undo next to the name to put back what you had. Scanning a printed QR for an item in a named collection (a Vehicle, a 3D Printer) now opens that item.
- Labels fill their height and print the cut line. The caption now hugs the top and the QR fills the space beneath it, so a bigger name shrinks the QR instead of pushing it off the bottom edge, and there is no dead whitespace up top. Both the on-screen preview and the Bluetooth print do this the same way, and both cap the name to the same line count so what you see is what prints. Tiled 2-up rolls now print a solid cut line between the two labels (it was being skipped whenever the printer had not stored a media feed type).
- Opening a printed QR label for an item in a named collection (a Vehicle in your Vehicles, a printer in your 3D Printers) now opens that item, instead of showing "not found" because it landed on the generic page that does not hold it.
- Replying to a receipt confirmation email no longer creates junk in your scan inbox. The confirmation used to invite a reply, and any reply, even a question like "what is this?", got pulled in as an unidentified inbox item. The confirmation now comes from a no-reply address and threads under your original, and a reply to it is ignored. To bring a receipt in, forward it to the receipts address shown on your Scan page, as before.
- The reply to an emailed receipt no longer calls a whole receipt "1 item," and it's shorter. When a receipt can't be split into line items (for example when AI parsing isn't set up), it's saved as a single note so nothing is lost, but the reply used to say "captured 1 item," which read as if a seven line receipt were one thing. Now that case just says "we've got your receipt, see it in your scan inbox," with no misleading count, and the reply emails are trimmed to the essentials.
- Emailed-receipt lines now show their progress spinner and refresh live in the scan inbox. A receipt line gets its name from the parser, not the AI identify, so the inbox mistook it for "already done" the moment it appeared: no finishing spinner, and the inbox fell back to its slow idle refresh, so an emailed receipt looked frozen and you had to reload. Now a freshly-parsed line reads as still-working until it is routed, so its spinner runs and the inbox polls quickly to surface and update it.
- Re-run AI now works on an item that has only a name (like a receipt line), not just barcode or photo items. The button was enabled for these, but the server still refused with "item has neither a barcode nor a photo." Now a name-only item re-runs its name-based lookup, searching the web for a product image and re-routing it, so you can fill an empty photo or re-identify it.
- Scanning a product whose box carries both a barcode and a generic marketing QR (a Nike shoebox's `qr.nike.com` link next to the UPC) now reads the barcode. The camera used to grab the QR link every time and ignore the UPC. Cobblr's own QR labels still take priority as before; only anonymous web links are held back so the product code beside them can win.
- More scan items get a photo automatically. When the auto image search found a product picture, it saved just the top link and hoped your browser could load it. Many product pages block that (hotlinking) or the link is dead, so the tile showed empty. Now it downloads the image into your workspace and, if the best one can't be fetched, tries the next few results until one sticks, so far more items land with a real photo.
- A scanned identifier and every photo you captured now survive the commit. When you scan an item whose barcode decodes to an identifier (a VIN off a vehicle, a serial off a nameplate), that value now lands in the item's real serial-number field instead of getting stranded in hidden metadata where the field could not see it. And when you merge several shots into one item (the barcode plus the plate, say), all of them attach as gallery photos, not just the catalog stock image. Both were silent data loss on confirm.

## 2026-07-23

### Features
- Common thermal label sizes like 50x30 mm are now built in, and you can pick them (and print two-up) even when no printer is connected to Cobblr, by choosing what you are printing on. Every size picker now groups into Label rolls and Sheets and shows only what fits your printer.
- Modules now show an Experimental or Beta badge in the module picker while they are still maturing, and public or trial deploys can hide experimental modules entirely so a half-finished connector never fronts a public instance.

### Improvements
- You can now put things inside an inventory item. Every part's detail has a Contents panel, the same one machines and assets already have, so a container you track as a single part can be itemized in place. Track "box of mugs" as one item today, then open it and add the individual mugs inside when the details start to matter. Nothing converts, and the box keeps its own record and history. It closes the gap where an opaque item could not hold its own contents.
- Filling a container is now a complete flow. From any item's Contents panel you can add something you already track, type a name to Create and add a brand-new one in a single step, or hit Scan in to open the camera already filing everything you scan straight inside. That last one is how you itemize a bag of parts from an invoice: open the bag, Scan in, scan each one, and they land inside with no separate step to move them.
- The Label codes panel now shows every list you could label from the start, each with a suggested prefix, instead of a list only appearing after you print its first label. Keep the suggestion, change it, or clear it to skip a code for that list, all before anything prints. Save locks a suggestion in; if you leave it, your first label just adopts it.
- The Labels preview now shows every sheet your queue will print as a strip of thumbnails, not just the first, so a multi-sheet run looks like what comes out. The sheet and label size pickers also moved to their own row so the toolbar stops wrapping.
- The browser Print on the labels page is friendlier now. It opens the print dialog right on the page instead of a separate tab, and it no longer clears your queue the moment you hit Print. Like a direct printer, it asks you to confirm the paper looks right first, so cancelling the print dialog keeps every label in the queue. The Browse tab you were on is also remembered across a refresh.
- You can now click a label's name in the print queue to rename it, so a long title like "2002 Honda Odyssey Minivan EX" can be trimmed to a short name that fits the label and keeps the QR big. The sheet numbers in the multi-sheet preview also moved below each thumbnail so they stop covering the label text.
- Join a Tailscale hostname by clicking a link, no auth key to mint. Self-hosting Cobblr behind its own tailnet name (`COBBLR_TLS_MODE=tsnet`) used to require pasting a reusable auth key from the Tailscale admin console. Now you can leave `TS_AUTHKEY` empty: on first start the box asks Tailscale to authorize itself, prints a one-time approval link, and you click it and sign in, the same flow you get authorizing any device. Setting a key still works for a fully headless join. Empty-key previously refused to start, so nothing that worked before changes.

### Fixes
- The Labels preview now draws every sheet at the same real-world scale, so a 2 inch label looks bigger than a 1.5 inch one and a 2 inch label matches a 2 inch cell inside a 4x6 sheet, instead of every sheet being squeezed to the same width.
- The label preview is readable again: it now scales so a 4 inch wide medium fills the preview, so small labels like 50x30 mm are big enough to read while a 2 inch label still shows bigger than a 1.5 inch one. Renaming a label is also clearer now, with a pencil on the name in the queue.
- Fixed the Labels page hiding the sheet and label size pickers when no printer was set up, so printing to your normal printer through the browser dialog now lets you choose the sheet and label size again.

## 2026-07-22

### Features
- Ask Cobb about any modal, and let it manage your label codes. Modals now carry an "Ask Cobb" button that opens the assistant docked alongside, pre-filled with a ready-to-send starter about what you are looking at, so it is a helper for the screen in front of you instead of a separate trip (and it works in sidebar mode too, since the button lives in the modal, not the nav). And Cobb can now see and change label codes: tell it "give my 3d printers a p prefix" or "group these by category" and it does it, keeping already-printed codes valid.
- Rename a code prefix even after you have printed labels. A code like `c1` used to lock the moment its label printed, so you were stuck with whatever prefix was auto-picked. Now a printed prefix shows a Rename anyway button: the codes already on your shelves keep working and still scan, and only new labels use the new prefix. The old prefix stays reserved so nothing else can reuse it.
- The Live box now shows a count on the printer icon while your labels are printing, like a print manager in the taskbar, so you can see how many are still to come off the printer.
- Network printers now declare their type (inkjet/laser or thermal) and widest media, so the label size pickers only offer sizes that printer can actually run.

### Improvements
- Ask Cobb can now change workspace-level settings, starting with your label-code prefixes. Ask it to give your 3D printers a "p" so their codes read p1, p2, and it happens through the same confirm-first flow as any other change Cobb makes.
- When you open Ask Cobb for something specific (from a screen's Ask Cobb button), your earlier conversation now scrolls up behind a quiet "Earlier" divider instead of piling under the new question. Nothing is lost, scroll up to read it again.
- When you tap Ask Cobb from a screen (like the Label codes panel), Cobb now opens with a short greeting about what it can do right there, instead of dropping a canned sentence into your input box. Your earlier chat parks behind the "Earlier" divider, and you just reply.
- The printer popover on the Labels page now offers the common thermal label sizes as one-tap presets, so you can set your loaded media without typing the dimensions.
- You can now connect a Bluetooth label printer without leaving the labels page. When you have none, the labels page shows a "Pair a Bluetooth printer" button right there: it pairs, auto-detects the model from its profile, and sets it up as your default in one step. The Printers page opens the same way now, with the printer types shown up front instead of hidden behind a form.
- The label preview has a new Actual size toggle: flip it to see a label at its true physical size on screen, so you can tell at a glance whether the text is too small to read or the QR too tight before you print. Calibrate once by dragging the little ruler to match a real one, and it is saved for that device.
- You can now give a list no code at all. In the Label codes panel, clear a list's prefix box and hit Save (or ask Cobb to remove it), and that list stops getting a human code, which frees its letter for another list so you are never forced to code something you do not want to. A list whose labels are already printed keeps its code and refuses to lose it. Type a prefix back in any time to turn it on again, numbering fresh.
- The Label codes panel is simpler and more capable. It groups your code lists under their kind (Machines, Locations) with friendly names instead of raw ids, drops the confusing "count by" control, and makes "show the code inside the QR" a per-list toggle. So your 3D Printers can print the code while your CNC hides it, even though both are machines.
- The label preview now tells you how big each QR actually prints and flags when it is too small to scan, so you can catch an unreadable code before you commit a whole sheet. It reads out the QR module size in millimetres and turns amber, then red, as the code gets too tight for a phone to read. The printed layout and the on-screen preview also now agree for every label shape, so what you see is what prints.
- A Bluetooth label printer can now fit more than one label on each physical label. Set "Labels across" when you add or edit the printer (2 across a 50mm roll splits each label into two faces), and both Print and Auto-print tile them onto one feed. You can also reuse a layout you already set up on another printer, so a configuration you got right once shows as a "you did this before" option instead of being re-entered by hand.
- Sending labels to a network printer no longer just fires and forgets. It keeps them in the queue and drops a to-the-side prompt, "Mark printed once it looks right?", so a jam or a wrong size doesn't burn the batch. Click Mark batch printed once the output is good and those labels clear; if the send fails, the prompt offers Reprint. Bluetooth printing is unchanged (it already knows the result the moment the labels are out).
- Sending labels to a network printer now shows a count on the Live box printer icon, the same as Bluetooth printing does.
- A network printer with no type set now shows a "Set type" prompt on the printers page, so its label sizes funnel correctly instead of defaulting to a full-size sheet.
- Label size options now match the printer instead of listing every roll and Avery sheet at once. Each printer has a capability, its kind and the widest media it feeds, and you only see the sizes that fit: a 2-inch Bluetooth printer never offers a 4x6, and an inkjet never offers a thermal roll. Pick the printer first and the sizes follow.
- The Labels page now leads with your printer. It shows which printer will print, hides the paper and sheet-layout pickers when your default is a Bluetooth roll (they do not apply to it), and prompts you to connect one when you have none. Adding a Bluetooth printer can now pair and auto-detect its settings from a known profile instead of you typing in the dialect, width, and orientation by hand.
- The label size presets in the printer popover now come from sizes you have already set up, on other printers or as custom sizes, so a layout you use elsewhere is one tap to match here.

## 2026-07-21

### Features
- Auto-print now works with a Bluetooth label printer: pick it under Auto-print and each scan (or every N labels) prints itself from your open Labels tab.
- Auto-print labels as they are added: turn on Auto-print on the Labels page, pick a network printer and a size, and choose to print when a sheet fills up, every N labels, or each one immediately. Cobblr renders and sends the job in the background, records a re-printable batch, and a short cooldown stops a burst of scans from firing a print per scan. Off by default.
- The workspace sidebar now has a Live box: a small, self-hiding home for ongoing session modes. When you have a printer connected, it shows an auto-print toggle right in the sidebar foot, so you can flip label auto-printing on or off at a glance instead of digging through settings.
- The "Claude is driving this screen" indicator and the "use this window?" prompt now live inside the Live box instead of a separate floating banner, so there is one thing in the corner instead of two. The Live box also shows up on mobile and in top-bar layouts now, not just the full sidebar.
- When a bridge scanner is connected, the Live box now carries a "Scans drive this screen" toggle with an Open/Print choice, so you can make any window follow scans from another device without going to the Scan page first.
- The Scan page has a new Print mode: scan an item's QR label and its label goes straight to the print buffer instead of opening the item, so you can reprint a run of labels as you walk the shelves.

### Improvements
- Set up a Bluetooth label printer by the media you loaded: enter the roll width in millimetres and choose the feed type, and Cobblr derives the print width and shows it in inches too. Printers you configured earlier keep working and show their width converted to millimetres when you edit them.
- The label size picker now lets you create your own size. Pick "New label size", type the media sheet and the label measurements, and a live preview shows how many fit before you save. Your sizes appear in the picker next to the built-in ones, and printing uses them like any other. So a 1.5 by 3 inch sheet holding two 1.5 inch squares is a few numbers, not something we have to add in advance.
- You can now define your own label sizes instead of picking only from the built-in list. Give a workspace a media sheet and a label measurement and Cobblr works out how many fit, so a 1.5 by 3 inch sheet holding two 1.5 inch squares just works. This ships the data and the print path; the setup screen for creating one lands next.
- Groundwork for defining a label size by its dimensions instead of picking from a fixed list. Cobblr can now work out how many labels fit on a sheet from the media and label measurements, which is the first step toward setting up your own sizes (for example two 1.5 inch squares on a 1.5 by 3 inch sheet). No visible change yet; the built-in sizes are unchanged.

## 2026-07-20

### Features
- Print your labels straight to a Bluetooth thermal label printer, with no print server and no cables. Add one under Configuration → Printers with the driver "Bluetooth label printer", set the command dialect (most label printers speak TSPL; Phomemo M-series use ESC/POS raster), the media width and the label geometry, then use Print test to check it. From then on, the labels queue's Send to printer prints the queued rows to it: your real QR codes and descriptions, respecting each row's quantity. Your browser asks which printer to use once per session, not once per label. Because the dialect, width and calibration live on the printer connection, supporting a new model is a settings change rather than a code change, and if labels drift off the edge or come out upside down the gap and orientation fields fix it. Note that iOS has no Web Bluetooth, so iPhones and iPads cannot drive these printers from a web page. For iOS, or for printing without a browser open, use a CUPS printer or an edge bridge.
- 1½ inch square labels, two ways. If you already stock 4 × 6 shipping labels, you can now tile 1½″ squares onto one, eight up, and cut them apart: near-zero marginal cost for a sheet of bin labels, paid for in guillotine time. If you would rather not cut anything, there are now die-cut square rolls too (1½ × 1½″ and 2 × 2″) that feed straight through a roll printer's adjustable guide, one label per feed. Both show up in the label picker like any other size, in the preview, the print sheet, and the direct-to-printer PDF.
- Print a label where you are standing. With a Bluetooth label printer set as your default, hitting Print label on any record now prints it immediately instead of only adding it to the queue. Add a bin at the shelf and the label is in your hand, so you can label a whole room in one pass without walking back to a desk. The printer stays connected between labels, so your browser asks which printer to use once rather than once per label. If nothing prints (no Bluetooth printer, or an iPhone, which cannot drive one) the label simply waits in the queue as before.

### Improvements
- Internal: the web-image picker now has a scoreboard built from real cases where someone replaced the automatic image, so changes to how photos are chosen can be measured instead of guessed at.
- The command palette (Cmd or Ctrl and K) now surfaces an exact match first. Type a part's serial number and that exact part leads the list, above looser name matches, the same identifiers the scanner resolves. Results are ordered by how well they match rather than by which list they came from, so the best answer is always at the top.
- Cobblr can now resolve a scanned or typed value against the fields a module marks as identifiers, starting with a part's serial number. A workspace that lasers its own serials onto parts no longer has to write a rule by hand for Cobblr to open the right part when one is scanned. This ships the groundwork; the scan and search surfaces move onto it next.
- Scanning a part's own serial number with the camera now opens that part, even when no redirect rule is set up. Cobblr falls back to the fields a module marks as identifiers, so a workspace that lasers its own serials no longer has to configure anything first. If a serial belongs to more than one part, it asks which one rather than guessing.
- A hardware scanner now resolves a part's serial number from any page, not just the Scan page. Scan a serial anywhere and Cobblr opens that part, the same as scanning it with the camera. If the serial belongs to more than one part it asks which one, and anything it does not recognise still goes to the scan inbox as before.

### Fixes
- Web image search now prefers a picture of the one thing you are cataloguing, instead of a photo of the whole set it belongs to, and no longer assumes a product is square (a book cover, a bottle and a poster are all taller than they are wide). Tapping a result now uses it; the small corner button opens it full size.
- Web image search now checks that a result actually names what you asked for, so searching a specific brand or model number no longer returns a similar-looking product from a different maker or a different model in the same range.
- Scanning a Cobblr label works again. Labels printed since 11 July encoded a shorter code than four of the scanning screens were looking for, so the camera, a USB or Bluetooth scanner, the Scan page and Live Sort all quietly failed to open what you scanned and filed it as an unrecognised item instead. Labels printed before that date kept working, which made it look like the feature had only broken for new labels. All four now read labels through one shared piece of code, so they cannot drift apart again.
- Scanning an item whose barcode catalog has no real photo now finds a matching picture from the web within about a second, instead of showing a grey "no image" graphic over the photo you took. The picture on a scan card is always searched from the name shown on it, so you can tell at a glance whether both are right.
- Scanning a serial number now opens the record it belongs to. Filtering on a native column (serial number, model number, and the rest) matched nothing at all, so a scan rule pointing at a serial reported every part as missing. Serials now also match regardless of case, since scanners report whatever the keyboard layout gave them. When a serial belongs to more than one item, Cobblr asks which one you are holding instead of quietly opening the first.

## 2026-07-18

### Features
- Fill in a whole collection's missing pictures in one press. When some records have no image, the collection header offers a "fetch N missing images" button that looks each one up the same way a single record's Auto does, using what the record actually is (its name plus its own author, director, or artist). It only touches records with no picture, so one you chose yourself is never overwritten and pressing it again simply continues where it left off. Images arrive one at a time and the list refreshes as they land.
- Self-hosters can power their instance's AI with their own Claude subscription. A new claude-code-bridge (bridges/claude-code/) exposes an Ollama-compatible and OpenAI-compatible endpoint backed by the headless claude CLI on your own machine: register it as a local-server AI provider and chat, Ask Cobb tool-use, photo identify, and strict-JSON features run on your subscription with no API key and no metered billing. It queues bursts, caches identical calls, and reports every degraded state by name (needs login, resting until a stated time, model not on plan) instead of failing silently. Personal, single-tenant use only.
- A new "Re-home a collection onto Records" API token scope lets an operator run that one data move without holding a full-admin token. Like the other capability scopes, it is deny-by-default: a token minted with it can call the re-home operation and nothing else, not tenant data, not workspace deletion, not minting another token.
- A new Records module gives catalog-like collections a lean home of their own. A record carries only a name, photo, location, notes, and the fields you declare, so a Bookshelf or a Movies list no longer has to ride on the assets module and inherit State, Warranty, and Serial number. Each collection is its own named instance with its own page, fields, and views, and the detail opens in the wide photo-left layout. An operator can re-home an existing assets-based collection in one call: ids are preserved so labels and links keep working, attachments and tags follow, and any real value in a borrowed field folds into the record itself rather than being lost.

### Improvements
- The print-failure AI now shows its work on the picture it judges. The camera wall and each fleet tile carry a graded verdict badge (green, amber, red rolling score, and a red "paused by AI" flag when it tripped), the printer cockpit overlays the same badge on the live camera frame with a banner when a print was auto-paused, and a new strip under the frame says when the AI last looked, through which detector, and how many samples this print. A one-tap "Check now" takes a single live reading and tells you whether it would trip the auto-pause. Printers watched by an external detector say so instead (Cobblr stands down its camera for those, as before).
- Custom fields open in place instead of in a pop-up. Clicking a field used to throw an overlay over the whole list, so tidying up five fields meant five dismissals and losing your place each time. The field now expands under its own row: the rest of your fields stay on screen, clicking another one swaps straight to it, and the row you were on is still where you left it when you collapse. Everything the pop-up did (choices, where it appears, delete) is unchanged.
- Picking a photo works the same way everywhere, and you can see the image full size before you commit. The scan inbox had the good version: it searched using what the thing actually is (a book's title plus its author and the word "book", a car plus its paint colour), while a machine or a record page searched a plainer phrase and found worse results. Now there is one derivation, done on the server from the thing's own name and fields, so the same book searches the same way whether you are in the inbox or on its page. The picker itself is now one component everywhere: an editable search box (the derived phrase is a starting point, not a cage), bigger thumbnails, and tapping one opens it full size with a "Use this image" button, so you can tell a clean cover from a blurry re-upload before choosing it.
- Searching the web for a photo now starts with the search term already filled in, and tapping any result opens it full screen with the other options underneath, so you can flip through candidates at full size before picking one. This is the same picker everywhere: the scan inbox, machines, and records on a shelf.
- Image results now have a ✓ button to use one straight away, so viewing a result at full size and choosing it are two separate gestures instead of one getting in the way of the other.
- QR scan codes and label printing are one module now. The former core-labels-qr module folded into Labels (0.6.0): same tokens, same printed codes, same scan behavior, one place to find it all. Workspaces that used QR keep everything (converted automatically, wires included); workspaces that never minted a code keep their blank slate, and enabling Labels brings the whole feature back.
- Machine details open wide, photo-left, matching the new record layout. A machine's detail modal now uses the same two-column shape as assets: the photo large on the left (with the web photo search right under it), fields and panels on the right, so a printer or tool record uses the width of the modal instead of scrolling tall. Stacks back to one column on phones.
- The nav leads with the workhorses. Lists, Projects and Purchases now start folded into the "more" menu (and off the dashboard's Jump-to tiles) instead of claiming prime space in every workspace: Inventory, Machines, Locations and your named tables come first. One tap in the navbar's Customize menu puts any of them back permanently. And the scan inbox stops hiding field chips behind a "+2" while the row still has room: it shows up to six, and never collapses a single chip into a summary.
- Adding a new thing to your workspace is one screen. It used to be three: the first asked which "kind of thing" you wanted and explained the difference between two options while offering only one, because the second had been removed and its question left behind. The second asked what your thing was "like" and listed "Like Inventory", "Like Assets", "Like Machines", which meant picking by the name of the machinery underneath. Now you type what you want to track and choose from lines about your own stuff ("things you own one of", "stuff you have amounts of"), with a suggestion pre-picked when the name gives one away, type "Cars" and it points at the right one. Where it sits in the navbar is a folded one-liner with a sensible default instead of a step of its own.
- Permissions is a grid you can actually read. It used to be one row per member and one column per capability, which ran off the side of the screen with six-line column headers, and it listed every action every module registers, including the wire-only ones an automation fires (an OBD dongle updating your mileage was offered as a human permission). Now capabilities are rows grouped by the module that owns them, collapsed until a group has something configured, and the columns are the holders: owners and admins as one implicit column, then each custom role, then each remaining member. A person's cell tells you where their access comes from, granted here, inherited from a named role, or implicit, so one screen finally answers "what can this person do". Editing a role column changes it for everyone who holds that role. If everyone in the workspace is an owner or admin, the whole grid collapses to a single sentence, because there is nothing to configure.
- The camera scanner feels confirmed everywhere. Every read now beeps as well as vibrates (iPhones ignore the vibration API, so scans there were silent), and a Sort-mode "Done" gets its own two-note chirp. Scanning a bin or location label no longer flickers the preview: the stream stays live while the label resolves. Locking the phone or switching apps mid-session is recoverable, the scanner re-acquires the camera when you return. A photo whose upload fails is kept with a one-tap Retry (stamped with the area you stood in when you shot it) instead of being lost, a failed serial names the code so you can retype it, and captures upload at 1600px instead of full sensor size, so saves are faster on cell connections. The desktop "Pair phone" code now lives 5 minutes instead of 90 seconds: finding and unlocking your phone routinely outran the old window.

### Fixes
- Automations on a named collection now actually run. A bundle that promised something like "supply runs low, add it to the shopping list" installed cleanly and then silently did nothing. Actions also show up for named collections again, so the Do... list when building a wire is no longer empty and generated apps get their buttons back.
- Label code prefixes stay editable until you actually print. A code group's prefix (the letter in front of `c1`, `m4`) is suggested automatically from the group's name, and was meant to stay editable until the first label is printed. It was actually locking itself the first time anything asked for a code, which could be as little as opening the label queue or viewing a QR, so most workspaces found their prefixes already frozen with a letter they never chose, under a tooltip claiming labels had been printed when none had. Prefixes now lock on a real print, the panel says "not printed yet, rename freely" while one can still change, and renaming a group carries its existing codes across so nothing is stranded under the old prefix.
- Phone layouts can no longer silently grow wider than the screen. A dashboard view card with a long view name or item title stretched the whole page past the phone viewport (the card is a grid child, whose minimum width defaulted to its content), and in home-screen (standalone) mode the workspace name rendered under the iOS status-bar clock. Both fixed, and a new automated phone-width sweep (e2e/mobile-overflow.mjs) walks the main routes on a seeded busy workspace and fails on any element that widens the page, so the class stays dead.
- Dashboard cards put the important things first. Three fixes to pinned-view cards and the add panel: a grouped view whose grouping field is unset everywhere (a "by tube type" fleet with no tube types yet) now shows the items themselves with the "set it to group them here" hint as a footer, instead of a hint-only card with nothing in it. The card header stops truncating the names while minor chips stay readable: the redundant view-type label is gone (the card already renders as its type) and the count no longer wraps, so "3D Printers · Printer fleet by state" reads in full. And on an established workspace the "What do you want to do?" panel is a half-width card instead of swallowing a full dashboard row (a blank workspace keeps the full-width hero).
- Live Sort routes to the bin that actually holds the family. The category facet used to send an item to the first bin containing ANY of its category, which favored big mixed bins holding one stray over a bin devoted to the category, and the "N similar items already here" line always said 1. It now counts how many of the category live in each bin and picks the strongest home, and receipt line items got a database index so confirming a long receipt stops rescanning the whole inbox per line.
- A record's image is now editable, and shows whole instead of cropped. Records shipped with a read-only thumbnail, so a book that arrived without a cover had no way to get one. The detail now has the same image controls as machines and assets: upload your own, hit Auto to fetch one by what the record is (its name plus its own author/artist/director/publisher field), or search the web and pick a specific result. The image also keeps its own proportions instead of being cropped to a square, so a portrait book jacket or film poster is no longer cut off top and bottom while the column has room to spare.

## 2026-07-17

### Features
- Edit a table straight from the list: click any cell to change it, without opening the record.
- Each serial you file under an item can now record who has it, a person, a department, or whoever borrowed it, set from the serial itself or the Assign cell in the Units panel. The item then shows, serial by serial, who holds each one, and the holder belongs to that individual rather than the whole item.
- You can now scan serials straight into an item: open its Units panel, tap Scan, and each barcode you read becomes a unit filed under that item while the camera stays live for the next one. The item's count stays put, so you can catalog a whole lot of serials in one pass without it ever changing what you counted.
- Re-running the lookup on a scan can now be undone. A re-run sometimes comes back worse than what it replaced (a dark photo of a tool tote re-read as a Bluetooth speaker), and until now the better answer was simply gone. The card keeps what it had before the run and offers Put it back.
- When you track serial numbers for something you also count, Cobblr now keeps both numbers instead of pretending they are one. While you are scanning serials in, the item just notes how many are not scanned yet. If the two numbers are still apart once you have stopped, it asks which is right rather than guessing, and taking the serials as the truth lands as a normal stock change with a line in the item's history.
- You can now file serial numbers under an item from its own page: open it, use the Units panel, and each serial becomes its own record you can label and place while the item keeps the count you gave it. Filing serials into a fresh item no longer nags you mid-scan. The question only appears once you have stopped adding them and the two numbers still differ.

### Improvements
- AI-authored collections come out lean, not cluttered with a borrowed module's fields. When you describe a new kind of thing and the builder tracks it on top of an existing module (a Bookshelf built on Assets), that thing used to inherit the module's built-in fields, so an authored book showed State: Working, Warranty until, and Serial number that make no sense for a book. The builder now marks each new collection base or inherit: a catalog you look things up in (books, movies, records, wine) keeps only name, photo, location, and notes plus its own fields and drops the module's built-ins, while a thing you own and maintain (tools, vehicles) keeps them. So an authored Bookshelf arrives clean, with no trip to the Fields editor.
- Trimmed the bundle catalog to a focused core. A group of overlapping general-purpose bundles (household supplies, home maintenance, maker workshop, collections, gifts & occasions, tool library, vintage tools, food & pantry, kitchen & fitness) are no longer suggested when you scan or listed in the marketplace. They matched almost anything, weakly, so a single scan was offered a dozen near-identical homes. Anyone who already installed one keeps it and its data exactly as-is; only the offer to new workspaces is withdrawn.
- The bundles Cobblr suggests while you scan are now a focused core set. Ten more specialized bundles (CNC machines, CNC tooling, laser cutters, medications, pet care, garden, subscriptions, wardrobe, documents & renewals, warranties & receipts) moved to a browse-only tier: they're still one click away in the marketplace and install exactly as before, they're just no longer pushed at you on every scan or in the "pairs well with" strip. Nothing installed changes.
- The first-run panel now leads with capture: one question, the add/scan box, and a short strip of suggested ready-made trackers. The full building-blocks and tracker lanes moved behind "More ways to start", so an empty workspace asks one thing instead of presenting modules, recipes, and capture all at once.
- "Just the essentials" trims a thing to its own fields in one click. When a list borrows another module's shape (a Bookshelf built on Assets), its records inherit that module's built-in fields, so a book shows State, Warranty until, Serial number, and Type that make no sense for a book. The per-thing Fields editor now has a Just the essentials button that hides every built-in field that isn't universal in one click (it keeps name, photo, location, and notes, and leaves your own added fields like author and genre alone), plus a Show all built-in fields to put them back. It is the no-code form of "a kind owns its fields."
- The Modules picker now leads with the everyday set (Inventory, Assets, Lists, Purchases, Labels). Machine-floor modules live together under a collapsed "workshop pack" row and every other domain under "more modules", each showing how many of its modules are on, so choosing what a home workspace is for no longer means reading past the print-farm stack.
- A scan session now has a real "File all" button, so a finished session is one tap from filed. Once the AI finishes a scan session, its header used to show a passive "✓ All set" check. Clicking it only collapsed the row, so it was easy to think a whole session had been filed when in fact nothing was committed, the items just sat in the inbox. That check is now a "File all N" button. Tapping it commits every ready item in the session to the exact destination the AI matched it to (each book to your Bookshelf, a VIN to Vehicles, and so on), not one lumped table. Items that still need a manual look are left behind and the header says "needs review"; a fully filed session reads "✓ filed". A bulk confirm that carried the item to a named table (like a Bookshelf) was also being sent with the wrong routing and would fail, that is fixed in the same change.
- An item that has serial numbers on file now shows how many, alongside the quantity you counted. The two are kept separate on purpose: the count stays yours to adjust, and the serials are simply what has been recorded so far.
- A record with a cover opens wide, image-left, instead of scrolling tall. The asset/collection detail (a book on your Bookshelf, a machine, anything with a photo) now uses a two-column layout: the cover large on the left, its fields in a grid on the right, so an image-bearing record uses the width of the modal instead of a long vertical scroll. The header subtitle also honors your field-hiding now: a Bookshelf that hid Manufacturer and State no longer leaks "Scholastic, Working" under the title. On a phone it stacks back to a single column.

### Fixes
- Scanned covers stay local, so they always come through when you file an item. A book (or any scanned product) fetches a catalog cover into your workspace's own file store when it lands in the scan inbox. A single transient network hiccup during that download used to leave the item pointing at a bare source URL with no local copy, and when you filed the item there was nothing to attach, so it showed up with no picture. The download now retries, and a heal pass pulls any item that is still stuck on a URL into local storage, so a committed record never depends on re-fetching the original source.

## 2026-07-16

### Features
- Bookshelf now keeps your books on their own shelf instead of mixing them into Inventory, so they read as a catalog with no quantities or warranty fields, and an existing bookshelf moves itself across on upgrade with your books and their authors intact. There is also a new Lending Library bundle for the shelf you lend from: it counts copies and lets you reserve one against a borrower until it comes back.
- A Heatmap view can now be told the cadence you meant to keep (every day, weekdays, every week, or your own rule). The days you were supposed to show up get outlined, so a gap on an expected day reads as a miss while a rest day doesn't count against you, and the legend keeps score of the days you kept plus your current streak. A weekend never breaks a weekday streak.
- A collection you make now shows a lean catalog by default (title, photo, your fields) and only reveals stock controls (quantity, reorder, cost, warranty) once it actually holds stock, and it stays stock after that even if you run it down to zero. Recording a price alone no longer tips a catalog into a stock list, and you can still pin either face under Settings, Stock tracking.
- Scan something you already track and Cobblr now asks "is this the same one?" and offers to merge what the scan learned into the existing record, instead of only offering to add a duplicate. Snap a license-plate photo of a car you first added by its VIN and it fills in the plate and color on that car. Only fields the record is missing get filled, so nothing you already had is overwritten. (This also fixes the match that powers it: a scan named "Honda Civic Hatchback" now finds your "2019 Honda Civic", where before the two names had to overlap almost word-for-word.)
- Saved views can now be told which fields to use where they previously could not: a Trend view takes its date and value fields plus an optional goal line, a Gallery can show a caption under each card, and Calendar and Gantt can pick the field their entries read as. Bookshelf and Collections now open on a wall of covers, and Bookshelf keeps your books on their own shelf instead of mixing them into inventory, so it stays a clean catalog with no quantities or warranty fields.

### Improvements
- Combining two captures of the same one-of-a-kind thing (a VIN scan plus a photo of the same vehicle) now reads as a detail-merge: the banner says "combine their details into one," the richer listing is kept with its gaps filled from the other (colour, plate), and the quantity stays 1 instead of doubling. Stacks of the same product still sum as before.
- On desktop, opening the confirm form's Location dropdown now expands into a full-width panel beneath the form row instead of cramming the chip grid into a half-width column. Mobile is unchanged.
- Opening an area in the location picker now reveals its containers directly below the row of chips you tapped, instead of after the whole grid, so the panel appears right where your thumb is.
- The location picker is now an accordion of areas (renamed from "Rooms": an Outside or a Garage zone is an area). Each area appears once: tap its name to pick the area itself, or expand it to reveal its bins inline, with a shelf's bins indented under the shelf so the hierarchy is visible. Collapsed by default so the picker stays compact; a pre-filled selection auto-expands into view; bins that live in no area sit in a final "Loose bins" group.
- The location picker's areas now wrap into a compact chip grid (like the camera's drawer) instead of one area per line, so a house full of rooms is a few rows, not a long scroll. An area holding bins is a split chip: tap the name to pick the area, tap its count/chevron to open that area's containers in a panel below (one open at a time, shelf's bins indented under the shelf). Loose containers sit in their own section below, open by default.
- Two phone-scanner fixes: the result card now opens showing the photo you just captured instead of a blank box, with the catalog image swapping in the moment it arrives. And scanning books works reliably: a cover carrying two barcodes (the main code plus the small price supplement) confused the reader into never settling; it now always locks onto the main retail code.
- The scan confirm form's Location picker is collapsed behind a dropdown that opens the rooms-and-bins chip drawer, instead of dumping every location into the form. The drawer's bins are also sectioned by room now ("In Garage: Metal Rack, Bin 4...", with a shelf listed right before the bins on it), so shelves, racks, and closets no longer intermingle with loose bins in one wall; bins that live in no room sit in their own section at the end.
- The unlabeled green checkmark on a scan card (which committed the item without opening it) is gone. In its place, the suggested-table chip is now a split button: tap the table name to review and edit first, or tap its green "Add" half to file the item there as shown, one tap. The action now says what it does and names where it goes.

### Fixes
- Fixed the location picker's area grid showing bins, shelves, and closets as if they were areas (which also pushed the opened area's panel out of view, so the chevron seemed dead). The grid now lists only areas, and opening an area scrolls its container panel into view.
- Scanning a book or film into a lean catalog no longer counts it as stock: the confirm only writes a quantity when you typed one or the label showed a real multiple, so your catalog keeps its clean face. Editing a quantity or reorder point onto a catalog record now switches the list to stock tracking right away instead of on the next visit.
- Undoing an accidental discard no longer jumps the item (and its whole session) to the top of the inbox: a restore now puts things back exactly where they were, with a brief highlight so a later restore from Recently deleted is still easy to spot. Also, book covers scan reliably on older Safari too (the fallback decoder now handles two-barcode covers like the main reader).

## 2026-07-15

### Improvements
- On phones, the floating "labels queued" pill no longer sits on top of the page and the thumb zone. It's desktop-only now; on mobile the label queue shows up as an entry inside the menu instead, still one tap to the queue.
- When a photo proves a barcode's catalog answer wrong (a yarn skein that resolves to an action figure, then a reverse-phone-lookup site for the same spam code), the scan now casts a downvote to the shared barcode database instead of only clearing its own cache. Once enough separate workspaces independently disagree, that code stops serving junk and future scans of it go straight to naming it from your photo. It is a vote, not a block: a barcode that is legitimately shared is never suppressed by one workspace, and it is reversible.
- On a scan inbox item, the Location picker is now the tappable rooms-and-bins chip drawer (the same one the bulk bar and camera use), not the old dropdown, and picking a location saves right away instead of only when you Confirm, so your choice sticks even as the inbox refreshes.

## 2026-07-14

### Features
- A custom field can now be a dropdown, and you can set the options when you make it. Records have always been able to render a text field as a dropdown with a + add new option, so anyone can pick from your list or add their own on the spot and have it stick. The catch was that there was no way to *set* that list unless you hand wrote a bundle manifest. Now there is: on the `/fields` page, a text field gets a Choices box where you type the options (Enter or comma to add, paste a comma separated list to add several at once). Leave it empty and you get a plain text box, exactly as before. You can also add, remove and reorder the options later by clicking the field, which is safe: values already saved on your records are kept either way, because the options are suggestions rather than a rulebook. The form also stops asking you to invent a snake_case key. Type the label you want, like Acquired from, and the key `acquired_from` fills itself in.
- The class picker now offers the named shapes, not just the raw traits. When you scope a field to a class of things, there is a second column beside the trait grid: or a known shape. It lists the profiles the system already uses to describe its own kinds, like `owned-thing` (a specific thing you own, tracked one by one), `stock-material` (bulk stock you count), `place` (somewhere things live), `digital-record` and `work-item`. Pick one and it ticks the grid for you, so you can take the shape closest to what you mean and adjust an axis or two, rather than assembling six axes from scratch. The preview underneath still shows exactly which kinds you will land on. Two of those profiles carry identical traits, so they show as one entry rather than two that would both light up. The renderer dropdown also stopped hogging a full row.
- Photograph a pile of different things and the inbox now asks what you want done with them. Snap two humidifiers, or a shelf of mixed gear, and the card says "2 different items in this photo", names them, and offers a choice right there on the closed card: keep them together as one record, or split into individuals. Splitting gives each one its own entry, cropped out of your photo where possible, and each goes and finds its own product image by name, so you get real records rather than fragments of a group shot. This costs no extra AI: the pass that reads every scanned photo was already counting the units it could see, and now it names them too. A pile of the same thing still counts as a quantity, so a sealed ten pack of screws will not ask you to split anything. There is still a manual Split into items button in the expanded card for the times you disagree with it.
- A category that outgrows its table can be promoted into a table of its own, and folded back if you change your mind. You should not have to decide your table structure before you have scanned anything. So: scan things, let categories emerge from what you actually own, and when one of them earns its own table (enough items, or it wants fields the others do not), promote it. The items keep their ids, their history and their QR labels, so a bin's printed label still resolves to the same item afterwards. The fields the parent table defined come across with them. And the reverse works too, because a decision you cannot undo is not really a decision: demote the table and its items fold back into the parent as a category, with that category stamped onto them and added to the parent's list. Promote, demote and promote again leaves you exactly where you started.
- Things of the same kind stop scattering across your tables. Scan five electrical parts and they used to land in four different places: one in Home Inventory, one in Household Supplies, one in Inventory, one in Maker Workshop. That was not really the AI being dim. It was being asked which table an item belonged in, when the only tables on offer all meant roughly "stuff", and it was asked separately for each item, so identical things went to different homes. Tables now carry a Category field, and a difference in *kind* is recorded there rather than by picking a different table. A vehicle is still not a part, so those stay separate tables. But an electrical part and a plumbing part are the same kind of record, so they live in one table and are told apart by their category. Anything the scanner cannot confidently place lands in a table you nominate as your catch-all, tagged with its category. The categories are yours: the list starts empty, grows only from values you actually confirm, and near-misses snap onto the value you already use rather than quietly creating a second spelling of it. This works with the AI switched off, too, since the identification step already worked out a category and it was previously being thrown away.
- Re-running the AI on a photo now finishes when it says it finishes. Reading a photo used to take three model calls in a row, and the card went quiet after the first one. The name would update, the spinner would stop, everything looked done, and then several seconds later a "2 different items, split?" offer appeared out of nowhere. It read like a bug. Now the pass that identifies the photo answers all three questions in a single read, so the name, what it saw, and the split offer all land together, and the spinner keeps going until the whole chain is actually finished. That is also one fewer vision call per photo. A re-run has stopped erasing things, too: it used to wipe the split offer and then pay to rediscover it, and if you had answered "keep as one", it threw your answer away and asked again. There is also a new Replay (no AI) button that re-runs everything except the model calls, which is free, instant, and useful when the identification was fine but Cobblr's handling of it was not.
- Scanning a multipack now records the pack you're actually holding. Household Supplies' "Usual pack" field is now "Pack size": it captures the package you scanned (a single, a 10-pack), read off the box, instead of guessing what you usually buy. Pack size is a platform dimension now (alongside quantity and unit), so any tracker can carry it and the scan fills it the same way.
- A custom field can now target any class of things you can describe, not just two canned ones. Yesterday's release let a field apply to "all physical items" or "all digital items". Those were the only two choices, which quietly implied the rest of the vocabulary did not exist. It does: every entity kind in Cobblr declares what it fundamentally *is* along six axes (is it physical or digital, tracked one by one or by quantity, does it hold other things, does it have a schedule, can it be finished, does it stick around). On the `/fields` page, picking A class of things now gives you the full grid. Tick the traits you mean and the page shows you, live, exactly which kinds the field will land on. Ticks in the same row are OR'd, and across rows they are AND'd, so "physical + tracked one by one" means assets, machines, and vehicles but not parts (a part is physical, but you track it by quantity). Four one-click presets sit above the grid for the common cases. It is the same control the Actions page uses to decide which entities an action appears on, so the two now behave identically.

### Improvements
- Scanning several things at once no longer makes the same AI request several times over. When you split one photo into separate items, or a burst of scans arrives together, Cobblr could send the AI the exact same question several times in parallel: each request checked the cache and found nothing, because none of the others had finished writing their answer yet. Now an identical request that is already in progress waits for that one and reuses its result. It applies only to identical requests happening at the same moment; a deliberate retry still makes its own fresh call.
- A barcode scan with a photo now reads that photo once, not twice. When a scanned barcode resolves to a name, Cobblr sanity-checks it against your photo: "does the picture actually show this product?" That check was a separate look at the image by the vision model, even though another step had usually already looked at the same photo and written down what it saw. When that description already exists, the sanity-check now just compares the resolved name against the description in text, which is cheaper, faster, and spends no vision budget. If nothing has described the photo yet, it falls back to looking at the image as before. No change to what you see; it just stops paying to read the same photo a second time.
- On the Fields page, the fields you added yourself now sit at the top under "your fields", and the ones that came from bundles or modules sit below a divider. Before, a field you just added could be buried under a wall of bundled fields, so you had to scroll to find it.
- The scanner and the inbox now pick a location by tapping a chip, not opening a dropdown. When you set the scan area on the camera, or file a selection from the inbox, your locations show as a grid of tappable chips under a Rooms (and Bins) heading, one tap to pick. It is the same mobile pattern across both surfaces, so they stop feeling like two different apps. New location is one tap away at the bottom.
- Filing a scanned barcode item now teaches the shared barcode database on its own: a plain Confirm counts as a strong signal that the listing is right, and when two different people independently confirm the same value it becomes a verified entry automatically. No need to reach for the operator-only "lock it in" button.
- The scan inbox's Photos button now takes several photos at once and files them as one batch: pick a whole shelf from your photo library in one go and they land grouped as a single session instead of scattering into separate intakes.

### Fixes
- Improving an AI prompt now actually reaches the photos you already scanned. Cobblr caches every AI answer so the same photo is never paid for twice. But the cache remembered an answer by the picture it was given, not by the question it was asked, and for photo identification the question lives in Cobblr rather than in the request. So when we improved the wording of that question, nothing changed for any photo already in the cache: it kept handing back the answer the old wording produced, and there was no expiry, so it would have done that forever. The prompt is now part of what the cache remembers, so improving it invalidates exactly the answers it should. This also fixes the internal tool we use to measure whether a prompt change was an improvement, which had been quietly scoring the old prompt's answers and reporting them as the new one's. One-off effect: cached AI answers from before this change are discarded, so the next scan of an old photo pays for a fresh read.
- A barcode that nothing on the web can identify no longer invents an answer, and no longer teaches that answer to everyone else. A bare twelve digit barcode looks like a phone number to a search engine, so a product with no listing anywhere reliably turns up phone directory pages instead. Cobblr was taking the first of those and calling it the product: a pack of Harbor Freight silicone ties came back as "411 - White Pages, Find Phone Numbers". Worse, it then wrote that guess into the shared barcode database that every workspace on the instance reads, so one bad scan was about to become everybody's answer for that barcode. Three things change. A name from a web search is now only trusted when something actually backs it up, meaning the model recognised the product or several independent results agreed on it. An unsupported name is never written to the shared database, and the guesses already in there have been cleared out, so they will be looked up properly next time. And when a barcode has no trustworthy match but you photographed the item, Cobblr now reads the photo to identify it rather than treating the photo as a mere sanity check, because the label in front of you beats a search for a naked number. Real catalogue lookups are unaffected.
- Fixed: Add field was dead when you scoped a field to a class of things. A field aimed at a class has no single entity kind by design (the system works it out from the traits you tick), but the button was still waiting for one, so it stayed greyed out no matter what you filled in, and it never said why. The button now reads the same readiness rule the form itself uses, so the two cannot disagree, and when it is greyed out it tells you what it is waiting for. Choices and Renderer now also share a row rather than stacking as two full-width bands.
- A burst of edits to the same item no longer risks quietly dropping each other's changes. Several parts of Cobblr write to a shared bag of details on an item, and a number of those writes were rebuilding the whole bag from a copy they had read a moment earlier, which meant a change made in between could be silently overwritten. Every one of those writes now updates only the specific keys it owns, directly in the database, so concurrent edits to different parts of the same item all survive. This closes out the last of a class of bug that a build check already prevents new instances of.
- Fixed: the New asset and New machine forms, and the asset and machine list columns, ignored a bundle's rename of the manufacturer field. Install Vehicles, which renames it to Make, and those surfaces still said Manufacturer while the detail page said Make. They now read the name from the same place every other form does, so a rename reaches every surface at once.
- The scan-item correction buttons that write to the shared cross-workspace barcode database (This is wrong / Right: needs detail / This is good, lock it in) are now limited to the platform operator, so a member can no longer change what every other workspace sees for a barcode. Everyone still has the research hint and Re-run AI, which only re-identify their own item.
- Two fixes to the scan inbox's bulk-select toolbar. It is no longer see-through (you could read the cards through it) and it is tighter, so it takes one action row on a phone instead of wrapping to three: Discard and clear are icons now, and the labels are shorter. And a card now shows where it is being filed. Setting a location on selected items writes their filing bin, but the card only ever showed the location a bundle's AI happened to guess (a Home Inventory item's `room`), so "Set location" looked like it did nothing. The bin you chose now shows on the card with a pin, distinct from any guessed field.
- Scanned items of the same kind land together, even in a workspace that uses several tables. Two fixes to how the scanner files things. First, the category it assigns is now the broad, everyday kind you'd actually file something under, rather than the hyper-specific label a product database returns. Those databases split one kind into dozens of narrow sub-types, which is too fine to group by. Second, when a workspace has several named tables and its generic default table is hidden, the scanner previously had no catch-all, so anything it couldn't place confidently scattered across the named tables. There is now always a catch-all (the default table, told apart by category), and it reappears the moment something lands in it.
- The inbox stopped offering to merge two products that just happen to be the same brand and colour. It was suggesting you combine a Leviton wall plate with a Leviton rocker switch, because the two names share the words "decora" and "white". Half of Leviton's catalog shares those words. Meanwhile both items had been resolved from their own barcodes, which said plainly that they were different products, and the suggestion ignored that. Barcodes now decide first: two items with the same scanned barcode are the same thing, two with different barcodes are not, and the one honest exception (a ten-pack of the very same unit carries its own barcode) is recognised from the pack size rather than from word overlap. Items identified from a photo with no barcode still fall back to comparing names, as before.
- Combining two scans of the same thing now keeps what each of them saw. The scanner can capture one object two ways and produce two entries: reading a code gives you the identity but not the colour, while a photo of the same object gives the colour but not the code. Combining them used to keep one entry and throw the other's details away. Now the combine keeps whichever entry has the stronger identity and fills in its blanks from the other, so you end up with everything that was learned in one record rather than losing half of it. Your own entry's values are never overwritten; the other entry only fills gaps, and only from an entry that was headed to the same table.
- A scan no longer loses what you told it. The scan pipeline writes to one shared bag of data on each item, and several passes write to it at different times, some of them minutes apart. A few of those passes were replacing the whole bag instead of adding to it, which quietly deleted whatever anyone else had put there. In practice: a re-run threw away your answer to "keep these together, or split them?" and asked again; a re-run erased the "2 different items" offer and then paid for a second AI call to rediscover it; and when a barcode provider was briefly rate-limited, the retry marker replaced the entire bag, so a receipt line silently fell out of its receipt and an imported row lost the key that stops it importing twice. Every one of those writes is now a merge, so a pass can only touch the keys it actually owns. A new build check (`lint:jsonb-merge`) fails any future write that replaces one of these shared bags, so this class of bug cannot come back.
- Three fixes to the phone scanner. The Filing into… confirmation now shows at the top of the frame instead of at the bottom, where it sat directly on top of the capture button and blocked it. Holding the camera on the same QR code no longer spams a new toast every couple of seconds: a code held steady in the frame is now treated as one scan, and only scans again once you move it out of view and back. And the live preview no longer stutters when a location QR is read. Reading a bin or location label sets a filing target and makes no inbox item, so it no longer grabs a full-resolution photo it would only throw away.
- A custom field scoped to a whole class of items (e.g. "Acquired from" on everything physical) now shows up when you scan an item into a bundle you haven't installed yet, not just on bundles already in your workspace.
- Splitting a group photo into separate items now actually uses the AI you were paying for. When you split a photo of several things, Cobblr asks the AI to find each item and draw a box around it, so each new entry can be cropped to just its own item. That request was being quietly discarded before it reached the model: two of the three AI providers ignored the question and asked their standard "what is this item?" question instead, which of course came back with no list of items and no boxes. So the step fell back to the names it already had, and every split paid for a picture-reading call that could never have answered. It had never once worked. The question now reaches the model, so a split can crop each item out of the group shot as intended, and repeated splits of the same photo reuse one answer instead of paying again each time.
- Fixed: a barcode scanner that mangles a VIN label no longer sends you down a rabbit hole. A real scan of a door-jamb label came back with a stray leading character, which was enough for the VIN decoder to decline it, so the code fell through to the product-barcode lookup instead and came back as a completely unrelated product, name, photo and all. Two fixes, neither of which costs an AI call. A VIN is now repaired from a messy scan when the maths can prove the repair: the letters I, O and Q never appear in a real VIN, and every VIN carries a check digit, so a wrong reading is caught roughly ten times out of eleven. When it cannot be proven, the scan is left alone rather than guessed at. Separately, product barcodes are only ever looked up when the code could actually be one: a real product barcode is a fixed run of digits, so an eighteen character mix of letters and numbers is no longer handed to a product database that will cheerfully return its best guess. The repaired VIN also replaces the mangled one on the record, so you are not left holding a code that exists nowhere.
- Fixed two things a scanned vehicle got wrong. The VIN field stayed empty after a VIN scan, and was still labelled "Serial number", even though the Vehicles bundle relabels it to VIN and declares that it holds the VIN. Two causes: the confirm form's built-in fields were hardcoded, so a bundle's relabels never reached them (every other form already reads them), and the fill planner recognised the "this field holds the code" role and then ignored it. The VIN now lands in the VIN box, and the box is called VIN. Separately, the colour was invented. A silver van came back as a blue-grey hex, because vision was asked what colour the car was and duly made up six hex digits. A colour resolved from the paint code stamped on the vehicle's own label now wins over the model's guess, and the photo pass is asked to read paint and colour codes off labels alongside the serial numbers it already reads.

## 2026-07-13

### Features
- Match a new set or part to the catalog as you create it. The new-item form now shows a "Match to a catalog" search on catalog-backed instances (Lego Sets, where it used to be hidden), and that search now reaches hosted catalogs. Type a set name or number, pick the Rebrickable entry, and the new set is linked to it, so its theme, year, pieces, and photo come along without typing them. Instances with nothing to match against stay clean, and scanning still auto-matches the same way.
- The new-item form is wider, and matching a catalog fills in the fields. The new set/part dialog now uses a two-column layout instead of one tall scrolling column, so it fits the screen. And when you match to a catalog, the entry's data flows into the fields: pick a Rebrickable set and its set number, theme, release year, and piece count fill themselves in, blanks only, so anything you already typed is kept. Which fields a catalog fills is declared on the catalog, so it works for any catalog that maps its columns to an instance's fields, not just Lego.
- Consumables can now be tracked one unit at a time. On a yarn's page, under Consumption, turn on "Track each skein separately" to get a plain count by state ("3 skeins, 2 new, 1 open") with the metres left in the skein you are actually pulling from shown quietly, never a blended total across skeins. It is opt-in per item and reversible, so existing consumables keep their single gauge until you ask for the per-unit view.
- Per-unit consumables (opt-in per item) now track parallel projects. Open a skein and assign it to a project, and its withdrawals post to that skein's own statement with the project as the reason; a second project can have its own skein open at the same time, and there is still never a total across them. When a project finishes with metres still on the ball, a clearly reusable amount returns to the pool silently while a small leftover asks a one-tap "keep it, or done with it?" (writing off leaves an explicit "written off" line, never a silent disappearance) - the threshold is a tunable percentage of the skein (15% by default). When a project's skein runs out you can continue it onto a fresh one in a tap. The per-skein/per-project cards appear only when you actually have parallel skeins or a binding, so a single working ball stays the simple count. Under the hood, consuming a reservation (and every stock-adjust) now writes the consumption ledger, so a project pulling from a bound unit finally leaves a statement line and the running balance is always right.
- Printers can now reach a CUPS printer on your LAN from a hosted Cobblr through the on-site edge bridge. Add Printer's manager field is now a "how Cobblr reaches it" chooser: Direct URL (self-host or same network, as before) or Via edge bridge. Install a bridge inline if none is running yet, or pick a connected one, and Cobblr hands the print job to the bridge, which speaks IPP to CUPS locally. The bare URL box that promised an edge bridge with no way to set one up is gone, and a hosted direct-to-LAN address now explains the fix ("run an edge bridge") instead of failing with a raw SSRF error.
- A Sets form leads with sets, not minifigs. When you quick-match on a new item, results now put the instance's own catalog first, so typing "falcon" on a Lego Sets form surfaces the Falcon *sets* before Falcon minifigs. The preferred catalog is worked out from the fields the instance already declares, so it needs no extra setup and applies to any catalog-backed instance, not just Lego.
- A custom field can now apply to *all physical items*, not just one kind. Say you want to track where something came from: FB Marketplace, a gift, bought new. That question is the same for a part, a machine, a vehicle, a whole room. Until now you had to create the same "Origin" field once per kind, and create it again by hand every time you started tracking a new kind of thing. On the `/fields` page, the destination picker now offers All physical items (and All digital items) above the list of individual kinds. Pick it, and the field lands on everything physical you track: parts, assets, machines, vehicles, locations. Anything physical you add later gets it automatically, with nothing to redo. If you want it worded differently on just one kind, add a field with the same name to that kind and the more specific one wins.
- Scan a vehicle and Cobblr now fills in its color too. It reads the paint code off the door-jamb label (the one already in your scan) and resolves it to the real color name: a built-in table covers the common makes instantly and for free, and a code the table doesn't know is looked up with a quick web-search, so the color fills with no AI tokens for the usual case. It only fills an empty color field, and if it can't resolve the code it leaves it blank rather than guess.
- Enter a vehicle's VIN and the form fills in the year, make, model, and body for you, pulled from the free NHTSA lookup. It only fills fields you left blank, tags each one "filled from VIN, double-check", and one tap undoes the lot.
- Scan a VIN and get the whole vehicle. Point the camera at a car's door-jamb VIN barcode and Cobblr decodes it against the free NHTSA database, then mints a record already named "2003 Honda Accord" with make, model, year, body, and fuel filled in, the same one-tap flow a product barcode uses (a UPC still goes to the product catalogs; a VIN goes to the vehicle database, decided by the code's shape). You can still type a VIN into a vehicle form to fill the same fields. It's a suggestion you can double-check: only empty fields are touched, and a bad or partial VIN says so instead of guessing. Works on the shipped Vehicle Maintenance bundle and on any make/model/year table you build.

### Improvements
- Assets instances (Vehicles, Plant Care, Documents…) now open the full page, not a bare list. Instead of a name-only stub, an assets instance renders the same rich Assets page scoped to it: thumbnails, the list/gallery view toggle, saved views, and full detail/edit, so a Vehicle on /vehicles is a complete record. Adding a new one and opening detail stay inside the instance, and "New …" uses the instance's own noun.
- Automatic backups nudge you to turn on a schedule. A backup destination starts manual-only, and a manual backup nobody remembers to press never happens. Now any manual-only destination shows an amber reminder with one-tap Daily / Weekly buttons, scheduled rows show their next run time, and connecting Google Drive confirms the connection and points you straight at the schedule.
- Your dashboard now nudges you to install the bundle your scans are asking for. If you've scanned things that fit a starter bundle you haven't installed (a pile of VIN scans with no Vehicles table), the dashboard shows a "3 look like Vehicles, install" card. One tap installs the bundle and files every one of those scanned items into its new table. It only appears on an established workspace (a brand-new one already gets the same offer in its first-run panel) and only for owners/admins.
- Enable more of a bundle's features right from its page. If you installed a bundle in a hurry (say the one-tap "Install Vehicles & add" from a scan) and skipped its optional features, the instance's page now shows a "More you can turn on from <bundle>" strip listing what's still off (maintenance schedule, connected-car…) with an "Enable features" button that opens the bundle's modal to switch them on. Renders nothing once everything's enabled.
- The "Migrate from another app" section is now app-first: you pick the app you are coming from (Homebox today, more to follow) and then choose how to bring it in (live connect or CSV) as a second step, instead of every method for every app crowding the section as its own card.
- Scanning something you don't have a table for is a first-class flow now, not a buried card. When a scan's best match is a bundle you haven't installed (a VIN → Vehicles), the closed inbox card shows a one-tap "Install Vehicles & add" (no need to open it), and if you do open it, that bundle is the default destination in the picker with its real, editable fields (year, trim, mileage, fuel, color…) pre-filled from the scan, instead of silently defaulting to Inventory with read-only chips. Nothing is created until you confirm; picking another table opts out. (Also: a flaky external catalog thumbnail now falls back to your own photo instead of showing broken on load.)
- Scan something you don't have a table for yet? Install the right bundle and file it, in one tap. When a scan's best match is a bundle you haven't installed (scan a VIN with no Vehicles table), the confirm form now leads with an "Install Vehicles & add this" card instead of quietly dropping the item into generic Inventory. It previews the fields the scan already filled (make, VIN, year, model, fuel…) and, in one tap, installs the bundle and files the item straight into its new dedicated table.

### Fixes
- Automatic backup destinations work again. The "Add a destination" button on the Backup settings page was stuck disabled because the page loaded its destinations and drivers from the wrong URL (it dropped the "/backup" path segment), so the list always came back empty. Corrected the path, so you can add and schedule a server path / NAS or Google Drive backup destination again.
- Bundle marketplace cards show their own icons again instead of a generic box. The self-hosted registry index fills in a placeholder box glyph for bundles whose icon is a web-only field, and that placeholder was overriding each bundle's real icon on the card. The card now prefers the bundle's authoritative icon over the index placeholder.
- The "N queued" label pill no longer collides with the feedback button. In top-bar layout the queue pill now stacks neatly above the feedback pill instead of overlapping it. In full-sidebar layout it moves into the sidebar foot as a row alongside Notifications and Feedback, so there's no orphaned floating pill left in the corner.
- Fixed a cluster of pages that scrolled sideways on a phone. Deeply-nested Locations now indent less on small screens and each card's meta + buttons wrap onto their own line instead of pushing the page wide; the trait fingerprint on Actions (physical · fungible · …) wraps between axes; and a long project title no longer stretches the project page past the screen edge. Nothing changes on desktop.
- On the Presentation settings page, the KIND/INSTANCE tag column is now a fixed width, so the name column starts at the same place on every row and the list lines up left to right instead of jittering between the shorter KIND and longer INSTANCE tags.
- Vehicle scans now name the body style and reliably fill the color. A scanned VIN mints a fuller name ("2019 Honda Civic Hatchback EX", adding body + trim), and the paint-code color now resolves even when the label read just lists the codes without labeling one "color": it matches each code against the paint table, so the real one wins (a Honda "NH830M" among "TGG K LJ5" becomes "Lunar Silver Metallic"). The catalog photo search also folds in the resolved color, so the suggested picture matches the actual car.

## 2026-07-12

### Features
- Bundle updates now apply by their version number instead of nagging you for every change. Small fixes (patch releases) apply themselves quietly in the background. Feature releases (minor versions) apply themselves too and show a toast telling you what was added. Big releases (major versions) still ask first, so you review and confirm exactly as before. Anything that would collide with a field you customised keeps prompting, so nothing you changed is overwritten silently.
- A catalog that ships with a source URL (like the Rebrickable catalogs the Lego bundle installs) now has a "Pull from source" button that imports its rows in one tap, instead of running a script. The very large set bill-of-materials still uses the bulk seeder.
- Hosted catalogs are now browsable. Open a hosted catalog (Rebrickable's sets, parts, minifigs, colours, themes, part-categories) and you can scroll its entries and search them, the same as a catalog you imported yourself. The rows come from Cobblr's shared reference catalog on demand, and colours show their swatch. The one exception is the set bill-of-materials: it powers Disassemble behind the scenes and isn't meant to be paged through, so the page says so instead of looking empty.
- On a hosted deployment, a large reference catalog (Rebrickable) can now be served from one shared, centrally-refreshed copy instead of imported into every workspace. Installing such a catalog links to the shared copy automatically; matching, hydration, and disassembling a set all read from it. Self-hosters are unchanged: catalogs import into the workspace as before.
- Label codes now show everywhere you work with labels, not just on the printout. The on-screen preview and the browser (⌘P) print sheet draw the code in each QR center, every queued row shows its code as a chip, and a new Codes button lets you find an item by typing its code, rename a list's prefix, or change what a kind's codes group by.
- Printed labels now carry a short human-readable code (like m1, p42, b7) in the center of the QR, so you can read an item off a shelf or type it into search without scanning. Each list of things gets its own prefix and its own running count, the codes ignore case and shrug off look-alike typos (o vs 0, l vs 1) when you search, and printing warns you if a code would ever crowd the QR too much to scan.
- Five fixes to the Labels page. The code in the middle of a QR now draws as a clean circle for short codes and a stadium pill for longer ones (a two-character code used to squish into a mangled oval). You can now hide the code from the QR center per kind of thing, from the Codes panel: handy for singular kinds like a single Office where a code adds nothing. In the queue list the full label URL moved onto its own line so it is never cut off. The sheet preview now fits the column it sits in, so the second copy of a two-up label is no longer clipped. And in the Add-labels browser an already-queued row lets you take it back out: it reads "Added" and turns into "Remove" on hover.
- Disassembling a set now opens the sorting planner over exactly the parts it just spawned, so you go straight from "taken apart" to "here's a plan to bin these." More generally, any action can now hand off to a first-party flow when it finishes, and the sorting planner is reachable as one of those flows from outside the scan inbox.
- The bundle marketplace catalog now comes from Cobblr itself instead of a GitHub repository. Your instance builds and serves its own catalog, so it works offline and always matches the bundles in the running version.
- A table view now lets you tick rows and hit "Organize selected" to open the sorting planner over just those items, so you can bin a hand-picked set from anywhere you can see them, not only from the scan inbox.

### Improvements
- Ask Cobb can now read your workspace through a local AI that runs its own tools. Some local AI backends are agents that run tools themselves and only return text, so they never hand tool calls back the way a metered API does, and Cobb couldn't see your data through them. Now you set "How this AI runs tools" on the connection (Ollama, OpenAI-compatible, or Local AI via edge bridge) to the "runs tools itself" option, and Cobb reads your real records through it. Each turn, Cobblr mints a short-lived, read-only grant scoped to just that one workspace, and hands the backend its own workspace address to read from, so one backend can safely serve several workspaces, each with its own grant. It is read-only through this path (Cobb can look, not change); to have Cobb make changes, connect a tool-calling provider (Anthropic, OpenAI, OpenRouter, or a tool-capable local server) instead.
- Ask Cobb keeps your conversation across a page refresh. The chat used to reset to empty whenever you reloaded the page. Now each workspace's conversation is remembered on your device, so a refresh (or reopening the panel) picks up where you left off. Only the plain back-and-forth text is kept, not live buttons like a pending Confirm card, and a new trash icon in the panel header clears the conversation whenever you want. History is per workspace and stays on your device (it is not uploaded).
- Ask Cobb now sees more of your screens. After the Scan Inbox, Cobb also knows what's on your Purchases (open orders + how many are in transit), Machines (count + a breakdown of whatever states your machines use), and Inventory (item count + how many are low on stock) pages. So "what do I have going on?" reflects where you are, and you can ask about what you're staring at.
- Ask Cobb now knows what screen you're on. Ask "what do I have going on?" while looking at the Scan Inbox and Cobb can reference the backlog you're staring at ("19 pending, 16 waiting 2d+"), instead of answering as if the page didn't exist. Each screen tells Cobb a one-line summary of what it's showing; Cobb uses it for relevance but still answers your actual question (a workspace-wide question still gets a workspace-wide answer). The Scan Inbox is the first screen wired up.
- The inventory item page got four yarn-driven fixes. Quantity now has inline +/- steppers sitting right next to its unit ("[-] 1 [+] skein"), so a tap adjusts stock with no pop-up. The "Reserve for" search names your own workspace's tables (a yarn and designs workspace reads "search a design, project…") instead of hardcoded generic nouns. And the Yarn table now treats yarn as the consumable it is: it hides the Maintenance log (that section is for machines) and turns on skein-by-skein consumption tracking by default. Existing yarn tables pick up the section changes when the bundle updates.
- Location labels no longer print the little code in the middle of the QR by default. A location is unique by its name (there is one "Office"), so a disambiguating code drawn in the center is just noise, unlike a part or machine where "p42" tells apart one of many similar items. Locations now default that center code OFF while everything else keeps it ON, with no setup and no re-print of existing labels. Your own per-kind toggle still wins if you want it a different way.
- Empty module pages now explain what the tab is for: the Purchases page shows a short "What's Purchases for?" hint (drawn from the module's own description) when you have no orders yet, so a fresh tab isn't a mystery.
- Added a guided Bluetooth printer self-test you can open from a support link. It connects to your label printer in the browser, prints alignment, print-quality, and QR test labels, and sends the results straight to your support ticket, so we can diagnose a printer we don't physically have.
- Photo scans now read a serial number or service tag off an item's label and file it into a real Serial number field on the item, not just the scan notes. It is read verbatim (never guessed or completed from a partly-hidden label), and Machines gained a native serial number field to receive it (Assets and Inventory already had one). Relabel it per table if you like, "Service tag", "VIN", and so on.

### Fixes
- When we close several of your feedback items in one combined email, replying to that email now threads your reply back into Cobblr as a followup, the same way replying to a single-item resolution already did. Previously a reply to a combined email had no tokenized reply address, so it landed in an unmonitored mailbox instead of reaching your conversation.
- Updating a bundle that ships a reference catalog no longer risks the rows you imported into it. Bundle upgrades now preserve catalog data (the shell is refreshed in place, your imported rows stay), and only an explicit uninstall clears a catalog. This also closes an edge where a new version that removed a catalog could have dropped your imported rows without asking. Catalog-bearing updates still prompt for confirmation during the rollout, so nothing you imported is ever lost silently.
- Capturing an item into a capability-rich bundle now shows what it can do. Type "lego set" into *Add your first thing* and the matchmaker's Create button used to install the bundle with its defaults and file your item silently. Now, when the bundle carries optional capabilities, Create opens the same chooser the recipe path uses so you pick what it does first, then your captured item lands in the tracker it builds. Bundles with nothing to choose still create in one tap.
- Editing an AI connection now shows its saved settings. The edit form used to blank every non-secret field on open, so a dropdown (like "How this AI runs tools") snapped back to its default and looked like it wouldn't save. Now the form pre-fills what's actually stored (base URL, dropdown choices, model), and saving merges your changes instead of replacing everything, so leaving a secret key blank keeps it. Secrets are still never shown.
- Dates on a design (target and completed) now save the instant you pick them, and a small "saving / saved" note confirms your edits are being kept, so there's no wondering whether progress was recorded. Previously the mobile date picker wasn't reliably triggering the save.
- Setting up a bundle with optional capabilities from onboarding now shows what it can do. Picking a recipe like Lego in "What do you want to do?" used to install it silently and drop you on an empty page, with no hint that Bricks bins, the Rebrickable catalog link, Disassemble, labels, and scanning were even part of it. Now, when a recipe has optional capabilities, "Set up" opens its detail card so you see and choose them first, then land in your new tracker. A recipe with nothing to choose still installs in one tap. Also fixed a bundle card that printed its description twice.
- Scanning into a named instance now defaults the confirm form's "Add to" to that instance immediately. Before, while the workspace's table menu was still loading, a routed scan (a yarn barcode → Yarn) briefly showed the generic base table (Inventory part) as the destination, so a fast confirm could file it into the wrong table. The routed instance is now seeded from the match itself and shown on the first render.
- An item whose identity is a colour, like a yarn with a colourway, now shows a colour swatch as its list and detail thumbnail instead of a generic catalog photo. Any item carrying a colour field with a hex value gets the swatch; the catalog photo still lives in the item's Photos section.

## 2026-07-11

### Features
- Describe your home to Cobb, right next to the plan. The little hint box handles quick corrections; describing places is a conversation. Tell Cobb on the put-away plan opens the assistant alongside it (both stay usable side by side). Cobb can see your live plan and backlog through a built-in tool, and as you describe your home ("there's an electrical closet in the hallway, the garage has a chemicals shelf") it proposes real location records, each created only when you confirm. And Cobb finishes the job itself: once your places exist, it re-plans for you (a refresh of the proposal only, nothing files without your accept) and the open plan updates in place with a small "Plan updated" note. Talking to Cobb never closes the plan, and pressing Escape in the chat leaves it alone.
- Rebuilt the Lego bundle. Installing it now gives you a Sets table of its own (each set sealed, built, or disassembled) instead of Lego fields bolted onto the generic parts table, with checkboxes to also track individual Bricks in bins, link the Rebrickable catalog so sets and parts match real data, disassemble a built set into its bricks, and print QR labels.
- The plan now covers what's already set, and your hint survives the session. Items that already have a home (you filed them by hand, or scanned them with an active bin) used to vanish from the put-away plan as "already filed"; now they lead it as ready groups: "All set, just put them away," pre-accepted and included in the put-away walk with nothing to click. And a hint you give the planner carries for your whole session: scan more items or send one back, and the recomputed plan still honors it; the plan line shows *guided by your hint "…"* with a clear link. Also in this pass: the plan dialog matches the inbox column's width, the hint field and its Re-plan button are one joined control with "or tell Cobb instead" beside them, and Tell Cobb now opens the assistant with ghost guidance in the composer instead of pre-typed text.
- Put away a fresh pile even without AI: the plan now proposes starter bins by category. When you plan a backlog on a workspace that has no bins yet (and AI is not connected), the planner used to leave every item marked "Needs a destination" with nowhere to send it. It now reads the category each item already carries and rolls them into a handful of common shelves (Groceries, Beverages, Liquor & Spirits, Books & Media, 3D Printing & Filament, Personal Care, Electronics & Cables, Household & Home, and so on), then proposes a new bin for each. A pile of a hundred scattered items collapses into a short list of bins you can accept, rename, or send elsewhere. Items with no category still cluster by name or wait for you to pick a spot. Detail and edit modals also use a bit more of the screen height.
- Ready groups get an "I did it!" button, and every destination is clickable. Items that already have a home now show an I did it! button in the put-away plan: one tap files them as real records at their set location and clears them from the backlog (previously they were quietly folded into a walk, which also spawned a phantom "resume put-away walk" nag just from loading the page; that's fixed). You can re-route a ready group by clicking its location chip, and a proposed new bin now has two editable parts: click the name to rename it, click the "in <location>" to change which location it goes under (both use the proper location picker). A related fix: confirming an already-located item now keeps its location instead of dropping it.
- Scan a printed Cobblr QR label and Cobblr jumps straight to it. If you have a USB or Bluetooth barcode scanner, it now works like a remote for the app: from any page, scan a bin, location, or item label and that thing opens on screen. Before, scanning a bin label from somewhere other than the Scan page dropped a mystery row into your inbox instead of opening the bin. The first scan asks once whether you want labels to jump automatically (Always / Just this once / No), and remembers your answer per device. Product barcodes still land in the scan inbox as before, from anywhere. Separately, the second-screen "drive this screen with scans" control (for a wall tablet that follows a scanner on another device) is now a small link instead of a large panel, since most people never need it.
- The put-away plan is now a lens on the inbox, not a popup. The sorting plan is just your pending scans regrouped by where they should go, so it lives right in the scan inbox behind a header toggle: By session (grouped by scan time, the default) or Sorting plan (grouped by destination). Flip to Sorting plan and the inbox becomes the plan in place, using the full height of the page instead of a cramped modal. Everything you could do before is still there: category starter bins, evidence-backed existing bins, ready groups, accept, rename, re-route, the hint box, and handing it to Cobb. The dashboard and scan-page "Put them away" buttons flip straight to the Sorting plan lens. Nothing is committed until you accept, and you can switch back to By session at any time.

### Improvements
- Open a printer's controls straight from its machine page. The Print manager panel on a linked machine now has an Open controls button that opens the full cockpit (camera, temperatures, pause/resume/stop, on-printer files, and print history), the same modal a fleet tile opens. Operating a machine now feels identical whether you reach it from Digital Fabrication or from the machine's own page. The button waits until the manager is reporting the printer live (a printer it can't currently see has nothing to control).
- The label print queue is now a live preview: each queued label's QR follows your current label base URL, so changing the base updates the preview (and what prints) without re-queuing. The queue row shows the URL each code will encode, and a note reminds you that printing is what bakes the address onto the physical labels.
- The label sheet preview moved to the bottom of the page. Order is now queue, then the browse-to-add panel, then the preview. You build the batch first (browse, add, set copy counts) and the preview sits at the end as the final check before printing, instead of pushing the browse panel below a tall mostly-empty sheet.
- Change how many copies of a label to print, right from the queue. Each row in the label queue now has minus and plus buttons next to its count, so you can bump a bin from one sticker to six without removing it and adding it back at a different quantity. The sheet preview and page count update as you go.
- Each row in the label print queue now shows the full URL its QR will encode, in the row's empty space, so you can see the address (and spot a mis-set base URL, or an old bare-path label) before you print.
- The labels page uses the width of a wide screen. The queue and its sheet preview now sit side by side on a wide desktop (preview on the right), and fall back to stacked on a narrower or portrait window. The browse panel's Locations tab lays its areas and containers out in two columns to match the other tabs, instead of one tall single-file list with empty space to the right.
- A "Needs you" printer now says why. A machine flagged for attention on the fleet floor used to show an amber ring with no reason, most visibly on the camera wall. It now names the cause right on the tile in every view: clear the bed (a print finished), print failed, or a printer error. This covers prints that finished outside a Cobblr job (a Bambu started from its own slicer), which previously read as "Needs you" with nothing to explain it. (A firmware update being available is not a "Needs you" reason, and never was.)
- The notifications inbox now shows each notification as its own rounded card with clear spacing, an accent tint on unread ones, and the workspace and time as a header line, instead of one unbroken wall of text.
- Jump from a printer's controls to its full record. The cockpit (the camera/controls modal a fleet tile opens) now has an Open machine link in its header that takes you straight to that printer's machine record, on its own collection page, with the detail open. Together with last change's Open controls button, you can now round-trip freely between operating a machine and editing its specs, mods, and notes, from either side. Under the hood, machines in a named collection (3D Printers, Laser Cutters) are now correctly deep-linkable, which also fixes cross-app links that used to point every machine at the default Machines page.
- The sorting planner now matches items to bins by their category, not just by name, so parts land in the right bin at a finer grain (a "1x1 plates" bin apart from a "wedges" bin). It also plans a specific pile of items on request and handles large batches without an AI ceiling, so a whole disassembled set can be organized at once.
- The put-away planner no longer dead-ends silently when there is no AI. Without a connected AI provider the planner can only reuse bins that already hold similar things, so a fresh workspace ended up with everything "unassigned" and only a faint "Similarity plan (no AI)" label to explain why. It now shows a clear, actionable notice: "No AI connected, the planner is grouping by where similar things already live, and won't propose new homes, so brand-new items stay unassigned," with a Connect AI link. When AI is connected but the plan call failed for a run, it says so and offers Re-plan instead.
- The put-away planner now self-heals a stale no-AI plan. Plans are cached (so reopening the sheet does not silently burn AI tokens), which meant a plan first built while AI was not connected stayed the similarity-only version forever. Now, when you open the planner and the saved plan is the no-AI one but your AI is connected, it re-plans exactly once with AI, caches the result, and never re-runs on its own again. The explicit Re-plan button is still there. Separately, the planner now passes the requesting user to the AI call, so a user-scoped personal AI connection (bring-your-own credentials) actually resolves instead of quietly falling through to the managed provider and landing on the heuristic.
- The put-away card now names both what needs a home and what's ready to go. Instead of only "3 scanned items without a home," the dashboard card and scan strip acknowledge the whole picture, e.g. "3 scanned items without a home, and 15 ready to put away", so items you've already assigned (waiting on an "I did it!") are counted too. The card also shows when everything is assigned and only needs putting away.
- The QR "New-code style" chooser now shows both options side by side as equal-width cards, each with its own description, so you can compare Self-describing vs Opaque without clicking to find out what the other does.
- QR label URLs are much shorter. A descriptive token is now a short kind code plus a 12-char slug (e.g. /qr/loc/Xk7d2mNq9pLc) instead of the entity's full UUID, so the QR is less dense and scans more reliably on small labels. Already-printed labels keep resolving. Public labels stay opaque and unguessable (a longer slug, no readable prefix).
- The scan inbox Export button is no longer a blunt "dump everything." It opens a chooser: pick exactly which items (checkboxes, pre-ticked from whatever you had selected), and choose how photos travel. Link keeps the file small and hands the destination a per-file, expiring link to fetch each photo (you pick the expiry: 1 hour, 24 hours, or 7 days). Baked in embeds the images in the export file itself, so it is fully self-contained and works for an offline or LAN-only destination that cannot reach back. None exports metadata only. The instance sets the default mode (self-hosters default to Baked in; a hosted instance can set `SCAN_EXPORT_DEFAULT_PHOTO_MODE=link`) and you override it per export. On the security side, a Link export now mints a per-file token scoped to only the file it names, instead of one org-wide token, and the default lifetime drops from 14 days to 24 hours. The importer restores baked-in photos with no network and still fetches links when present.
- The scan inbox is harder to disturb by accident. The merge control, which folds one scanning session into the previous one, used to sit on the collapsed session header with a down-arrow that was easily mistaken for the expand arrow, and a stray tap mixed unrelated sessions together with no way back. Merge now lives inside the expanded session as a plainly-labeled "Merge into the previous session", and every merge shows an Undo. Separately, the standalone green "Scan session active" banner no longer repeats the session row it sits above: when the live session is already shown in the list, its active dot and End control move onto that row instead.
- Sent-back items get surfaced, and a session shows when it was last edited. When you send a committed scan back to the inbox, it now returns to its original spot in history (its scan time is preserved) and the page scrolls to it and flashes a highlight so you don't lose it. And a scan session whose items were touched well after the session (a later fix, or an item sent back from a commit) now shows a faint "edited <time> ago" next to its real scan time, so the header reflects when you scanned, not when you last poked it.

### Fixes
- AI features that quietly ignored your connected AI now use it. Several AI calls (scan identify, receipt reading, web-search product lookup, pattern extraction, template matching, catalog matching) were not telling the AI layer which user was asking, so a personal AI connection routed to just you would not resolve and the feature fell back to no-AI. All of these now pass the requesting user, so your bring-your-own AI is used everywhere it should be. A new build check (`lint:ai-invoke`) keeps it that way: every AI call must pass the user, or be explicitly marked as a background job, so this whole class of "why is there no AI here" bug can't come back.
- Machine and asset tiles show their photo when browsing to label them. The label page's browse panel drew an initial-letter chip for every machine and asset even when the record had a picture, because those two kinds dropped the photo on the way to generic lists. They now carry it through, the same way inventory parts already did, so a printer shows its printer and a tool shows its tool.
- Cobb's opening line now matches your write-mode chip. It only promises to check with you before changes in Ask mode; in Auto it says changes apply as you go (tracked and undoable), and in Off that it won't change anything. A fresh Laser Cutters install also pins "Laser fleet by state" (always populated) instead of "by tube type" (empty until you fill it in).
- Clicking "Ask Cobb" again now hides the assistant panel instead of doing nothing. The button toggles, and its tooltip reads "Hide Cobb" while the panel is open.
- Labels added from the Labels page's browse panel now encode a scannable QR URL (your label base URL plus a QR token) instead of a bare internal path, so a phone camera opens the record. Previously only the per-item "QR" button did this, and browse-printed labels ignored the custom label base URL.
- Opening Labels no longer crashes with a "Something broke on this page" error while the list of labelable items is still loading. A hook in the browse panel ran only on some renders; it now runs on every render.
- The put-away plan no longer shows two conflicting messages at once while re-planning. When you re-planned, the "Re-planning… the current plan stays visible" banner and the stale "AI couldn't build a plan / No AI connected" notice both showed at the same time. The AI-status notices are now hidden while a re-plan is in flight, matching how the other banners already behave, so you see one clear message instead of two contradictory ones.
- Adding a scanned item no longer wrecks the plan you built for the others, and unrelated things stay separate. Two planner fixes. First: if you'd grouped some items (say a headset and a mount into "Aviation Accessories" via a hint) and then scan one more unrelated thing (a TV), the plan now keeps your existing groups and just adds the new item, instead of re-planning everything from scratch and losing your work. Second: items that have nothing in common no longer get lumped into one "Unassigned" group. Each unrelated orphan is its own entry with its own destination, so a TV, a headset, and a tablet mount are three separate decisions, not one impossible-to-file pile.
- Sending a committed scan back no longer rewrites its scan time. Un-confirming an item (Send back) used to stamp it with the current time, which dragged its whole scan session's header time and inbox position forward to the moment you undid it. Un-confirm is an undo, so the item now returns to its original spot with its original scan time intact; only its "last touched" time moves.
- The workspace list (the menu you open from the workspace name) is tidier. Names sit further left now that the active workspace is marked with a slim accent bar instead of a reserved checkmark column, so more of each name fits. Hovering a row no longer squeezes or hides the name: the rename, members, and default-star buttons fade in over the right edge instead of pushing the name aside. The drag-to-reorder handle is hidden until you hover the row, so it stays out of the way.

## 2026-07-10

### Features
- Cobb (and MCP) can now reach everywhere. The AI tool registry grew real coverage of the whole workspace: purchase orders + vendors, sales customers + orders, builds, locations, tags, and catalogs are fully creatable/editable/deletable through the AI (digifab jobs: edit/delete; files: delete). Your custom fields: including their dropdown choices. Now ride along with every kind, so "set the finish to matte" works on a field you defined yourself. A new list_related tool follows a record's links ("which projects use this part?"). And tagging + placement are now invokable actions, so "tag these summer-stock and put them in Bin 4" just works, in Ask Cobb (with the usual confirm cards + undo ledger) and over the MCP server alike. Long lists are honest too: a capped page says "there may be more" instead of letting the AI overclaim.
- Cobb gets an autopilot, because every change he makes is now tracked and undoable. The "Propose changes" toggle grows into a three-way mode, Claude-Code style: Ask (every change needs your Confirm; the default), Auto (record creates, edits, and deletes apply immediately), and Off. Auto is safe to hand him because of the new AI change ledger: every write Cobb performs, confirmed or automatic, is recorded with a full before-image, and gets an ↩ Undo button right in the chat. Undo a create and it's deleted; undo an edit and the old values come back; undo a delete and the record is recreated. Actions (print a label, adjust stock) still always ask; they can't be un-done in the real world. And the classic record kinds (parts, machines, assets, projects, tasks, lists) are now fully editable/deletable through Cobb too, not just createable.
- Cobb can now actually look at your workspace, and do several things at once. Ask "which of my yarns would suit this pattern?" and Cobb *reads your real records* (search, list, fetch, under your own permissions) before answering, instead of guessing. Ask him to "save that pattern as a note and add the hook to my shopping list" and you get each proposed change as its own card to confirm or skip: writes still never run without your say-so, now including updating fields on a record and deleting one (where a module allows it). Works with every provider that supports tool calls (Anthropic, OpenAI, OpenRouter, tool-capable local models); anything else falls back to the old behavior gracefully.
- You decide what Cobb may touch: right in the chat. Two toggles now sit at the top of Ask Cobb: Read my data (may Cobb search and read this workspace's records to answer you, worth knowing: that record data is sent to the workspace's AI provider, which for a shared AI is another member's connection) and Propose changes (may Cobb suggest creates, updates, deletes, and actions, each still needs your Confirm). Both are on by default, per-person per-workspace, and enforced on the server, flipping one off actually withholds those abilities from the model; it doesn't just hide buttons.
- Number fields can declare their unit. Adding a number field (Configuration → Fields) now offers a Unit picker: "mm", "g", "in", or any unit from your workspace vocabulary. Values render as proper quantities everywhere ("12 mm", honoring your symbol-vs-word display preference), the input shows the unit while you type, and bundles can ship fields with units built in (the CNC tooling, fasteners, yarn, laser, pet-care, and plant-care bundles now do). A declared unit also tells Cobblr what a number physically *is*, which is what upcoming size-aware features read, never your field's name.
- Fill a parts rack with one grid. A layout's edit mode gained + grid: rows × columns and a naming scheme (A1…C8 or Bin 1…24) create and place a whole set of bins at true scale in one shot; a 24-bin rack stops costing 24 modals. Each bin is an ordinary location afterward: scannable, labelable, heat-mapped.
- Per-layout snap grid: 42 mm = exact Gridfinity. Every layout now has its own snap pitch (edit mode, "grid"): a drawer set to 42 mm quantizes bin positions and sizes to true Gridfinity cells, the edit dots show the real pitch, and the panel reads sizes in grid units ("3 × 2 u"). The garage keeps its coarse default.
- Named instances are first-class kinds now. Every named instance (a "3D Printers" copy of Machines, a "Yarn" copy of Inventory) registers its own entity kind, synthesized from its module's declared primary kind. Instance items now place on floor plans, resolve in search, and are reachable by AI tools, all through the one registry, with instance-rooted endpoints that are never guessed.
- Live Sort now works on a totally empty workspace: bins that name themselves. No bins yet? Set up bins in a Live Sort session creates numbered Bin N locations (plus an optional Unsorted catch-all); you write matching numbers on real containers with a marker. Scan the first item of a brand-new family and the directive becomes a bind offer: "go to Bin 4, this starts your Fasteners bin". Confirming names the bin ("Bin 4 · Fasteners"), the number on the container stays right forever, and the next fastener routes there automatically. Undo un-names it. QR labels stay optional: print them from Labels any time, the bin's identity never changes. The new-workspace homepage now offers the whole flow as a first step ("Got a pile of stuff to put away? Start a Live Sort").
- Live Sort: zero-tap sorting and a paired big screen. Two additions to the sort session. On the phone camera, an opt-in auto-Done toggle makes the next scan confirm the previous directive: point at thing after thing, zero taps, with the Undo chip covering mistakes. And the sort session now pairs across devices: open Live Sort on a PC, scan with your phone in Sort mode, and each directive appears on the big screen the moment it's minted; confirming on either device advances both. Same session, two screens, no setup.
- Live Sort: scan a thing, get told where to put it. The scan page's new Live Sort button (and the camera scanner's Sort chip) opens a streaming put-away session. Every scan comes back with a destination directive ("go to Bin 3", with the evidence: "4 similar items already live here"); you drop it in and confirm with one gesture (spacebar at a bench, a big Done button on the phone, or scan a bin's QR to file it there instead). Similar items in a row route instantly ("Same as the last one."), a no-confidence match parks honestly in your catch-all bin, an Undo chip reverts the last confirm, and End session shows the payoff: items sorted, per bin, plus what's left for review. The Guided Organize put-away walk now runs on the same session engine, so a walk resumes exactly where you left it, and a phone sort session resumes on the PC.
- Your fleet tiles and your machine records are finally the same machines. A fleet tile whose device is linked to a machine now wears the machine's own name (the connection label demotes to the subtitle), and on the camera wall a printer with no camera shows the machine's photo instead of a dark void. In the other direction, the 3D Printers / Laser Cutters pages now show a small live chip on each linked machine (printing 46%, needs clearing, idle, offline) right beside its lifecycle state. The registry and the floor stopped being two disconnected identities: first slice of the one-machine-two-lenses plan (docs/design-decisions/machines-digifab-unification.md).
- The put-away plan gives you the last word on everything. Four new controls on the plan sheet. Rename a proposed new bin inline (pencil next to the chip): your word for it beats the AI's, and the renamed bin becomes the evidence that routes every future item of that family. Split any item out of a group with the × ("not related"): it stays in the inbox, unfiled. Expand any item (the chevron on its row) to open its full card right in the plan, accordion-style, and fix a wrong name, photo, or identification in place; the card is a fixer there (no confirm-into-a-table, no discard, so nothing gets accidentally filed mid-plan), and the sheet notices your edit and offers Re-plan. And a hint box lets you tell the planner ground truth ("these are camping gear") and re-plan with it folded in; groups you already accepted stay filed.
- The floor plan's whole someday list, closed out. The describe seed now also places the furniture you name (matched to existing sub-locations, never invents or overwrites); big rooms on a floor plan preview their contents inline from the zoomed-out view; members get read-only plans at `/portal/…/locations/…/plan`; and search results grew a where-is-it location chip. Plus the floor-plan end-to-end drive joins the e2e yardsticks.
- Items on the plan, photos in the rects, trapezoids for the wrenches. A layout can now place the things themselves: machines, assets, and parts each take a cell on their location's plan, with an "items here, not placed" strip in edit mode and drag-to-re-file into any box. Placed rectangles (items and bins alike) can carry decoration: photo fill, or a silhouette outline (taper-left/right, triangle) drawn inside. Overlapping rectangles are fine; the smaller stays clickable on top.
- The put-away front door. Scanned things and never put them away? Now the dashboard says so: whenever captures exist with no home, a card appears ("14 scanned items don't have a home yet") with one action: Put them away. It previews where everything should go (grouped destinations, with the evidence), nothing moves until you confirm, and the guided put-away walk handles the physical half. The scan page carries the same strip above the inbox, and the card disappears when everything has a home. Live Sort stays where scanning starts (the scan page, the camera's Sort chip); if you re-scan something already captured mid-sort, it gets routed, never duplicated.
- A wrong commit is redoable. Committed a scan into the wrong table, or as the wrong thing? The scan page's new Recently committed list has a Send back button: the scan returns to the pending inbox for you to fix and redo, and the entry it created is removed from its module. A scan that merely attached to an existing entry (add-quantity, link, move) never touches that entry when sent back. The mirror of Recently deleted: every scan decision now has an undo.
- Your external Claude can now fully read AND write your workspace. The MCP server gains `cobblr_create_record`, `cobblr_update_record`, `cobblr_delete_record`, and `cobblr_search_records` (cross-kind search) alongside the existing reads and actions, so a Claude Desktop / claude.ai connector can save a knowledge entry, fix a record's fields, or clean one up, not just look around. Under the hood these come from a new shared tool registry (`@cobblr/workspace-tools`) that the in-app Ask Cobb chat is being wired to as well, so the two AI surfaces stay capability-identical by construction. What's writable is honest by design: a record kind is creatable/updatable/deletable only where its module declares the route, no guessed endpoints, no surprise 404s.
- Organize now knows what fits. Tell a container its interior size (Edit location → "Interior size (mm)", optional) and give your measurement fields units, and the put-away planner stops suggesting physically impossible homes: a 300 mm drill bit never gets routed into a 100 mm bin, however similar its neighbors, and if a plan does target a tight bin, the group carries a visible "may not fit" warning instead of a silent mistake. Declared data only: no dimensions declared, no size logic. Cobblr never guesses physics from names.

### Improvements
- The homepage is more useful. A pinned view that groups its data (a kanban "by state", "by type") now shows the group breakdown with counts and proportion bars, so you see the distribution at a glance instead of a flat list of the first few rows. The redundant full/half width toggle is gone. And your scanner's waiting-to-file queue (with the "what do you want to do" shortcuts) is now a proper section above recent activity, where you can reorder or hide it, instead of buried at the very bottom.
- The Fleet tab on 3D Printers shows your 3D printers, not the whole shop. A machine collection's Fleet tab (3D Printers, Laser Cutters, ...) now scopes to devices linked to that collection's machines: the laser no longer photobombs the printer floor. Nothing is silently hidden: a footer counts the farm devices outside the scope and links to Digital Fabrication, which remains the whole-shop floor (pools and production runs cross collections on purpose).
- The plan hint can name places that don't exist yet. On a fresh workspace with no locations, tell the planner "I have an electrical closet, electrical items go there" and the plan proposes New location: Electrical closet with the matching items grouped into it; accepting creates it. Your first locations can be made by describing your home.
- Entity kinds can now declare a `listEndpoint` in their manifest, alongside the existing create/update/delete declarations. First-party generic surfaces (the floor plan's entity occupants, future pickers) enumerate a kind's rows through its module's own list route, with the module's own role gating, instead of hardcoding module names. Machines, assets, and parts declare it; any future module joins with one manifest line.
- The dashboard "at a glance" tile for a multi-type module (Machines, Assets) now lands on that module's page instead of diving into just one type. When the base list is empty but you have types like 3D Printers and Laser Cutters, the page shows those as a chooser with counts. So "Machines · 59" opens a page that actually shows printers and lasers, not just the biggest one.
- Organize plans stopped guessing. A put-away plan could previously file items into a room-level location on vibes ("Garages is the natural home for vehicle equipment") with no evidence anything similar lived there. Now: destinations backed by what's actually in a bin show their evidence as before; a pick with no evidence is either converted into a proposed new bin under that room (rooms aren't homes, bins are) or clearly labeled as the AI's suggestion, not a match (amber). Plans also use your workspace's own vocabulary for group names instead of invented retail categories, plural forms and the Accept-all button now say what they count, and the dialog title matches how you opened it ("Put away your scanned backlog").
- "Put them away" opens instantly, and the plan sheet stopped being clunky. The dashboard card and scan strip now pre-compute the plan in the background the moment they show the count, so clicking reveals a ready plan instead of a spinner (it refreshes automatically when your backlog changes). The sheet itself got wider (the same width the inbox uses), every item row expands with a proper right-side dropdown arrow (the whole row is the click target), a proposed new bin is renamed by clicking its chip and typing right there, and clicking any destination chip opens the location picker directly beneath it instead of a control stranded three levels down.
- The floor plan's entity occupants now derive placeable kinds from the entity-kind registry (declared list + update endpoints, physical trait) instead of a baselined hardcoded module list. Any future module's items become placeable with one manifest line; the lint baseline is empty again.
- When the scanner confidently recognizes a specific known item (a named book, film, album, or a product by make and model), it now fills in fields it can know for that exact item, like a book's ISBN, publisher, and publication year, even when they aren't printed in the photo. Fields filled from catalog knowledge rather than read off the item are flagged in the notes so you can double-check them. Generic or uncertain items stay conservative and only use what was actually observed.
- The "A new version is available" prompt now appears as a toast in the bottom-right, alongside the other toasts, instead of a pill floating in the middle of the screen. Tap Refresh to load the update.

### Fixes
- Tightened the spacing between the actor, action, and item name in each recent-activity row so they read as one sentence and more of the item name fits, especially on mobile.
- A pinned grouped view on the dashboard now groups strictly by its own group-by field. Before, a view grouped by a field that was empty on the items (like "Laser fleet by tube type" when no laser has a tube type set) fell back to showing each item's subtitle, which is a different field, so it displayed the wrong data under the wrong heading. Now an unset value is its own bucket, and when nothing carries the grouping field the card says so and points you at the fix instead of drawing a misleading breakdown.
- Operator Console light mode no longer shows a dark band across the top. The header now follows your theme (light on a light page, slate in dark mode) while staying visually distinct so it's clear you've left a workspace.
- Opening the guided put-away planner or Live Sort from a deep link no longer leaves `?organize=pending`, `?livesort=1`, or `?sort=1` stuck in the URL. Before, the parameter stayed behind, so every page refresh reopened the planner (or re-armed Sort mode even after you turned it off). These links now trigger once and clear themselves.
- The scan inbox location picker is wider now, so a nested "Room > Bin" path shows in full instead of truncating to a few characters. On a wide header the "Email receipts to" block is pushed to the right edge to make the room.
- Once you tag a series on the scan page, later-arriving books in that same series now tag themselves automatically instead of resurfacing the "1 of N isn't tagged yet" banner. Vision identifies items incrementally, so a straggler that finishes after you tapped "Tag series" used to slip through. Now it's swept in.

## 2026-07-09

### Features
- Organize what you already track. The scan page's new Organize header button points the put-away planner at your *committed* things that have no location yet: everything Cobblr tracks but doesn't know a home for, grouped and routed to bins the same way a scanned pile is (evidence, proposed new bins, per-group accept, the put-away walk). Homeless inventory gets homes without re-scanning anything.
- Organize a whole pile at once. Select 2+ scanned items and hit Organize…: Cobblr groups them by what they are and proposes where each group should go: an existing bin where similar items already live (with the evidence shown), a sensibly-named new bin, or "needs a destination" when there's no defensible answer. Accept per group or all at once; nothing moves until you say so, and anything you already filed by hand stays put. Works without AI too, grouped by where siblings live. Then Start put-away walk for the physical half: bin by bin, each destination becomes your active filing bin and you scan (hardware scanner) or tap items off the checklist: progress saves, so a Resume put-away walk chip picks up where you left off.
- Homebox is now a live sync connector. Alongside the CSV import, you can connect a self-hosted Homebox instance under Configuration → Integrations → Live sync → + Add connection → Homebox: paste its URL + an API key. It brings your items across whole (name, quantity, cost, serial, model, manufacturer, warranty, custom fields, and each item's photo), rebuilds your location hierarchy, and files each item into its location. Import once, or leave the sync on to keep mirroring; matched by Homebox id so a re-run updates in place. (A quick heads-up for the curious: Homebox's API lists items in summary form and keeps locations as a tree, so the connector re-fetches each item's detail and flattens the tree; you don't need to think about any of that.)
- Switching from Homebox? There's now a one-stop migration under Configuration → Integrations → "Migrate in from another app". Pick Homebox, upload its CSV export, preview exactly what will happen, and import: your items become Inventory parts (with serial, model, warranty, cost and all), the location paths rebuild your Locations hierarchy, and labels become tags. Anything Cobblr has no column for is kept safely in each item's details, so nothing is lost. It lives right beside the live sync connectors (Ravelry), because Homebox's CSV is its most reliable interface. (Photos aren't in Homebox's CSV, a follow-up will bring those.)
- One-click "Import from Homebox (live)." Under Configuration → Integrations → *Migrate in from another app*, a new Homebox: live card imports your whole Homebox in one step, paste your Homebox address + an API token, and it brings your locations, items, custom fields, and photos across (photos are the thing the CSV export can't carry). When it's done it asks whether to keep it synced (future Homebox changes mirror automatically) or import just this once: either way the connection is saved under Live sync, so you can re-run it, turn on syncing later, or archive it anytime. A Homebox on your local network is reached through your workspace's edge bridge. The old CSV import is still there as Homebox, CSV for when you'd rather not use the API.
- Whole-house floor plans: floors as tabs, rooms that zoom. A building's page now shows one plan per floor behind tabs ("Main floor" / "Basement"; "+ floor" creates them sized). Rooms draw on their floor as visible rectangles; clicking one zooms into the room's own plan in place, fully editable, without losing the house view. A toolbox's laid-out face zooms the same way. Dropping something inside a room re-files it there and lands it on the room's plan at the same physical spot.
- Floor plans: draw a space to scale, and let it file things for you. A location's detail page can now hold a real 2D plan: the room as a rectangle (dimensions in whatever unit you think in: "33ft", "7.3m"), interior walls with door cutouts, and every rack/toolbox/bench placed where it stands. Each rectangle IS a storage location: click to open it, drag it into a bay and it re-files there. Zones show as faint toggleable labels, never chrome. Containers get the same treatment in elevation, a toolbox's face lays out its drawers at true scale, partial-width ones visibly partial. Or skip the drawing: describe the space in a sentence and the AI drafts the plan for you to true up. The day-to-day view shows no numbers at all, measurements live in the data and appear only while editing.
- OpenRouter, first-class: one key, any model. Add an OpenRouter connection (workspace AI or your personal AI connections): paste your key, name a model (`anthropic/claude-sonnet-5`, `openai/gpt-5.5`, …), done, no base URL to know, and switching models later is editing one field. The generic OpenAI-compatible provider gains the same optional Model field, so vLLM and other exact-name servers work on a personal connection too (LM Studio still needs nothing). And Ask Cobb's create-from-chat got honest and smarter: modules now *declare* which records can be created from chat (knowledge entries and tracking metrics join in), Cobb is told each kind's real field names so a saved pattern lands with a proper `title` instead of erroring, and it never offers a create that would fail on confirm.
- The floor plan works for you now: scan into, heat, print. Every plan's toolbar gained three buttons: scan into opens the camera already filing into that space (rooms' zoom views arm directly: "I'm standing at the workbench"), heat shades each box by how much is stored in it (rooms count their whole subtree), and print opens a paper-white plan in a new tab, one page per floor for a whole building.
- Tell a bin what it is: pair its QR with the product's UPC. Scan a bin's QR label to make it the active bin, then scan the container's own product barcode (a "Sterilite 6qt", say) and tap "This is ‹bin› itself" on the result card. The product identity lands on the bin's own location record, brand/product under its details, the catalog photo as its picture, instead of filing a new item into it.
- Self-hosting gains a fourth TLS mode: `COBBLR_TLS_MODE=tsnet`. The bundled proxy joins your tailnet as its own virtual node and serves Cobblr at `https://cobblr.<tailnet>.ts.net` with a real, auto-renewed certificate. The box needs no Tailscale install and nothing exposed; you paste one reusable auth key into `.env` and `docker compose up`. The blank-`COBBLR_ACME_EMAIL` crash in the DuckDNS/Cloudflare modes is fixed in the same image (the optional line is now dropped when empty).
- Docs now ship with the feature, and publish with the release. Every feature changeset carries its user documentation (`## docs` + `docs_target:`), written in the same PR while the change is fresh, enforced at the merge gate. `scripts/docs-flush.mjs` publishes each blurb into its target doc only once the feature is actually live, so public docs never describe something that hasn't shipped and release-day docs work is zero.
- Sync connections (Ravelry and friends) get a history. A connection you're done with: imported once and don't want it polling, say, can now be Archived instead of removed: it drops out of the Live-sync list into an Archived section below it and stops syncing, but nothing is lost. One click Re-activate puts it back where it left off (its id-map and settings survive). Archive ≠ remove, *Remove* still deletes for good; *Archive* just tucks it away for later.
- Zones: areas inside an area draw as thin dashed outlines, not nested boxes. Bay 1 / Bay 2 / Bay 3 divide a garage. They aren't rooms inside it. A nested *area* in the Locations tree now renders as a featherweight dashed outline with its name pinned to the border line, and its contents stay at full width in the parent's own flow. Dashed = zone, solid = container, so the two grammars read apart at a glance. Pure re-draw: nothing about the hierarchy, scanning, or filing changes.

### Improvements
- Ask Cobb got noticeably smarter and more consistent. Two workspaces used to give very different answers to the same question: one cause was a prompt-delivery bug where the managed AI silently dropped Cobb's workspace instructions (personal-bridge AI kept them), so they now behave the same. Cobb also stops deflecting real questions with "I only manage records"; it answers helpfully (general knowledge included) and *offers* to save the result. And it can now create more kinds of records straight from chat, anything a module exposes, like a knowledge entry, instead of a fixed short list. (Groundwork for full tool-calling, reading your workspace's contents and taking multi-step actions, is written up in docs/design-decisions/ai-chat-tool-calling.md.)

### Fixes
- Cleanup from field-testing the shared-AI flow. The owner's offer email now says Ask Cobb (one last "Ask Cobblr" straggler); a workspace chatting through a direct Ollama provider gets Cobb's workspace instructions again (a prompt-delivery fix had missed that one adapter); resolving an offer clears the stale notification for every owner, not just the one who clicked; and the Edge bridge ● online/offline indicators now also cover URL connections that ride your bridge (LM Studio via bridge transit), not just the dedicated Local-AI connection.
- Saved views open as a real page, and their rows are clickable. Opening a pinned view from the dashboard now lands on a full-page view (`/views/‹id›`) instead of a preview modal floating over the Views configuration list. And every row is now a link: dashboard pinned-view rows, kanban cards, and list rows all click through to the machine/asset/part they name (registry-driven, so instance kinds like 3D Printers resolve too).

## 2026-07-08

### Features
- See what's inside your things. A machine's detail now has an "Installed components" panel: add the parts that live inside it (a printer's mods, a server's PSU/SSD), see them at a glance, and remove them. It's a generic containment panel that will show up on other containers next.
- Scan components straight into a machine or server. Point the camera at a machine or asset's QR label to make it the active bin, then scan each component's barcode. It's identified and dropped *inside* that container automatically. The "scan into a location" flow you know, now for any container.
- Saved views got a real builder, and sorting now works everywhere. Creating or editing a view is now a field-aware form instead of a raw text box: pick filter fields and operators from dropdowns (`is`, `is any of`, `is not`, and numeric comparisons), pick columns and a group-by field, and (new) add sort rows to order a view by any field, ascending or descending, with tie-breaks. "Is any of" lets one filter match several values at once (e.g. machines that are *building* or *rebuilding*). Sorting is now honored by every module's list (machines, assets, and orders join inventory and projects), so you can finally order your things by most-recently-updated, cost, priority, or whatever field matters.

### Improvements
- When a member offers to share their AI with a workspace, it no longer sits unseen. The workspace owner now gets an email as well as the in-app notification, a dashboard callout with Approve / Decline right there, and an amber dot on Configuration: instead of just an easily-missed bell item. And on phones, the header's notification bell finally has a home: the menu now carries a notifications row with an unread badge (and a red dot on the ☰ button when you have unread items), so mobile users can actually reach their inbox. The person doing the sharing also sees, on their AI connections page, whether each share is *awaiting owner approval*, *approved*, or *active*. No more wondering if it went through.
- Your personal edge bridge (the local-AI tunnel) now shows whether it's actually connected: the same green "online" / grey "offline" dot the workspace's 3D-printer and device bridges already use. It appears on each edge-bridge connection in Connections, and, if you run one, as an Edge bridge ● row right in your account menu, so you can glance at it from anywhere instead of guessing why local AI isn't answering. Same liveness signal under the hood, just surfaced by *your* bridge instead of a workspace's.
- Groundwork for tracking what's *inside* your things: a new containment relationship so a component can live inside a machine or server (and a part inside a printer). This step adds the plumbing; the panel to see components and scan them straight into a container arrives in the next update.

## 2026-07-07

### Features
- Adding a printer to a detection service (like PrintGuard) from Cobblr now works for any connection type the detector supports: including Bambu: driven by a small manifest mapping rather than hardcoded per type. Mirror an existing Cobblr machine and it reuses the credentials Cobblr already holds (they stay server-side, never sent to your browser); for a Bambu you pick which printer (serial). For a single-printer connection you can tick "stop polling it in Cobblr" so only the detector talks to that printer, no double-load.
- When an external detector (like PrintGuard) owns a printer, Cobblr now fully stands down for that printer: per printer, so it works even inside a shared multi-printer connection (e.g. one printer of a Bambu farm moved to the detector). Cobblr stops its own AI watch and camera pull for it, won't send it jobs, and marks it *"watched by detector"* on the floor, so the printer's camera is only ever touched by the detector, with no double-load. Its telemetry (status/progress) keeps flowing, so you still see it.
- New "PrintGuard (send frames)" detector option: Cobblr grabs a frame and posts it to PrintGuard for a verdict, instead of PrintGuard pulling the camera itself. This is the path for cloud Cobblr + a LAN camera (or any setup where PrintGuard can't reach the camera but Cobblr can). It uses PrintGuard's new `/classify` endpoint, which ships in PrintGuard 2.3.0, and Test now reads the running version and tells you clearly if the box is too old ("needs PrintGuard ≥ 2.3.0, found 2.2.2") instead of silently doing nothing.
- Ravelry is now a proper sync connector you point at any table. It used to be a fixed card on your profile that only imported into the Yarn bundle. Now it lives with your other integrations (Configuration → Integrations → Add connection → Ravelry), and you choose which inventory table your stash lands in: the flagship Yarn table or one you built yourself. You no longer need the Yarn bundle installed. You get the standard sync tools too (a before/after import preview and ongoing reconcile), and length is stored as Ravelry provides it (metres), with yd/m display left to your unit preference.

## 2026-07-06

### Features
- Print-failure detection can now use a self-hosted detection service you run anywhere: point it at an Obico ML API or PrintGuard box (or any HTTP model on your LAN) and the spaghetti watch scores prints through it. Pick "External detector" as the backend, add the service's URL + token, and go. Nothing is hardcoded: each service is a small manifest, so new ones drop in without a code change.
- Printed QR codes can now outlive this instance. Under QR codes (owner/admin), set a Label base URL: a stable name you control (a domain, a DuckDNS, or a Tailscale name) that forwards the `/qr/…` path to wherever your workspace lives. New codes encode that base instead of the instance's own address, so if you later self-host, change domains, or leave the hosted app, you just re-point the forward and every printed label keeps working. Leave it blank to use the current address; already-printed codes are unaffected.
- Your lists now show the right face on their own. A collection you count and reorder (filament, screws, groceries) keeps its full stock view: quantities, reorder points, allocations. A collection you just catalog (films, books, recipes, anything with no quantities) reads as a lean list of your own fields, with none of the stock machinery cluttering it. The platform decides from your data, so there's nothing to choose; a one-tap Auto / Always track stock / Catalog only control in the list's Settings is there for the rare case you want to override it. Your list also speaks its own words now: name a collection "Films" and it says "New Film", "search films", "No films yet", never "part", with a "What do you call these?" setting to fix the odd plural. Two new saved-view layouts arrive too: a gallery (a poster wall) and a heatmap (a habit-tracker grid).
- The scan inbox can now export itself to a file, not just import one. The new Export button downloads your whole inbox as an interop file (JSON or CSV): photos included, via short-lived no-auth links another instance can fetch. So moving a scan inbox between workspaces or between Cobblr instances is now Export here → Import there: no re-scanning, no database surgery. It round-trips both ways (and still interops with companion app); on re-import Cobblr re-runs its own matchmaker.

### Improvements
- Feedback moves into the sidebar in full-sidebar mode. When you hide the top bar, the Feedback button is no longer a pill floating over your content. It becomes a row at the top of the sidebar foot, in the same "notices" area as the verify-your-account nudge, set apart from the workspace tools below it. Top-bar mode keeps the floating pill (there's no sidebar foot to host it there). Same one-click Send-feedback dialog either way.
- The quick theme toggle can now offer to set your account default too. Flipping a screen to dark (or light) with the header or ⌘K toggle still only changes that one device, the way it always has. But if you haven't set an account-wide default yet, a small prompt now offers to use it on all your devices. Take it and the choice syncs everywhere (and that device goes back to following your account default); ignore it and nothing changes. It stops asking once you've made an account-level choice, only ever appears a few times, and has a "Don't show again" if you'd rather it stay quiet.

### Fixes
- New workspaces provision more reliably. If a momentary database collision hit while your workspace was being created, signup could return a workspace that wasn't actually set up yet (its first page load failed). Provisioning now retries through that brief contention, so the workspace lands ready.

## 2026-07-05

### Features
- Knowledge Base entries can now hold an image: upload a screenshot (e.g. a scanner's config barcode, which must be *scanned from the image*, not regenerated) or any visual reference. It shows as a thumbnail in the list and full-size in Quick Access, so a pinned entry's barcode is scannable at a glance.
- New Knowledge Base module: keep rich Markdown entries (notes, references, SOPs, prompts, papers) in one place. Each entry has a Markdown body (with the Write/Preview editor), a category, an optional code, and a pin for quick access; search + filter by category. Enable it from Configuration.
- The 3D Printers page (and every Machines instance) now has saved views as chips: one click to switch perspective instead of re-checking a filter. Pick the shipped "Printer fleet by state" to group your printers by state, or hit Save view to name your own (which states to include + optional grouping) and pin it as a reusable tab. Your saved filter becomes a first-class chip, the same way Inventory and Projects already work.
- New QR field renderer: any text field (and a Knowledge Base entry's code) can render as a scannable QR of its value. Great for *owned* codes: a UPC, a location or asset tag, a URL. Pinned entries with a code now show a scannable QR in Quick Access.
- New Quick Access drawer: a floating button (shown when the Knowledge Base is enabled) opens a slide-over of your *pinned* entries, so a reference, code, or SOP you check often is one tap away from any page. Pin an entry in the Knowledge Base to add it here.
- Fields can now be Rich text: a Markdown editor with a Write/Preview split and a formatting toolbar (bold, italic, headings, lists, code, links). Ideal for long descriptions, notes, SOPs, and the knowledge base. Renders as formatted Markdown on detail pages and a clean one-line summary in tables.

### Improvements
- When Cobblr can't be reached (a deploy blip, a dropped connection), you now see one calm "Can't reach Cobblr, reconnecting…" bar at the top instead of scattered raw errors. It clears itself automatically the moment the service is back.
- Ask Cobb answers more out of the box. Even with no AI connected and nothing trained, Cobb now fields ~20 common questions: "what is Cobblr", find & search, add/edit/delete something, enable a module, custom fields, invite people, settings, export & backup, labels & QR, mobile, account & password, and feedback, on top of the originals. It's the untrained baseline every workspace gets; you can still tweak or add to it under Configuration → Assistant.
- DMing the Cobblr Discord bot a bug or idea now just gets a 👀: the "this is feedback & support" heads-up shows only once, on your first DM, instead of on every report.
- Picking a location no longer means scrolling one endless indented dropdown. The scan inbox's location pickers (New-scans target, bulk "Set location", and the confirm form) now use a drill-down: rooms open into their contents with a breadcrumb (Kitchen ▸ Rack 1 ▸ Shelf 2), plus a search box to jump straight to any bin, and "New location…" inline. Tap a name to pick it, tap ▸ to open it.
- Scan routing suggestions are less noisy and more consistent. The matchmaker now asks the model for one primary table plus at most one secondary (only when an item actually belongs in two), and never to list the same table twice, instead of padding "up to 3" with a duplicate or a marginal table. So a batch of the same kind of thing (a shelf of books) routes the same way, instead of some getting a stray "Collections" and others a duplicate chip.
- Self-hosters can now control exactly which third-party product catalogs barcode scanning contacts: a master switch (`COBBLR_SCAN_EXTERNAL_LOOKUPS`), independent per-provider toggles (upcitemdb, Open Facts, DuckDuckGo, go-upc), and API-key fields where a provider offers one. go-upc uses its official API when you supply a key; its web scraper is now off by default.
- Photographed books now get their ISBN backfilled from Open Library. An ISBN is almost never printed where a camera can read it, so when a book's match has no ISBN, Cobblr looks it up by title + author: preferring the edition whose publisher matches the book in hand (e.g. the Scholastic paperback, not a random hardcover). It only fills a blank ISBN (never overrides one the identify already found) and leaves it empty when there's no confident match rather than inventing one.
- Every "take a photo of this" spot in the scanner now uses the same in-app capture drawer (the scan result card's *Nice photo* and *Not it) photograph it*, plus the inbox item's *Retake for catalog* and *Add photo*. None of them launches the phone's native camera anymore: on the live scanner they reuse the running viewfinder, and elsewhere the drawer opens its own camera (with a photo-picker fallback when there's no camera). One consistent shutter → Retake / Use this flow everywhere.
- Scan cards now show what a match will fill, right on the collapsed card: the actual field values as mini chips (e.g. `genre Fiction · author Laura Ingalls Wilder · publisher Scholastic`) instead of a meaningless "fills 4". No need to open the item to see what's going into the table.
- Taking a "Nice photo" in the scanner no longer launches the phone's native camera on top of the one already running. It now opens an in-app capture drawer off the live viewfinder: frame it, hit the shutter, then Retake or Use this: and the catalog photo swaps in place with a clear "updated" confirmation instead of a fire-and-forget toast. (Desktop, with no live camera, still uses the file picker.)
- Scanning a whole series now routes uniformly. On top of the tightened matchmaker prompt, a deterministic pass aligns the *secondary* table suggestions across same-series items in a session: a shelf of Little House books either all offer a given secondary table (e.g. "Collections") or none do. No more the AI offering it on some books and not others. It only ever removes a stray secondary, never fabricates one, and never touches the primary table.

## 2026-07-04

### Features
- Public download for the headless edge-bridge binaries: GET /api/v1/desktop/bridge/:os/:arch streams the prebuilt standalone binary for that platform, so the headless install script works with no token on or off the tailnet (it server-side-fetches the private release asset). Needs the operator to set EDGE_BRIDGE_REPO_API + EDGE_BRIDGE_REPO_TOKEN.
- Meet Cobb: the cobblestone-headed craftsman is now the face of the assistant (the Ask Cobb chat and the Build page). And Ask Cobb no longer dead-ends without AI: if your workspace has no AI provider connected, it tells you up front: a "basic mode" strip before you type, and still answers the essentials with no AI at all ("what can you do", "how do I add a part", "where do I scan", "build my workspace"), pointing you to Connect AI for anything about your actual data.
- Teach Ask Cobb what to say without AI. A new Configuration → Assistant page lets you edit the answers Cobb gives when your workspace has no AI connected: reword a built-in reply, turn one off, or add your own keyword-triggered answers (give it trigger words like "returns, refund, send it back" and the reply). A live "try it" box shows exactly what a question matches. It's per-workspace and matches by keyword, no AI involved, so basic mode goes from a fixed floor to something you can actually train.
- New Mobility capability: give a tool, bin, or asset a home, mark it mobile, and Cobblr flags it away when its current location drifts from home, with a one-tap Return home that snaps it back. Fixtures (the default) are untouched. The away age is preserved across unrelated edits and shows as "3d ago", not a timestamp. Works on inventory items *and* assets. Along the way the field system grew two generic pieces every module now gets: relation fields render as a real picker (choose the record by name, never paste an id), and server-managed fields render read-only: no more inputs that silently revert.
- The New-job form now reads the filament material and grams straight from the gcode/3MF you pick, instead of asking you to retype what the slicer already computed. Pick a printable file and Cobblr parses its slicer metadata (PrusaSlicer / SuperSlicer / OrcaSlicer / Bambu comment headers, plus 3MF `slice_info`), pre-fills the grams, and matches a spool by material (PLA → your "PolyTerra PLA" spool): all still editable. On completion the grams deduct from that spool as before. Multi-extruder prints sum correctly, and the material type is recorded on the job even when no spool is matched.
- Reply directly to a Cobblr feedback email and your reply now threads back onto the report: no need to open the app. Inbound email (feedback replies and forwarded receipts) now runs through one shared address handler.
- Cobblr now predicts when you'll run out and reorders *ahead* of empty: no counting, no waiting for zero. It learns each item's burn rate from the usage you already log (every "use one", every stock change), and when something's a few days from running out it lands on your shopping list early. Parts show a live "≈ N days left" estimate. Items with their own signal (a replace-clock, or stock tracked by another system) opt out automatically.
- The honest answer to "how does Cobblr know I used something": no chore, no counting. Two zero-friction ways to log consumption arrived:

### Improvements
- Your activity feed no longer stops at "100 actions". It shows the real total ("50 of 412") and loads older activity as you scroll. Cursor pagination is now the platform default for growing lists.
- Emailing things into Cobblr is now easy to find: Configuration → Integrations → Email in shows your forward-a-receipt address and explains reply-by-email, and searching "email" or "receipt" jumps you straight there.
- The New location form's *Kind* is now two labelled buttons: area (a region: room, corner, workshop) and container (things go INTO it: bin, drawer, shelf), instead of a dropdown, so each option's meaning is visible at a glance. It also guesses the Kind from the name as you type: "White Bookshelf", "Bin 17" or "Kitchen Drawer" pre-select container; "Storage Room" or "Workshop Corner" pre-select area. A small hint shows what it guessed, and tapping a button yourself always wins, the guess only sets the default so you stop choosing it by hand on every location.
- The ⌘K command palette (and the header search bar) now find every settings and system page, no longer only a hand-picked few. It reads the same registry the Configuration hub uses, so anything you can reach in Configuration you can now reach by typing it: "permissions", "backup", "healthcheck", "openapi", "form builder", plus synonyms the label doesn't say ("roles" → Permissions, "audit" → Activity log). And searching "dark mode" (or "theme" / "night" / "light") now surfaces the theme toggle right in the palette: one keystroke flips your per-account light/dark preference, instead of hunting for the sidebar icon (which a branded workspace hides).
- The scan inbox now tells you clearly when the AI is actually done. Enrichment lands in a few passes: the tidied final name, cover, genre note and where-to-file keep arriving for up to a minute after the first result, so a session header now shows a live "N finishing…" pill that flips to "✓ All set" the moment every item is settled, and any card still being refined wears a quiet finishing… tag (distinct from the bold AI reading… of the first pass). No more wondering whether a name is final or about to change under you.
- Three scan-inbox clarity fixes. Each identified item now shows its series as a chip on the card (e.g. *Little House on the Prairie* on a stack of those books): the most useful at-a-glance media label, not buried in the "tag them all" banner. Two suggestion chips that read as the same table are collapsed to one: a workspace with two tables both named "Bookshelf" no longer shows "Bookshelf · 3 fields" next to "Bookshelf · 1 field" with no way to tell them apart (the richer match wins; either is still pickable in the confirm form). And the item subtitle no longer starts with a stray "· " when there's no barcode/ISBN, the author/publisher line is now built from the fields that exist, so nothing reads as missing.
- The per-device theme override (Profile → Appearance → This device only) gains a Match OS option: make just this device follow its own operating-system light/dark setting: live, so it flips with your system day/night schedule, even when your account default is a fixed Light or Dark. Previously a device could only use the account default or hard-lock to Light/Dark; now "follow this screen's OS" is a first-class choice, independent of and without changing your global preference. The Appearance card was also tightened to about half its previous height.
- Theme is now two separate things: a global account default that syncs to all your devices, and a per-device lock that overrides it on just one browser without ever syncing. Profile → Appearance shows both: Account default (Match device / Light / Dark) and This device only (Use default / Light / Dark), so you can, say, keep a light default everywhere but force one shared display dark. Precedence is *this device → account default → the device's OS*, and the quick theme toggle in the header now sets this device only (toggling back toward your account default releases the lock so the device syncs again). The device override persists locally across reloads; it's never written to your account.

### Fixes
- Appearance is now an explicit setting in Profile → Appearance: Match device · Light · Dark. Before, the only way to change theme was a session toggle (an icon that's hidden on branded workspaces), and it only saved a preference if you happened to hit it, so if you never did, each device just followed its own OS and two devices could disagree ("dark on my Mac, light on my phone"). Now picking Dark (or Light) makes that choice explicit and it follows your account to every device, overriding each device's OS; Match device is a deliberate, saved choice that follows each device's OS live. Persists per-account, unchanged for other members, and a workspace with its own branded theme still shows that theme.
- An empty multi-instance module (Projects, Inventory) no longer shows a permanent "0 / none yet" card on the dashboard. Its per-instance tile now collapses into the quiet "Also enabled" line exactly like every other empty module, and a stale duplicate (the module appearing in "Also enabled" *and* as a card) is gone: dashboard tiles now clean up their empty-state report when they're swapped out (which happens the moment a module's instances finish loading).
- The scan matchmaker no longer offers to install a bundle whose table name collides with one you already have. Previously, if your workspace had a "Bookshelf", the menu could still surface a community "Bookshelf" bundle right beside it: two chips you couldn't tell apart, and confirming the wrong one would create a duplicate same-named table. Now a not-yet-installed bundle entry is dropped when its display name matches a live table's, so you file into the table you actually have.
- Three scan-inbox papercuts. Re-run AI now respects an image you picked: a plain re-run keeps your chosen cover and only re-images when you explicitly flag the item as *wrong* (the identify used to wipe the lock and clobber your pick). The "part of the series" banner counts every book: it now folds in a same-author title whose series the AI happened not to label (so 9 Little House books read as 9, not 8), and its wording reads *"N items are part of the "…" series."* And a card no longer shows the same table twice (a duplicate "Bookshelf" chip): candidates routing to one table are collapsed to the richest single suggestion.

## 2026-07-03

### Features
- AI failure detection (the spaghetti watch): turn it on in Setup and Cobblr watches each printing machine's camera, builds a rolling failure score, and auto-pauses + alerts you when a print is clearly failing. It uses a local model on your bridge when available: the frame never leaves your network and costs no AI tokens, and falls back to your workspace's vision AI. Tune the sensitivity, the detector, and whether it auto-pauses or just alerts.
- Pin the workspace that opens on a fresh device. In the workspace switcher, a star on each row sets your default: the one a new device (or a bare URL with no history) opens into. Before, a fresh sign-in just landed on your first *owned* workspace, which isn't always the one you want; now you decide. A device you've already used still resumes wherever you last were; the default is the no-history landing. One default at a time; click the star again to clear it.
- Auto-update feed for the Cobblr Edge Helper desktop app: GET /api/v1/desktop/updates/:target/:arch/:version answers the Tauri updater (204 when current, else the signed download), and super-admins publish a release via POST /api/v1/desktop/updates/publish. Lets the (private) helper auto-update through the hosted instance without public GitHub releases.
- Elegoo printers, no middleman. A new built-in `elegoo_sdcp` driver speaks the Centauri Carbon's own SDCP protocol directly on your LAN: discovery, status, upload, print start/pause/stop, with no manager software in between. Point a connection at the printer's IP and it joins the fleet, pools, and production runs like everything else. *Hardware-unverified first release, written to the SDCP spec and field-observed firmware behavior; tell us what your printer does.*
- Production runs: tell the farm *"make 250 of these, stop when done."* Pick a pool, a plate file, and a target quantity (with parts-per-plate); Cobblr queues exactly the plates needed, drips them onto free printers, and replaces failures automatically. A plate only counts when you clear the bed with a good verdict: scrapped plates are reprinted without you thinking about it. The run closes itself at target and can be paused, resumed, or cancelled mid-flight from the Digital Fabrication floor.
- The print-history panel is now a proper analytics view: a prints-per-day trend (completed vs failed, stacked, over 7/30/90 days. Hover a day for its counts and filament) sits above the success-rate / filament / hours tiles, and the By-printer rollup now shows each machine's success rate % so a problem printer stands out at a glance.
- Completely sidebar. Sidebar mode grows up: hide the top bar entirely (the panel icon in the sidebar's top-right corner) and the sidebar becomes the whole chrome: brand + workspace at its head, your nav in the middle, and a labeled action list at its foot (Build · Scan · Search · Notifications · Ask Cobb · Profile · Configuration) with your account row and its compact menu flying out beside the sidebar. Nothing in the sidebar opens a floating dropdown: the workspace switcher accordions in place, and you can drag nav rows to reorder them right there (same per-device order the top bar uses, the two always agree). Theme, pin/auto-hide, and top-bar toggles are small icons at the sidebar's top right; the verify-email nudge docks as a compact card instead of a bar over your pages; Calendar rides with the quieter surfaces in the nav. Everything is reversible from the same corner it came from.
- Your nav, your way: top bar or left sidebar. A busy workspace runs the top navbar out of room fast, so, Vivaldi-style, the tiny panel icon left of the cobblr wordmark now flips navigation into a skinny left sidebar (and back). In sidebar mode every dropdown child, specialisations, instances, grouped categories, is simply *there*, indented in the hierarchy, no hover menus and no "more ▾" fold. The sidebar can be pinned (a fixed column) or auto-hiding (a thin left-edge strip that slides the panel over the page when you reach for it), flip that with the pin at the sidebar's foot. Per-device preference; mobile keeps its menu either way.
- A design can now lift its finished-object photo straight out of its pattern PDF: one tap on "pull photo from PDF" in the pattern & photos panel. Cobblr reads the images embedded in the PDF (crochet patterns, manuals, spec sheets all carry them), picks the real photo over the diagrams, and attaches it: no separate upload needed.
- Emailed invoices now unpack themselves: attach a PDF/CSV invoice (or a photo) to an email sent to your workspace inbound address and each line item arrives as its own confirm-ready row in the scan inbox: grouped so one tap turns the lot into a single purchase order. Multi-item email bodies ("1x Pico W, 2x 10k resistor") split the same way, and an email that can't be parsed always lands as a note, never lost.
- The scan inbox can now import a whole batch from a file: companion app inbox exports (JSON or CSV) work as-is: 497 real items in one go, photos fetched, duplicates skipped on re-import, and any other system's CSV works with a quick column mapping in the preview. Imported items arrive as ordinary pending scans with the source's suggestions riding along as hints, so Cobblr's own matching still decides where things go.
- Scan a stack of books (or movies, albums) from the same series and the inbox now recognises it: the photo identify names the series a titled work belongs to (Harry Potter, Little House on the Prairie), and when two or more pending items share a series, a one-tap banner offers to tag them all with it: the tag rides to each item's entity when you confirm, so the whole set is grouped under its series regardless of where each lands.
- After a scan session, the inbox now spots a shared theme and offers to act on it in one tap: tag every item with a derived label (scan a pile of aviation gear → "tag them all 'Aviation'") and suggest a category for the non-book items ("Aviation Accessories"), all derived on the fly. Nothing pre-baked. The tag rides along and attaches to each item's entity when you confirm it, so books land on the Bookshelf and accessories in inventory, but all carry the same tag.
- Dark mode follows *you*, everywhere. Your light/dark choice is now saved to your account, so it carries across all your workspaces and every device you sign in on: flip it once and it sticks. It's purely how *you* view Cobblr: it never changes another member's view, and a workspace that wears its own branded theme still shows that theme to everyone (your preference just governs the normal, unskinned workspaces). Toggle it from the same places as before (the sidebar's theme icon, or Dark mode in your account menu). A device you haven't set still follows its own OS setting until you choose.

### Improvements
- The Actions page stops burying its lede. Tunable actions come first: and there are more of them: "Run a device command" is now trait-scoped to physical things (it no longer offers itself on subscriptions and tasks), joining Print label as per-workspace adjustable. Everything below sits under an honest "fixed scope" heading where each row says *where* its scope comes from: a module's precise kind list, or a deliberately-universal wire-driven shape, instead of a shrug.
- The "Build it for me" plan now shows everything it's about to do. When the AI creates a new tracked section (like a "Bookshelf") or groups your nav under a parent (like "Machines"), those now appear in the "Here's what it'll do" preview: the new section with its fields, and the nav grouping, instead of only "Will enable: …". No more applying a change you couldn't fully see.
- The dashboard tells the truth and wastes nothing. Overdue tasks now lead the "needs you" feed (they were invisible: the feed only read tracker date fields, never the projects module's own due dates). Activity bursts stop lying: "created K1 Max ×2" for two *different* machines now reads "created 2 machines: K1 Max, Voron 2.4, Beta". Enabled-but-empty modules collapse from full-size zero tiles into one quiet "Also enabled: …" line. The scan entry shrinks to a single row, and the "add more" bar moves below your data, prime rows belong to what's actually happening in your workspace.
- Act where you see it. The "needs you" rows now carry their actions: overdue tasks show inline checkboxes right on the row (≤3 items: check them off without opening anything), a printer holding a finished plate shows Good / Scrapped verdict buttons in place, and rows with more items expand inline to a per-item list. Printers waiting for a bed-clear verdict and failed prints now appear in the feed at all. At-a-glance tiles slimmed to stat-chips (same info, half the height), an empty *your views* section now invites you to pin a saved view (dismissible), toast bursts cap at 4 with a "+N earlier" pill, and the account menu gains Pair phone. The standing scan card is gone from the dashboard, scanning lives in the sidebar, the header, your paired phone, and any barcode reader (which works from every screen).
- "Camera unreachable" now tells you *why*. When a camera tile can't get a frame, Cobblr cross-checks the live bridge status and the printer's own reported state before pointing a finger: bridge offline ("nothing on-site can be reached"), printer unreachable ("the bridge is connected, so it's probably the printer: powered off, maybe on purpose, or unplugged"), or camera unreachable ("the printer is responding and the bridge is fine, check its camera / LAN-access settings"). No more blanket "(bridge?)" when the bridge is provably fine.
- The library reads your slicer's mind. Uploading a plate file now parses the slicer's own comment headers (estimated time, material, layer height, filament grams) plus the `4x Name_…_PLA_…_1h30m` filename convention (which is what carries parts-per-plate). Library cards show material / time / parts-per-plate chips, and picking a file in a production run pre-fills parts-per-plate and shows the per-plate estimate. No more counting parts off the plate preview.
- The printer cockpit's file list now recognises what's on the card: an SD file that matches a print in your cloud history shows that print's proper model name and picture (the cryptic cache/ filename drops to the subtitle), recognised entries sort above the anonymous ones, and files the history can't name get their preview read straight out of the gcode/3MF the slicer wrote. No more wall of blank squares.
- Printer file rows now say where their knowledge comes from: an SD chip means the file is on the card and printable right now, a Cloud chip means the cloud print history knows it (name, picture, outcome): a matched entry wears both.
- The fleet floor now loads instantly: the server remembers each connection's last-known machines and shows them while it re-checks live (surviving restarts), so no more "checking your machines…" wait. The Cameras wall now shows a hybrid Bambu's own chamber camera automatically (no manual URL or relay needed), and Arrange, drag machines onto the floor grid, positions remembered, works from the Cameras view too.
- Arranging the fleet is now just dragging: no Arrange mode, no cell grid: grab any machine tile and the others shift to show where it lands, drop on the strip between rows to start a new row in place, and the layout is remembered for the whole workspace. Machines you haven't placed flow in their own trailing row, so a new printer never scrambles your floor.
- Sidebar mode never stacks two sidebars. With navigation in the left sidebar, opening Configuration used to add a *second* sidebar beside the first. Now the main sidebar simply becomes the configuration panel: hub link, settings search, and every grouped destination fold into it, with a quiet "← workspace" row to swap back, and the settings page gets the full width. Leave the configuration area (or flip back to the top bar) and everything returns to normal.
- Fields can now reference another record. A new `relation` field type stores a link to another entity (e.g. a location) and renders it as that record's name: the "link to another record" building block. Lists, tables, search, and the generic entity API all show the referenced name instead of an id. First used by the upcoming home-vs-current location tracking; a general-purpose picker in the field builder follows.
- Scan inbox cards now show the author (or director/artist) under a titled work's name instead of the publisher: "Little House in the Big Woods · Laura Ingalls Wilder" reads better than "· Scholastic". Normal products still show their brand.
- Bigger scan photos, and nothing cut off on mobile. Inbox cards now show the item photo at the full height of the card: a book cover or product shot reads at a glance instead of squinting at a thumbnail, with a consistent width so every card's text still lines up. And the expanded item detail no longer runs off the right edge of a phone screen: the photo/source/hint columns now wrap to fit any width.
- Better catalog-photo search in the scan inbox: the image search now sharpens a weak title with the author and a media word (so "Farmer Boy" searches "Farmer Boy Laura Ingalls Wilder book" and finds the real cover, not generic farm pictures), there's now an editable search box to type your own term, and a photographed item auto-suggests a clean catalog photo on identify (it used to only do that on a barcode mismatch).
- Series detection now works for barcode-scanned media too, not just photos: when a scanned book/movie/album's barcode resolves to a titled work in a known series (Harry Potter, Little House on the Prairie), the series is captured, so scanning a boxed set by barcode groups it under its series just like photographing the covers does.
- Scan inbox card subtitle now shows the author *and* the publisher for a titled work: "Little House in the Big Woods · Laura Ingalls Wilder · Scholastic", rather than only one of them.
- Settings links all behave the same way now. Modules, Members and + New thing open as pages inside the settings sidebar: same dialogs, same logic, no more overlay-vs-page lottery (old `?open=` links redirect). And Digital Fabrication in settings shows Setup only: connections, pools, print-update rules, with a one-click "Open the shop floor →" out to the operate surface where it belongs.
- Toasts land in the sidebar. In full-sidebar mode, message bubbles now appear in the sidebar's quiet middle: stacking upward from just above the action cluster, instead of the far bottom-right corner. Where you're already looking, never over your content. Every other layout keeps the classic bottom-right stack. (Dev builds: clicking the env chip fires a sample toast of each kind, one per click, the old companion app tradition.)
- Automations can now fire only when a field actually changes, not on every edit. A wire condition can compare two live values: e.g. `{{event.after.location_id}}` vs `{{event.before.location_id}}`, so "when status changes to Done, notify" runs on the transition, not every time the row is saved while it merely *stays* Done. Entity updates now carry a before/after snapshot for this. Groundwork also lands for server-managed fields (values the server owns and stamps, which a client write can never overwrite), the foundation for upcoming home-vs-current location tracking.

### Fixes
- The scanner "waiting to file" tiles on the dashboard no longer crop the image: book covers (and other tall photos) showed with their tops and bottoms cut off; they now fit the whole picture inside the tile.
- Three settings paper-cuts, cut. Files shows actual thumbnails now (images were fetched without credentials and every one rendered broken; opening a file is fixed the same way). A bundled custom field tells you which bundle owns it and links straight to managing that bundle, instead of the dead-end "uninstall the parent bundle to remove". And the settings search moved into the sidebar: filter every destination from any settings page, Enter jumps to the first hit; no more detour through the Configuration hub.
- Dragging a machine tile no longer bounces: the tile you grab hides in place and a dashed slot shows exactly where it will land, the other tiles shift once and stay put, and the "new row" strips between rows now actually take the drop (the hovered strip swells into a full slot and holds steady under the cursor).
- The fleet floor now appears the instant the page loads (it paints from the last-seen snapshot while the live check runs), and a machine's card reads the same in every filter: narrowing to "Needs you" no longer swapped a printer's name with its connection label.
- Print-update rule editor: the chamber-light step now reads "Light on" / "Light off" so a narrow phone dropdown can't hide which one you picked, and the params hint now says what actually happens: a line is shown only if at least one of its {{fields}} has a value (an all-empty line is hidden), instead of the misleading "a line with a missing param is dropped".
- Scan faster: the camera stops flickering and the confirmation gets out of your way. After each photo, the "saved: identifying it" note now appears briefly at the *top* of the viewfinder instead of a toast at the bottom that covered the shutter and UPC field, so you can fire off a series of shots back-to-back. And the camera preview no longer blinks/resets when that note comes and goes (a toast was quietly restarting the video stream). The on-screen hint also reminds you that you can snap a photo, not just scan a barcode.
- The scan inbox shows every session, not just your last one. Finishing a scan now opens the full inbox with each scanning session as its own collapsible section (newest on top), scrolled to the one you just finished, instead of scoping the whole inbox to that single session and hiding everything scanned earlier behind a filter chip. Scan six things across three trips to the shelf and all three sessions are right there, grouped, the way you'd expect.
- Three scan-inbox triage fixes: an item the AI routed to a generic home inventory no longer lands on the wrong specialised form (a radio bag was defaulting to the Bookshelf's book fields); a research hint on a photo ("it's actually an RA200 headset") now re-identifies the item: the words override the picture, and "This is wrong" drops a stale hand-picked photo so the re-read isn't fighting it; and books from a series are no longer flagged as duplicates of each other (the combine offer now skips titled works and, when it does fire, names every item it's asking about).
- The settings sidebar no longer abandons you. Views, Tags, Custom fields, Files, Wires, Actions, Activity and Bundles: the settings-family pages that live at their own URLs, now render inside the same sidebar as every /configuration page, with your spot highlighted. Same links, same pages; the map just stops disappearing.

## 2026-07-02

### Features
- Local AI endpoints can now ride your edge bridge: the Ollama and OpenAI-compatible (LM Studio, vLLM, …) providers have a "How Cobblr reaches it" choice: direct, or via my edge bridge when the endpoint runs on your own machine/LAN. The form shows live bridge status while you pick, so "will my local URL work?" answers itself.
- The dashboard now opens with a "needs you" feed: things that are low on stock, dates that are overdue or coming up in the next 30 days (renewals, refills, returns, maintenance: read from your trackers' own date fields, whatever you named them), and captures waiting to be filed. Each line is one tap to act; when nothing needs you, it shows nothing.
- The App Builder learned "now change it". After a build, a new Refine box under Apply takes a plain-English change (*"also add a price column"*, *"drop the label wire"*) and revises the current result instead of making you re-describe everything or hand-edit JSON. Everything you didn't mention is preserved verbatim, the same validation gate applies, and every refine is kept as a linked draft so you can trace how the design evolved. Works with hosted AI or the copy-paste prompt flow.
- The AI builder got real about two things it used to wrongly refuse: "track Books, call it Bookshelf" now creates a proper Bookshelf: its own nav entry and page (a named instance), not book fields bolted onto Assets, and "put these under a Machines parent" now works, because bundles can declare nav headings (a parent nav entry with module/instance members, including ones that already exist). The Build page also spots when a ready-made featured bundle already covers your ask and offers to install that instead of generating a worse one.
- Bundles got an undo. Updating or uninstalling no longer discards the old version: the bundle's detail modal now shows a version history with one-tap Restore (validated before anything changes, and the restore itself is undoable). Updates also show exactly what changes: which fields, wires and views are added, changed or removed, instead of asking you to trust a version number.
- Press ⌘K (or Ctrl-K) anywhere to do or find anything: scan, add a thing, open the inbox, describe a setup, customize the workspace, or type two letters and jump straight to any item you own. The bundles page is now titled "Setups & trackers", matching how the homepage talks about them.
- Stock that reorders itself. When a part drops below its minimum, it now lands on a draft purchase order for its usual vendor at its usual quantity: both learned from the part's own purchase history, zero setup. You review, approve, and the arrival restocks (re-arming the loop). No duplicate drafts while a reorder is in flight; several low parts for one vendor collect on a single draft. Also available as a "Draft a purchase order" button on any part, and it's a plain wire on /bindings: edit or remove it like any other.
- Installing an edge bridge no longer needs any Docker registry: the generated command runs a stock public `node:22-alpine` image that fetches the bridge code straight from your own Cobblr (sha-verified) and keeps itself updated automatically: anyone can finish the install, on any network.
- Edge bridges get a home. Configuration → Edge bridges shows every on-site bridge in one place: which relay agents are connected right now (last poll, in-flight work), every machine manager running through a bridge with its health and a Test-now button, and how to set a new one up: direct-LAN or cloud relay. And search now finds Cobblr itself: type "edge bridge" (or scan, build, calendar, backup, tokens…) into ⌘K or the header search and jump straight to the feature, not just your data.
- Your app, one button, no AI. On Build → "A page for members", *Generate from my workspace* instantly assembles a member-facing app from the trackers you already have: a capture page (scan + add) plus a page per tracker with its saved views and an add form. Regenerate any time your trackers change; then describe tweaks on the same tab if you want the AI to take it further.
- On phones, a bottom action bar puts the three things you actually do on mobile under your thumb: Scan (straight to the camera), Add (a two-tap "write it down, we'll find it a home" sheet that works from any page), and Search. Desktop is unchanged.
- Bring your own AI from anywhere that speaks the OpenAI v1 API: a new OpenAI-compatible provider takes a base URL (and an optional key): point it at LM Studio, vLLM, llama.cpp, LocalAI, or a gateway like OpenRouter/Groq, and every AI feature (matching, vision, summaries, embeddings) runs through it. LM Studio users: paste `http://<your-ip>:1234/v1`, no key needed.
- You can now send someone a premade workspace: when minting a friend invite, tick "send them a premade workspace" and their brand-new workspace arrives already set up with a copy of your chosen workspace's configuration (modules, trackers, fields, views: no data). The join page tells them it's coming pre-configured. And if a friend sends you an exported blueprint file, "New workspace" now takes it directly, pick the file, and the workspace is built from it.
- The thesis dashboard got a face. The operator console's new Product Metrics section renders walls-hit per workspace (what was denied, what failed validation, which wires broke: 7-day and 30-day) plus time to first working app, sorted by who's hitting the most friction. Same numbers as `GET /super-admin/product-metrics`, no curl required.
- Cobblr now measures its own thesis. Every time someone hits a wall: a permission they don't have, an AI-built or pasted bundle that fails validation, a wire that errors or cycles, it's counted per workspace, and GET /super-admin/product-metrics rolls it up: walls-hit this week/month plus time-to-first-working-app (signup → first real item) for every workspace. Telemetry is sparse, best-effort, and self-pruning after ~180 days; it never slows a request.
- Emails can now land in Cobblr directly: forward an order confirmation or shipping notice to a workspace inbound address and its contents arrive as a routed, confirm-ready item in the scan inbox: identified and location-suggested like any other capture, instead of being read out in a chat channel. Set it up by minting an "email" inbound token; wire a mailbox to it with a Cloudflare Email Worker (docs/operations/email-inbound-capture.md).
- Unbox → identify → put-away: once a scanned or captured item is identified, Cobblr now suggests where to put it, from where similar items already live ("Bin 4: 3 similar items are stored here"), with a one-tap "Put here" in the scan inbox. It's a hint drawn from your own placements, never applied without you accepting it, and it stays out of the way when you've already filed the item.
- Scan a bin's QR label and: when that bin holds exactly one SKU (a bin of M3 screws, a drawer of GT2 pulleys), go straight to adjusting its count: +1/+5/+10 and −1/−5/−10 chips, a "set exact count" recount, and undo. The count shown is that bin's row only. Works from the phone scanner, a desktop wedge gun, and the bin's own page. Derived automatically: a bin with a second SKU keeps the normal filing flow, and an "Add something else to this bin" chip hands off to filing when a bin is gaining one.
- Wires can be picky now. Every wire gets an optional "only when…": plain conditions like `event.newQty ≤ 5` or `material = PLA`, joined with AND, editable in the wire composer. The event still fires; the wire just declines politely unless all its conditions hold. Bundles can ship conditions too, and a typo'd condition is rejected at save instead of installing a wire that silently never runs.

### Improvements
- Two settings families stop pretending to be strangers. Access & permissions is now one area: overview & grants, custom roles, and accounts share a tab strip and a single Configuration entry (search "roles", "accounts" or "password reset" and land in the right place). Notifications gets the same treatment under your account: inbox · delivery · channels, one strip, one click between them. Every old URL still works.
- Permissions now opens with a "who can do what" overview: every member on one line: stock role, custom roles, direct grants (or "everything, implicitly" / "read-only"), with links to where each piece is managed. Access control still lives in its four places; now there's one place that reads them together.
- The operator console's 18 tabs now read as five labeled clusters (workspaces · community · platform · ai & scan · ops) instead of one long undifferentiated strip. Same deep links, same mobile scroll row; your eyes just get waypoints.
- Bambu printers on LAN now list the files already on their SD card in the cockpit's "Files" panel, so you can start (or re-run) any of them with one click: no re-upload. Previously the panel was always empty for Bambu because file operations went through the cloud driver, which can't touch the SD card; they now route over your LAN bridge like the camera and controls do.
- The Digital Fabrication page reads like a farm now: the Fleet section owns its space from first paint (a skeleton instead of popping in seconds later), machines sort working-first with a status color band on every card and a stop button next to pause on running prints, and the printer cockpit's files + recent prints merged into one "On this printer" surface: SD files show when they last printed, and prints whose file is gone get a history row with Print again.
- The fleet view now works like an operator's tool: the summary counts became clickable buckets (All / Working / Needs you / Idle / Off), tiles say why a machine is blocked and what it will print next, running tiles show time-remaining and a "~done" clock, a Compact mode fits several times more machines per screen, you can select machines and pause/resume/stop/clear-bed them in one batch, and a library file can be dragged straight onto a machine tile to print it there.
- The fleet can now mirror your actual shop: hit "Arrange" and drag machines onto floor-grid cells: the layout persists, unplaced machines sit on a shelf below, and the floor stays put for everyone who opens the page.
- The Digital Fabrication page is no longer one long stack: it now splits into a Floor tab (the fleet, print queue, file library, and history: what you touch daily) and a Setup tab (connections, pools, and print-update rules, the set-once plumbing). The floor is the page; the config is one click away.
- Print jobs got much harder to lose: a cancelled job stays cancelled (the next status poll can no longer resurrect it), a scrapped/failed/cancelled job now puts its build's materials back into inventory and removes the never-made output, real farms tolerate ~3 minutes of manager downtime before declaring a print failed (and auto-retries wait for you to clear the bed), failed or cancelled jobs can be retried in place, and deleting a connection cleanly cancels and unlinks everything that depended on it.
- Recent prints are now a first-class, full-width section at the bottom of the printer cockpit, and every print that ran as a Cobblr job has a one-click "Print again" that clones the original (same file, routing, material draw and build link) and sends it. Also fixed a quiet hole where a printer's history could show empty because it was matched by display name instead of device identity.
- The printer detail modal is now a proper "cockpit": a wide, two-column dashboard (camera + live temps + filament on the left, controls + files on the right) that fits on one screen with no scrolling, and the motion controls are a real directional jog pad: X/Y as a cross with Home in the middle, a dedicated Z column, and a 1/10/100mm step selector, instead of a flat row of buttons.
- The Digital Fabrication queue is easier to work: click any job to open a detail view with its full error, timeline and the build/material it draws; retry a failed or cancelled job in one click; filter the queue by Active / Failed / Done; edit a connection (rename, move host, rotate its key, pause it) instead of delete-and-recreate; and the page finally fits a phone screen. A fresh workspace now sees a clear "Enable Digital Fabrication" prompt instead of a dead page.
- Two new ways to see the farm: the print queue gained a Timeline view: one row per machine showing what's running (with its finish clock) and the jobs lined up behind it in the order they'll actually run, and the Fleet section gained a Cameras view, a webcam wall where every machine's feed is the card with status and progress as an overlay.
- The homepage funnel now speaks in things you'd actually say. Column 1 lists only kinds of things you track: Labels and Digital Fabrication moved to a small "Add a capability" strip (they act *on* your things; nobody "tracks labels"). Column 2 leads with pre-shaped trackers: one-kind setups like the new Fasteners tracker (thread, length, head, drive as real columns, grouped by thread), with the big multi-module setups tucked under their own "Full setups" heading instead of being the first thing a new user has to decode.
- Scanner group cards breathe better: the photo strip now spans the full card width, and the "Create … & file all N" action is a compact button up on the title row instead of a large block beside the images.
- No-AI matching now understands everyday household things: bedsheets, towels, bedding and furniture route to Home Inventory; detergent, batteries, toilet paper and their kin to Household Supplies; clothing to Wardrobe; pet food, vitamins and fertilizer to their trackers. Under the hood, trackers carry a small taxonomy vocabulary, a keyword that names what the item IS (its head noun) counts as a confident match on its own, and multi-word keywords only match as full phrases, so "paper towel" can't claim your bath towels and a ribbon cable still isn't yarn.
- The tenant-credentials encryption key can be rotated now. `api/scripts/rotate-tenant-creds-key.ts` re-encrypts every workspace's stored DB credentials to a new key: dry-run first, every row verified before anything is written, all-or-nothing, safe to re-run. "Never lose the key" is still good advice, but it's no longer the entire disaster plan.
- A disaster-path restore that doesn't need Cobblr running. If the api won't boot, `api/scripts/restore-backup-offline.ts` loads a backup's data straight into the tenant database with plain Postgres access: dry-run preflight, ids preserved, type-aware, all-or-nothing. The in-app restore remains the full-fidelity path; this is for the day that path isn't available.
- The Refine box now works for apps too, not just bundles. Built a member page and want one more block, a friendlier intro, or a tweak to the custom code? Describe the change under "Create app" and the builder revises the current app: pages and blocks you didn't mention round-trip untouched, same validation gate.
- Three operational safety nets for self-hosters. Downgrade detection: booting an older image against a newer database now logs a loud, named warning instead of silently half-working. Restore preflight: send a backup with `dry_run=true` and get a full report of what a restore would do: rows, tables, files, whether existing data would be replaced, with nothing changed. Audit-log retention: an optional `ACTIVITY_LOG_RETENTION_DAYS` bounds the activity log on public hosts (unset = keep everything, as before).
- The dashboard's scanner groups now show the items themselves: a single row of photo tiles per group (the photo is how you recognize a scan), with a "+N" tile into the Scan Inbox when there are more than twelve. Names alone weren't cutting it.
- The dashboard's scanner queue now consolidates by what things ARE: "20 look like Filament Stash → Create Filament Stash & file all 20": one card per match, biggest group first, with samples. Matches to trackers you already have get "Add all N to …"; anything unidentified collapses to one review link. Twenty scans, one button.
- Scanned items and typed items now live where each belongs: things you type into the homepage stay in its third column as part of the guided flow, while things arriving from your scanner or phone get their own "from your scanner" queue below the panel: a compact preview of the first 8 with one-tap filing, plus a link into the full Scan Inbox for batch review. The "needs you" feed no longer double-counts waiting captures, and a pile of scans no longer forces the guided panel open.
- The homepage columns now use all the height the page gives them: when the right column grows, the kinds and setups lists grow to match instead of cutting off early. And the "↓ more" scroll hint is accurate now: it only appears when something is actually hidden, and it disappears the moment you reach the bottom.
- Settings you can actually navigate. Every Configuration page now has a persistent grouped sidebar: your current page highlighted, every other setting one click away, instead of bouncing back through the hub each time. The hub itself regrouped into six plain-language sections (set up · your data · people & sharing · automation & AI · machines & devices · safety & system), search understands synonyms ("roles" finds Permissions), the Modules/Members dialogs are deep-linkable, and a signpost points to your *personal* settings so workspace-vs-you is no longer a guessing game.
- Custom app blocks get a live preview in the editor. Toggle it under the code box and your block renders in the exact sandbox members will see (real bridge, real data, scoped to your access) with a "refresh with current code" button while you iterate. No more save-and-pray into a red error box.
- Scanner-group photos now actually use the full card width: tiles grow to share the whole row (bigger pictures in big groups, sensibly capped in small ones) instead of huddling small on the left.
- Two reliability touches: scanner-group photo tiles now fall through their image sources (cached catalog file → catalog link → your own scan photo) instead of giving up at the first broken one, and an open tab now notices when a new version of Cobblr ships: a small "tap to refresh" pill appears instead of silently running stale code.

### Fixes
- "What's new" in the account menu actually goes somewhere now. Inside a workspace the link resolved to a route that didn't exist (the changelog page was only mounted on the signed-out router), so clicking it silently bounced you home. The workspace router now serves the changelog too: same feed, your workspace chrome kept.
- Wires can no longer loop forever. A wire whose action re-triggers its own event (say, "when stock changes, adjust stock") used to spin endlessly in the background; the engine now stops the chain after 8 hops and records a wire_depth_exceeded entry in the activity log so you can see exactly which wire cycled: normal multi-step wires are untouched.

## 2026-07-01

### Features
- Golden-pass polish from re-auditing the companion app scanner source: "Already tracked" matches now show where the item lives and never offer a move it's already had; fuzzy matches exclude entities carrying a different barcode; linking a barcode also files the entity into your active bin; the camera keeps a full undo history for moves ("Undo last N"); the inbox gets a search box (name/barcode/notes/brand/area); gallery triage auto-advances to the next item; split children and URL items carry provenance chips; the stale nudge tightens to 2 days; and an "empty box" item no longer mislocates the entity: the box's location is recorded as a note instead.
- The scan inbox reaches full companion app parity. Scanning something you already track shows an "Already tracked" banner: bump its quantity, move it to your active bin, link the barcode, or open it, and the camera's new Move mode relocates tracked items hands-free. Photos are first-class: multi-photo gallery, rotate, "retake for catalog" (a nice shot as the display image), and "split into items" turns one photo of several things into separate items. Plus gallery view, session select-all + merge, a stale-items filter, "looks fine" review dismissal, bulk "Add all to…", pack-size and box-state chips, an undo on the scanner, and a "fill photos" backfill.
- The scan inbox now shows every scanning burst as its own session: including old history from before sessions existed (clustered by time gap), and scans typed, wedge-gunned, or photo-uploaded on desktop now join sessions exactly like camera scans.

### Improvements
- The dashboard no longer opens with a bar restating your workspace name, URL slug, role, and a greeting: everything on it lived in the navbar already. Customize workspace moved into the workspace switcher menu (click your workspace name), bundle-update notices still appear right at the top when there's one to act on, and on workspaces shared with you a slim note says your role. Your actual content now starts one full card higher. Also fixed the Scan inbox toolbar overlapping itself at medium window widths.
- The homepage "just add it" box now actually finds homes for what you type, with or without AI. Typing "my passport", "Netflix subscription", "a monstera plant" or "spool of black PLA" suggests the right ready-made tracker (Important Documents, Subscriptions, Plant Care, Filament…), community setups like Lego and 3D Printer Parts are now suggestible too, and suggestions keep working after your first save (they used to silently stop once anything landed in Inventory). Saving an unmatched item now says where it went ("Saved to Inventory, it's in your navbar") instead of a vague "Filed it." On phones, the add-something box is now at the top of the panel instead of two screens down, and a few "try:" example chips demo the flow in one tap. Also: typing in the kinds column now ranks name matches first (typing "mach" + Enter picks Machines, not Assets), and the dashboard no longer fires a burst of failed background requests on every load.
- A friendlier first week: the homepage and the Build page now say up front when AI isn't connected (and what still works without it) instead of failing quietly, with a one-tap "Connect AI" path. "Describe what you have" now lands on the right Build tab, and the Build tabs speak plainly ("Design my workspace", "A page for members"). When AI is off, "Write a prompt instead" becomes the primary path. Creating a tracker from a capture now lands with a "here's what just happened" strip (scan more / add more) instead of a bare table, and captured names drop leading articles ("A spool of black PLA" files as "Black PLA"). The verify-email banner stays dismissed once dismissed (the reminder lives in your account menu), and young tables hide their all-empty columns behind "+N more columns" until you have real data.

## 2026-06-29

### Features
- Typing where something went now files it for you. A freeform capture like "Logic analyzer in cabinet 002" or "Dial calipers placed in Tool Chest 001" (typed in the scanner, or sent from Slack) now splits the item from the location, resolves the location against your workspace, and drops the item into that bin pre-filed. Zero-padded label-reel numbers match too ("bin 0087" → Bin 87). Ordinary names with an incidental "in" are left alone.
- Queue a fabrication job and it draws down the materials. A print or laser job can now be linked to a build (a bill-of-materials). When you send the job to the machine, Cobblr consumes that build's components from inventory and bumps the finished-product count, so "order in → subtract stock → queue the laser" happens in one step. Re-sending the same job never double-deducts.
- Link a fabrication job to a build, right in the New Job dialog. When you queue a print/laser job, you can now pick a Build it produces (and how many) from a dropdown: no API needed. On send, that build's components are drawn from inventory and the finished part is added, just like before. The field only appears once you've defined a build.

### Improvements
- On the new-workspace homepage, the things you add now show up right in the third column with a one-tap action, instead of dropping into a separate "scan inbox" with the next step stranded in a banner at the top. Each capture shows what it looks like and a button to act: Create [tracker] for a ready-made setup it matches, Add to [tracker] for one you already have, or Save it to file it as a general item. And even with no AI connected, the keyword matcher routes recognised things to a tracker and everything else still gets a home: no capture is left in limbo.

## 2026-06-28

### Features
- Scan with your phone from a computer. On a desktop (no camera), the Scan card now has a Pair phone button: it shows a QR; you scan it with your phone's camera, and your phone is signed in to the same workspace: its camera scans then flow straight into the inbox your computer is already showing. The pair code is single-use and expires in ~90 seconds; nothing to type, no separate login on the phone.
- The new-workspace homepage is now a guided "What do you want to do?" funnel that converges modules and ready-made bundles into one place. Pick a kind of thing to track, narrow to a ready-made setup, and the third column walks you the rest of the way, for kinds that hold several collections (Assets, Inventory, Machines, …) it helps you create a category ("Cars", "Printer Parts") and drop you in to add items; for a ready-made setup it installs it and takes you straight in. Examples now match what you picked (a Filament setup suggests "a spool of PLA", not "a pulley"), specialised machine setups (3D Printers, CNC, Lasers) show under both Machines and Digital Fabrication, and the freeform "just add it, we'll figure out the fields" path is always there, including Pair phone on a desktop, so you can scan straight into the workspace with your phone's camera.

## 2026-06-27

### Features
- Adding a category (+ New thing in workspace) now lets you put it straight into a navbar menu: so a home inventory's Pantry, Cleaning Supplies, Tools, Medications, … collapse into one tidy Inventory ▾ dropdown instead of sprawling across the top bar. It's automatic after the first: the first category pre-fills a menu named after the kind, and every category you add after auto-joins that same menu. Deleting a category cleanly removes it from its menu.
- A brand-new workspace now opens with "What do you want to do?": one distilling search over three converging lanes: ready-made setups (recipes), building blocks (capabilities), and build-it-yourself (describe/scan/AI). Type "parts" and all three narrow at once; pick a building block like Inventory and the recipes lane funnels to its ready-made versions (Home Inventory, Filament, …) with a "start blank" option. Modules and bundles finally converge on one on-ramp, so a new user can actually get started.

### Fixes
- The Spoolman import button (3D-printing filament weight sync) now only appears on a filament inventory, not on every list. It was just clutter on a food pantry, medications, or a generic parts inventory.
- A new workspace now lands at the bottom of your switcher. Creating a workspace used to drop it near the top; it now appends after your existing ones (still drag-reorderable).

## 2026-06-26

### Improvements
- The Labels "Add labels" browser now shows a tab for each collection you actually have: your own instances like *3D Printers*, *Laser Cutters*, *Parts*, instead of generic module names, and hides empty collections entirely. So you only see real, non-empty places to pull labels from, matching what's in your navbar.
- The Labels browser now shows your Locations tab as the real hierarchy: an Areas tree with rooms and their nested bins, and a separate Containers section for loose bins, exactly like the Locations page, instead of one flat list. Easier to find the right shelf or bin to label.
- Your custom location order now applies everywhere, not just on the Locations page. Drag locations into the order you want and that order is honoured in every location dropdown, the Labels browser, and any module that lists your places. The page also clearly separates Areas (rooms & regions) from Unsorted containers with their own headings.

### Fixes
- A barcode that resolves only to a generic category ("Whiskey", "Beverages" from Open Food Facts) now upgrades to the real product on re-run / "needs detail": e.g. Maker's Mark Bourbon Whisky. The web identify already found it; an over-strict same-word check ("Whiskey" vs Maker's "Whisky") was discarding the correct answer.
- Locations now sort the same way everywhere: "Bin 2" before "Bin 10", not lexically. The Locations page and the Labels browser now share ONE location tree viewer, so their structure and order can never drift apart again.
- A catalog image you hand-pick (the "other photo options" strip or "use my photo") is now sticky: a later re-run / hint correction no longer overwrites it with the provider's image. Reverting to the auto image releases the lock.

## 2026-06-25

### Features
- - Computed fields can now build clickable links: a new `{{ name | slug }}` template filter turns a value into a URL-safe slug, and computed fields can use the `url-link` renderer, so e.g. a "git repo" field with template https://github.com/me/{{ name | slug }} renders as a clickable link per entity.
- Labels page can now find things to label: a new Add labels panel under the queue, with a tab per kind that supports labels (Parts, Machines, Assets, …), each listing its items with thumbnails and a one-tap Add. Search within a tab to narrow it. The tabs aren't hardcoded: a kind shows up here for the same reason it gets a Print-label button, so enabling a new module with labelable items adds its tab automatically.
- Filter the machines/printers view by state. A "states" control lets you hide whatever machine states you don't want to see (e.g. shelved, sold): the states come from your data, and your choice persists per view.
- Prints started on the printer now show in the queue while running. A print you start directly on the machine (e.g. from Bambu Studio/Handy) (not sent from Cobblr) now appears in the Print Queue as a read-only "on printer" entry the moment it starts, instead of being invisible until it finishes.
- The phone scan card now has a "Not it: photograph it" action: when a barcode resolves to the wrong thing, or to a junk, non-product code nothing can fix, snap a photo of the item and AI identifies it from the package, overriding the bad barcode result. The corrected name appears on the card a few seconds later.

### Improvements
- - Navbar: pin individual entries to the “more ▾” overflow so they always fold there even when the bar has room: per-entry toggle in the navbar Customize menu (the ••• icon next to each entry). Keeps a busy header down to the few sections you use most; the rest still auto-fold responsively as before.
- - JSON editors now have a Copy button (next to Format): grab the whole block in one click. - Sync sources: re-syncing a live entity type now shows the same both-sides preview as the first import before it writes, so a mapping/JSON edit is reviewed (create/update/merge/delete) before any data changes, not applied blindly.
- - Sync manifests can declare cross-section references: a field (e.g. a printer location_id) that points at another section by external id, resolved through that section id-map to the mirrored Cobblr entity. So importing 3D printers links each to the location you already imported. Shown resolved in the preview.
- - Import name-merge is now case- and punctuation-insensitive, so near-duplicates link instead of duplicating: e.g. "Prusa Mini" merges with "Prusa MINI+". Truly different names (CR-10 vs CR-10S) still stay distinct, and every merge is shown in the both-sides preview before it is written.
- - Sync sources can pull images across: a section declares "images": { "image_path": "$.image_url" } and the engine fetches each image through the edge bridge, stores it in your file library, and points the record at it. Works for LAN sources (e.g. companion app printer photos) the cloud can’t reach directly.
- - Synced images that don't come across on the first try (e.g. before an edge bridge has self-updated to binary support) now self-heal: the record is re-pulled on the next reconcile until the image lands, instead of sticking with no photo.
- - Sync sections can filter source rows by a field, so one endpoint feeds several sections: e.g. companion app /printers returns printers + lasers + CNC, and "filter": { "from": "$.category", "equals": "printer" } imports only the 3D printers. Each category into its own section/instance.
- - Sync sources: each section now has its own "edit" action: tweak just that section's JSON (mapping, references, images, target instance) in a pre-filled editor, no need to re-paste the whole source.
- - Live Sync settings: a source and its connection are now ONE card instead of two stacked blocks that repeated the section list. The source defines its sections once (with edit/remove); each connection nests underneath carrying only its own sync state (live toggle · synced count · Preview & sync · webhook). Add a second connection and it just joins the same card: the manifest is still edited once above.
- - A sync section can target a module instance: add "targetInstance": "<slug>" (e.g. "3d-printers") and imported rows land under that nav entry instead of the generic base. Adding/changing it re-targets already-imported rows on the next sync.

### Fixes
- Fixed a printer modal flicker. A printer photo set via the web-image search could make the detail modal flash, from a doubled image URL (404 → retry). The file-URL helper is now idempotent so an already-resolved image path is never wrapped twice.
- On mobile, an inbox item's name no longer gets crushed to a single letter while the "AI reading…" badge hogs the row: the status badge now wraps below the name so the name keeps the full width.
- - Sync sources: one section can now fan a single endpoint out to several instances with "instanceBy": e.g. companion app /printers routes to 3d-printers + laser-cutters + cnc by category, instead of one near-duplicate section per category. - The section editor sanity-checks pasted JSON: structural problems block the save; an instance slug that does not match a real workspace instance warns with a "did you mean" suggestion (catches the targetInstance typo that silently shipped lasers nowhere).

## 2026-06-24

### Features
- The "same barcode: same item?" merge offer now fires on an exact match too (not just off-by-one), and when the two listings read differently it asks which one to keep, showing both names side by side. Either choice merges, sums the quantity, keeps the scanned barcode, and never loses the photo you took.
- - Declarative sync-source engine: external systems are mirrored into Cobblr via installable manifests (data), not source-specific code. Phase 1: the manifest schema + interpreter, proven to reproduce the companion app mapping exactly.
- - Declarative sync sources, Phase 2: install table + API (POST/GET/DELETE a SyncSourceManifest per workspace), org-aware connector resolution (built-in ∪ installed, mirroring digifab resolveDriver), and removal of the hardcoded companion app connector. A sync source is now installable data; Cobblr ships no source-specific code.
- Structured-JSON inputs now go through one shared, validated control instead of a dozen ad-hoc textareas. Wherever you paste a JSON blob: connector credentials, a vendor-resolver manifest, and (incrementally) view configs, app schemas, channel configs; you get live parse errors, "Format" pretty-printing, and inline JSON-Schema validation that points at exactly which field is wrong. Sync connections gained a "paste JSON →" toggle for credentials; the scan vendor-resolver editor now validates as you type.
- - Machines can now be a sync target: the module registers an entity writer (like locations), so a sync source can mirror external machines (e.g. companion app 3D printers) into machines:machine with name-merge, both-sides preview, and live updates.
- The phone scanner's result card now has a "Not right?" action, so you can re-check a wrong identification on the spot instead of waiting to fix it in the desktop inbox.
- When you photograph an item with no scanned barcode, the AI now reads the UPC/EAN off the package (when legible) and captures it into the item's barcode field, marked "(read from photo)" so it's clearly an OCR read rather than a hardware scan. This is the start of the "photograph it to recover the barcode" flow.
- When the barcode read off a photo is a near-match (within a digit or two) to a barcode you scanned earlier, the inbox now offers to merge them: "the barcode read from this photo is one or two digits off from <X> you scanned, same thing?". Merging keeps the photo you took and adopts the scanned (authoritative) barcode, summing the quantity.
- The scan inbox now offers to combine items that look like the same product: when you scan several of one thing but a pack carries a different barcode, a banner appears ("N items look like the same product: combine into one?"). One tap merges them into a single line with the summed quantity, keeping each distinct barcode in the item's data. Always opt-in (a different barcode can be a different size), and dismissable to keep them separate.
- The scan inbox no longer caps at a fixed number of items. It loads in pages and pulls the next batch as you scroll toward the bottom, with the true pending count in the header. And the "combine similar items" offers now appear inline, right above the items they apply to, instead of stacking at the top of the list.
- Sync connectors can now reach a local source over an edge bridge. When you add a connection you choose how Cobblr reaches it: Direct (the cloud fetches the URL: public URLs only on the hosted service) or Via edge bridge (a small agent on your own network fetches it and relays the result up). That's how you connect a LAN system like companion app to hosted Cobblr, the cloud never touches your private address; the bridge dials out and does the fetch locally.

### Improvements
- - Ask Cobb panel: opening it now shifts the centered page content left into its margin (on wide screens) so the panel and content coexist, instead of the panel overlaying the content.
- Outbound requests from connectors now route through one per-tenant egress policy. On the hosted service, a sync connector can reach public hosts and your own registered edge connector, but not arbitrary private/internal addresses (a tenant can no longer point a connection at a LAN or internal IP). Self-hosted instances are unchanged (your own LAN is allowed). To connect a *local* system (like companion app) to hosted Cobblr, use an edge connector rather than a direct LAN address.
- - Import preview now shows each row both-sides: click a row to see the source data coming over and, for a merge, the existing entity it links into with its current fields, so you can verify the match and what data transfers before approving.
- - Edge bridges: connections (sync + machines) now use a standardized bridge picker that lists your connected bridges with live online status and lets you select one, instead of a free-text field. New GET .../digifab/edge/bridges enumerates them.
- - Integrations → Live sync now has an in-app "Install a source" path: paste a sync-source manifest to install it (no curl), then add a connection through it. Installed sources are listed with an uninstall.
- - Sync sources are now a container + sections: expand an installed source to see its sections (entity types), "+ Add section" to sync another thing (paste just that section JSON, e.g. 3D printers), remove a section, or edit the whole container JSON. The source is defined once; you grow it over time.

### Fixes
- A research-hint correction on a barcode item now actually re-identifies it. Before, a hint like "it's 1 unit, not 96 packs" was stored but never re-ran the lookup when the provider already had a (wrong) name, so nothing changed. Now a hint re-derives the name via the web identify and accepts the corrected result even when it's shorter than the wrong one.
- Photo identification now works through the edge-bridge AI (the "Local AI / Claude bridge" path many hosted workspaces use). It was sending a bare "Describe this." prompt for image identification, so the model replied with prose the scanner couldn't read as a result: surfacing as "no vision provider configured." It now uses the same structured identify prompt the OpenAI/Anthropic providers use, so a photographed item gets identified.
- Photo identification now succeeds when the vision model returns a richer result shape. It was identifying items correctly (e.g. a screen-protector package as brand/product line/product name) but the scanner only read a strict "name" field, so a correct identification surfaced as "no vision provider configured." It now pulls a usable name from the richer shape too, and a re-run forces a fresh look (bypassing any result cached under an older prompt).
- Photo identification now works on workspaces whose AI is a personal connection. The detached "identify this photo" step ran with no caller, so it couldn't resolve the owner's connected AI and failed instantly with "no vision provider configured." It now routes through the user who triggered the scan/re-run, and a vision failure is logged instead of swallowed.
- The "combine similar items" offer now recognises more genuine matches: it anchors on the brand plus the shared product words rather than a strict overall name ratio, so "Charmin … Toilet Paper … Jumbo Roll" and "Charmin … Bath Tissue Jumbo Roll" (the same product under different wording) are now offered for combining.
- A barcode that can't be identified anywhere no longer shows a junk result: the web-search fallback used to accept an "Unknown Item" / "XXXXXXXX" placeholder as the name (and then image-search it). Those are now rejected: the item is left unnamed for a photo or manual entry, with no bogus name or photo.
- When a barcode provider returns a localized, non-English product name (e.g. go-upc handing back "Charmin Papel Higiénico Ultra Soft" for a US item), the scanner now automatically re-identifies it from the web, which prefers English, so you get the English name for the same product.
- Three scan-inbox fixes: the inbox now loads up to 500 pending items instead of silently capping at 100; an unidentified item ("Unknown Item") no longer pulls junk catalog photos (it searched the literal word "unknown"); and an unidentified item no longer pre-selects a commit target (e.g. "Inventory part"). You pick where it goes.

## 2026-06-23

### Features
- Duet printers get a full control panel. The Duet driver now exposes Home, a 3-axis Jog, and Nozzle/Bed set-temp (not just pause/resume/stop), so a Duet on the edge bridge has the same rich modal controls as a Bambu. Ships via the embedded bridge bundle; bridges self-update.
- Duet printers: full controls, your macros as buttons, and print-from-SD. The Duet modal now has live tuning (speed/flow), baby-step Z, chamber temp, part-fan % + LED toggle, a button for every macro on the printer (LED on/off, bed presets, etc.), and a Print button on each file in the list to start an on-SD file without re-uploading.
- Sort the printer file list + see upload dates. Each gcode file now shows when it was put on the printer, and the list can be sorted by Newest / Oldest / Name / Largest.
- Printer modal: live print progress + per-file estimates. When a printer is mid-print the modal now shows a progress bar with % / current layer / time-left / elapsed. In the file list, tap any file to see its slicer estimate (print time, filament length, layer count, height) fetched once and cached.
- Gcode previews in the printer file list. Files sliced with an embedded thumbnail (PrusaSlicer/Orca/SuperSlicer) now show that preview right in the list: same image DWC renders. Previews load lazily as you scroll and are cached, so the printer isn't hammered. Click one to enlarge.
- When a barcode resolves to a thin entry (a bare category like "Bourbon" from a crowdsourced food-facts mirror, missing the brand), the scan now uses web + AI to build the full product title ("Bulleit Bourbon Frontier Whiskey 750mL") and writes the richer result back to the shared Barcode Intelligence DB, so it supersedes the thin entry for every future scan. External databases are a starting point; we enrich our own on first encounter.
- Klipper printers get the full cockpit. Klipper/Moonraker machines now report live job progress, list their on-disk gcode files with slicer thumbnails and print-time/filament estimates, run from storage, and expose the complete control set: speed/flow, home/jog/baby-step, temps, part fan, and every one of your Klipper macros as a button.
- See the files already on a printer. The device modal now lists the gcode files on the printer's own storage (Duet via the edge bridge). It's cached (5-minute server cache, fetched once in the UI, never polled) with a manual Refresh: so a fairly-static list doesn't hammer the printer.
- Edit a Scan Rule as JSON. The rule edit/new modal gets a Form / JSON toggle: flip to JSON to tweak the raw rule and flip back, with the form and JSON kept in sync. Pairs with the Paste JSON button for create.
- QR labels can now encode a self-describing URL: `/qr/<kind>/<id>` instead of an opaque random token. The win: a scanned code stays meaningful even if this Cobblr instance is ever gone or moved (you can read what it points at straight off the URL, and it survives route changes), where an opaque token is a dead pointer without the database. It's the new default; flip a workspace to opaque (shorter, reveals nothing, scans better on tiny labels) on the QR codes page. Both styles resolve identically and anything you've already printed keeps working.
- New green "This is good: lock it in" button on a scanned barcode item: when the listing is correct, one tap verifies its name, brand, category and photo into the shared barcode database, so every future scan of that code, in any workspace, gets your clean entry instead of a thin crowdsourced one. It doesn't re-run the AI or commit to inventory; it just locks the data in.
- The scanned-item correction buttons are now a pair, half-width each: "Right: needs detail" (the product is correct but the listing is sparse, keep the identity, re-check every source + the web to fill in the proper name, size and photo) and "This is wrong" (re-derive the product from scratch). Both steer off your optional note and push the result back to the shared barcode database.
- The source-data panel on a scanned item now shows a short history of what you did to it: "Asked for more detail · 2 min ago", "Flagged wrong", "Locked into the barcode database", newest first, so you can see what's already been tried. The correction buttons are also reordered into traffic-light order: red This is wrong → yellow Right, needs detail → green This is good, lock it in.
- The scan card gets a proper image viewer: tap the catalog or your-photo image to view it full-size, "Revert to original" to undo a photo pick and restore the image the lookup first gave, and "Use my photo" to make your own scan the catalog image. The original is preserved server-side, so revert works even after a reload.
- Create a Scan Rule by pasting JSON. The External QR Rules page gets a Paste JSON button. Drop in a rule's `{name, match, resolve}` and it's created, instead of filling out the form field by field. Handy when someone hands you a ready-made rule.
- Scans now group into visible "sessions." Whatever you scan with a hardware scanner (or the camera) flows into one session until you idle for 30 minutes, then a fresh session starts. The inbox shows these as collapsible groups with a time, location, and count header ("Today 2:48 PM · Garage · 6 items"), so you can finally see how your scans are batched, and a "New session" button starts a fresh group on demand.
- New "This is wrong: re-check everything & fix it" button on a scanned item. One press re-queries every source (go-upc, upcitemdb, Open Facts) plus a web search, steered by your optional "what's wrong" note, distrusts the flagged listing, reconciles the correct name + photo, and pushes the correction back to the shared barcode database so the next scan (anyone's) is right. The source panel now also shows when the listing was last updated ("10 min ago"). "Re-run AI" is unchanged.
- You can now keep a live link to another system. Add a *sync connection* (the first is companion app), and its records mirror straight into your workspace: connect it once, and e.g. every room, rack, and bin you add in companion app shows up as a Cobblr location, hierarchy and all. It's a real connector, not a one-off: changes arrive instantly via webhook, with a periodic reconcile that backstops anything missed and removes things deleted at the source. Credentials are encrypted at rest, and the same machinery is built to host more connectors over time.
- Sync connectors now do a proper one-time import preview before they touch anything. Enabling an entity type (e.g. companion app locations) no longer writes straight to your workspace. You pull a dry-run plan that shows exactly what *would* happen (create / merge-into-existing / update / unchanged / remove), eyeball it, and approve. The first import even merges a source record into a same-name item you already have instead of duplicating it. Once you approve, that entity type switches to live sync (webhook + poll). Live pushes and the background poll stay withheld until the import is approved.
- Search the web for a printer's photo. The 3D-printer / machine modal now has a "search the web for a photo" strip. Pick a product shot and it becomes the printer's image. It's the same picker the scan inbox uses, now a shared component available anywhere an item needs a photo.

### Improvements
- Connecting a print manager from a printer is now one step. Opening "Connect one" from a printer jumps straight to the edge-bridge machine form (no type-picking), pre-fills the machine name from the printer, and pre-selects the driver from the printer's kind (Klipper/Prusa/Duet).
- Printer file lists & previews are now served from a durable backend cache. A background warmer keeps each printer's file list and gcode thumbnails fresh in the database, so opening a printer is instant and never pulls from the machine itself: the printer is touched only on the backend's schedule, and each thumbnail is fetched exactly once.
- Printer files & previews are cached harder. The file list is cached 15 min and each thumbnail 30 min, on both the server and in the browser, so reopening a printer is instant and the printer itself is touched at most once per window, never on every modal open.
- Printer/asset location pickers now show rooms in your order. The location dropdown for printers and machines lists only areas (rooms/zones, not bins) in your arranged order with natural sorting (Bin 1, 2, … 10, not 1, 10, 2). And you can connect a print manager after the fact: the printer's Print Manager panel now has an inline "Connect one" / "+ new" that sets up a manager without leaving the page.
- The top navigation bar now spans the full width of your window instead of being boxed into a centred ~1150px column, so the module nav gets the whole row before links fold into the "more ▾" menu. Page content (tables, forms, lists) stays centred at a comfortable reading width.

### Fixes
- Newly-connected print manager auto-selects. After "+ new" creates a manager from a printer, it's now selected in the dropdown (with its printer auto-picked) instead of dropping back to "choose…", so you can Link immediately.
- Duet printers report live temps. The edge-bridge Duet driver now reads bed/chamber/nozzle temperatures (from `rr_status?type=2`), so a Duet on the local bridge shows live telemetry like the others. Ships via the embedded bridge bundle: bridges self-update automatically.
- Live temps in the printer modal for edge-bridge machines. The device-detail telemetry was Bambu-cloud only, so a Duet / PrusaLink / Moonraker printer connected through the local edge bridge showed no live telemetry in its modal. It now pulls live temps + state from the bridge's device list when there's no Bambu cloud report.
- Thin-hit enrichment now captures the spec, not fluff: when a barcode resolves to a bare name and we enrich from the web, the result keeps the package size / quantity (1.75 L, 750 mL, 12 ct, proof) that identifies the SKU, and only replaces the thin name when it actually adds the size or the brand: never just longer marketing wording.
- Scan catalog images are far more accurate for store/own-brand products. The image search now includes the brand in the query (not just the ranking), so a generic name like "Blended Scotch Whiskey" + brand "Kirkland Signature" no longer comes back as a random Johnnie Walker bottle. The brand is skipped when the title already contains it, so the query is never duplicated.
- Scan matchmaker suggestions come back much faster. The AI now only writes an explanatory note when something actually needed reconciling (a title-vs-photo disagreement, an inferred field, an unconfirmed pack count) and omits it for a clean match, instead of always composing a 2–4 sentence paragraph. Typical routing dropped from ~18s to ~5s with no loss of useful explanations.
- Scan cards no longer show a broken-image icon when a catalog photo URL is dead. If the main catalog image fails to load it falls back to the server-cached copy or your own photo; and any "other photo options" thumbnail that won't load is dropped from the strip instead of showing a broken tile.
- Re-scanning a barcode now always merges into the one pending item you already have, no matter which scanning session you're in, and moves it into the current session. Previously dedup was scoped per session, so re-scanning the same code in a new session created a duplicate (and the original looked like it had gone missing).
- The scan inbox now groups un-batched items by day (Today / Yesterday / date) instead of lumping them all under one "No session" header. Explicit scanner sessions still show as their own timed group; everything else reads as coherent day buckets, newest first, so the list makes sense even for items scanned before sessions existed.
- The scan item's research-hint box is now a full-width, multi-line field so its placeholder is no longer cut off (Enter submits, Shift+Enter adds a newline). And the enlarged-image viewer now shows the "other photo options" strip along the bottom, so you can swap to a different photo without leaving the zoom: picking one swaps the big image in place.
- For items identified by web search (e.g. books), the cleaned-up name the AI describes is now actually used as the item name, so the note ("publisher and author stripped from name") matches what you see, instead of the header still showing the raw "Publisher Title (Author)". Sizes/specs are never dropped, and database-identified products are unaffected.
- Scan names now lead with the brand when a sparse source name leaves it off: "Black Label No.7" (Jack Daniel's) becomes "Jack Daniel's Black Label No.7". Names that already include the brand are left as-is.
- Re-scanning a barcode that's already in your inbox, or restoring one from the deleted section, now brings it back to the TOP of the queue instead of leaving it buried at its original position. Previously a re-scan or restore updated the item in place far down the list, so it looked like the scan did nothing and the item had gone missing.
- The scan card's "Source data" panel (provenance + the fields it routed to) is now expanded by default instead of collapsed.
- When a barcode isn't in any database and we fall back to a web search, the AI now picks the product that most search results agree on, instead of occasionally latching onto a single obscure or accessory listing ("Customers also bought…", "Related…"). Pairs with the earlier fix that feeds it real product-page titles.
- Scan items can no longer get stuck spinning on "finding the best table…" forever. If the matchmaker step ever failed (e.g. a slow/timed-out AI call) it left no completion stamp, so the card spun indefinitely with no way to recover. Now the matchmaker records completion even on failure, and the card gives up the pulse after a few minutes, showing the identified item so you can route it by hand.

## 2026-06-22

### Features
- Import & export locations as CSV. On the Locations page, Export downloads your whole location tree, and Import lets you paste/upload a CSV and preview the exact create/update/skip diff (and any unresolved parents) before anything writes. Generic: pick which column matches existing locations, set parent by that same column, and any extra columns are kept on the location's metadata, so you can round-trip another system's ids.
- Locations now has a navbar entry. Previously the rooms/places tree was only reachable under Configuration; it's a top-level Locations item now: per-device show/hide/reorder like any other nav entry, so the place tree is one click away.
- Order your locations by hand. Each location now has up/down arrows to set the display order within its group (rooms in the order you want, rather than alphabetical). The order persists and applies everywhere the tree renders; un-ordered groups stay alphabetical.
- Rename the Locations nav entry to your word. The top-level Locations entry now honors the org-wide display-name override (Configuration → Presentation): call it "Rooms", "Areas", whatever fits your workspace, consistent on every device.
- Locations: unsorted containers get their own section + natural sort. Loose containers (bins not placed in any room/area) now sit in a separate "Unsorted containers" section at the bottom, so the room/area tree stays clean. Names also sort naturally: Bin 1, Bin 2, … Bin 10, Bin 11 (not Bin 1, Bin 10, Bin 2).
- Select-all + bulk delete on Locations. A Select all button (and a Delete action in the selection bar) lets you clear out locations in one pass: handy for re-importing a corrected CSV. Deleting a parent cascades to its children.
- When a scanned barcode finds nothing in the catalogs or on the web but you snapped a photo, the item is now named from the photo instead of being left blank. The AI already reads the photo to describe and sort the item. Now it turns that read into an actual title (e.g. a museum-gift souvenir box, a foreign-language book edition the catalogs miss), so you get a usable name to confirm rather than an empty "name required" field.
- When you add a photo to a scan whose barcode came back wrong, the photo now wins. A barcode/web search can return a spurious listing (a debug probe coming back as a "power supply"); if your photo unambiguously shows a different product, the item is now renamed to what the photo shows: the old wrong title is dropped, the photo-derived details become the ones to work from, and the corrected barcode→name is reported back to the shared barcode database so the next scan is right. (Previously this check only ran for direct barcode hits, not web-search results, so a wrong web-search title would stick.)
- Book and music scans now resolve from the local mirrors. A scanned book's ISBN-13 is an EAN barcode, so it's now looked up through the shared barcode database (which now holds the Open Library catalog): instant, offline, no per-scan call to Open Library's API; a brand-new book not yet mirrored still falls back to the live API. CDs/vinyl already went through that path, so they pick up the new MusicBrainz music data automatically. Provenance reads "Open Library" / "MusicBrainz".
- Bulk-triage gained Set location: tick a batch of scans and file them all into one place in a single step (it appears in the selection toolbar next to Confirm/Discard whenever your workspace has locations). Pairs with the filing context: scan a pile anywhere, select them, and drop them into "Garage Shelf 3" at once.
- Bulk-triage the inbox. Tick a few scans (or "select all") and a toolbar lets you Confirm or Discard the whole selection at once. No more one-by-one. Confirm sends each item to its own best table match with the fields the matchmaker already extracted; anything without a confident match is left in place for a quick manual look, and the summary tells you how many landed. Turns a 30-scan session into one tap to file.
- New URLs intake on the scan page: paste a list of product links (one per line, up to 50) and each becomes an inbox item, identified in the background through the same web-lookup the camera uses for a scanned URL. Handy for cataloging an online order or a wishlist when there's no barcode to scan. Drop the links in, and they fill in names/photos/categories on their own.
- Scanning now has a filing location ("active bin"). Set "Filing into → {location}" on the Scan page (or pick an area in the camera), and every scan: hardware gun, UPC, or camera, is filed into that location, persisting on the item so even a later triage already knows where it was scanned and pre-fills it. Confirming files the new item into that location in your `core-locations` tree. It's the warehouse "put-away" pattern: choose where you're standing once, then just scan; the location rides along instead of being re-picked per item.
- Barcodes that the lookup services can't find now get a little more help. Every barcode's prefix encodes the country where it was registered (a Prusament filament barcode starts 859 = Czech Republic), and the AI identifier now gets that as a hint, so when web results are thin it can still make a better guess at the maker. Free and instant, no extra lookup.
- The scan inbox now has a "⚠ N needs review" filter. After you bulk-confirm the confident scans, flip it on to show only the ones that didn't cleanly resolve: no name yet, a short/ambiguous barcode, a rate-limit retry, or low confidence, so you focus your attention exactly where it's needed instead of scrolling the whole pile. Bulk-select respects the filter, so "select all" + Confirm/Discard acts on just the shown set.
- When you scan an item and the barcode resolves to the wrong product, snapping a photo now gives you a one-tap fix. The photo cross-check already noticed the mismatch; now it also reads the real product off the label and offers a "Use photo's name: …" button. One tap renames the item to what the photo shows, and reports the correction to the shared barcode database, so the next person who scans that code gets the right name automatically.
- Re-scanning an item to add a photo now improves the entry you already have instead of making a second one. When a barcode came back with the wrong (or no) name, scanning it again with the camera attaches that photo to the existing entry (even from a later session) and re-checks the name against the photo, so the "this doesn't look right" flag (and the one-tap fix) appear on the row you were already looking at. No more duplicate rows for the same thing.
- Two scan clean-ups. Clearer provenance: when a barcode resolves instantly from the shared Barcode Intelligence DB (its cache or the grocery mirror) the note now reads "BIdb / go-upc" instead of a bare "go-upc", so an instant hit no longer looks like a live lookup it couldn't have been. Better English names from web search: the fallback that names an item from web results now prefers English titles over foreign-market listings (it was only filtering non-Latin scripts, so French/Spanish names slipped through), fewer "pâte à tartiner" names when an English one is available.
- Scan a location to start filing into it: zero clicks. Print a QR label for a room/shelf/bin (any `core-locations` place), and scanning it makes it the active filing bin: everything you scan next files in there. Scan a container while a room is active and it *nests*, the container is filed into the room and becomes the new bin (so "scan the garage, scan the tote, scan the parts" lands the parts in the tote, the tote in the garage). Scanning another area just switches context (it won't wrongly nest one room inside another), and a move that would create a loop is skipped. Works the same from the camera and a hardware scan gun.

### Fixes
- Bambu connection messaging now reflects hybrid LAN+cloud. LAN and hybrid modes are marked available (they shipped), the capability notes describe what each mode actually does, and the connection badge says "cloud · monitor + light/pause" vs "lan/hybrid · full control" instead of a blanket "control". Stale "coming soon / Phase 3 / no remote control" copy removed.
- Personal/BYO AI (the edge bridge) is more reliable under a flaky agent connection. The agent that carries your AI long-polls the cloud, and in the brief gaps between polls a request could hit "no edge device connected" and fail, which made the scan matchmaker fall back to "Matched by keywords (no AI)" even though AI was connected. Those momentary disconnects are now retried (a few seconds, with backoff) so the call waits for the agent's next poll instead of giving up.
- Scan catalog pictures are more reliable: the image search now retries when DuckDuckGo's image endpoint returns empty (it's flaky from a server IP even when the web search has results), and re-running AI on an item now re-fetches its picture by name, so a barcode whose title was corrected upstream but whose image stayed wrong (e.g. a "Pinecil" showing a ribbon cable) fixes itself on re-run.
- Locations: clearer area icon. The "area" kind icon was a plain square that looked like a second, unchecked checkbox next to the real select checkbox. It's now a location pin. No more double-checkbox confusion.
- When AI isn't doing the matching, the inbox no longer force-fits an item into the wrong bundle. The keyword matcher used to suggest any table that shared a single word, so a "Pine64 LCD Ribbon Cable" got tagged Yarn (because "ribbon" is a yarn word). Now a table is only suggested when the item actually *is* that kind of thing (its noun matches) or a specific attribute matches, otherwise the card just offers the generic "Inventory part." Fewer, better chips (at most two), and no more cables in the yarn bin.
- When an item gets renamed: whether you fix it yourself or the photo correction replaces a wrong barcode title, the catalog picture now updates to match the new name instead of keeping the old product's image. (Previously a corrected name like "Pinecil Soldering Tip Set" could still show the wrong barcode's ribbon-cable photo.)
- Scanning a big batch no longer drops items to a raw, uncleaned name or a scary "502" note. The AI bridge that names and sorts each scan is a single agent, and a burst of scans used to overwhelm it, so some items kept the messy web-title ("… - Botland - Robotic Shop") instead of the tidy name the AI would have produced. Scans now queue gently to the bridge (a few at a time) and retry a momentary overload, so far more items come back with a clean AI name. Also: junk "inventory checker / price tracker" pages (BrickSeek, camelcamelcamel, bare UPC-lookup results) are no longer mistaken for the product name.
- The scan inbox now fits a phone screen. The intake buttons (UPC / URLs / Upload / Receipt / camera) sit in a strip that scrolls sideways instead of overflowing off the edge, so adding new intake options later can't break the bar again. And the per-item "which table" chips no longer stack into a tall wall on mobile: each card shows just the best match plus a "+N" you can tap to see the rest, while desktop still shows them all.
- A scanned physical product no longer gets suggested for admin tables it can't belong to. Scanning a barcode (or a photo the AI reads as an item) will never route to Subscriptions, Warranties, Documents, or Bills: those are records you add by hand, not by scanning. Scannable physical tables like Medications and Collections are unaffected. The guard is deterministic, so it holds even when AI is slow or offline.
- Three inbox fixes. Undo on remove: removing a scan (one or a whole selection) now shows an "Undo" button right on the toast, so a mis-tap is one click to put back instead of a trip to Recently deleted. Right words for a barcode: an item that's just a barcode (no photo) no longer says "Couldn't identify this photo". It says "barcode". No more stuck "Awaiting lookup": when a barcode lookup is rate-limited and the automatic retries are used up (e.g. the daily lookup quota is spent), the item now settles into a clear "couldn't identify, name it" with the name box ready, instead of spinning "retrying…" forever or sitting on a misleading "Awaiting lookup…".
- The "Removed: Undo" toast now names what you removed (the item's name, or its barcode) instead of a bare "Removed.", so a stack of them is easy to tell apart, and they auto-dismiss after a few seconds instead of piling up until you dismiss each one.
- The barcode web-search no longer "gives up" before the AI has answered. On a self-hosted or personal AI connection that's slow (100s+ per call), the web-search identify was timing out at 20 seconds and reporting "nothing found": even though the AI was still working and would have identified the item. The timeout is now generous enough (matching the matchmaker) for a slow model to finish, so far fewer scans fall through to "fill in manually" just because the AI was slow.
- Barcodes that aren't in any database now resolve far more often. The web-search fallback used to ask DuckDuckGo's image index for a name (which can't resolve a bare UPC) so it usually came up blank. It now asks the text/web search too, which returns the actual product pages (e.g. a Cuisinart pan's UPC → "Cuisinart Chef's Classic Nonstick Hard Anodized 11″ Square Griddle"), giving the AI real titles to identify from.

## 2026-06-21

### Features
- Barcode lookups now learn from your triage. When you confirm a scanned item after fixing its name, because the barcode lookup was wrong or found nothing, Cobblr quietly feeds that correction back to the shared barcode database, so the next scan of that product (in any workspace) gets your fix. It's invisible and never affects your own item; only a meaningful rename is sent, tagged anonymously. (Operator-configured via the resolver correction token; off until set.)
- Operators can now review barcode corrections from a public approval queue. When an instance is set to *propose* mode, user fixes to wrong/missing barcode lookups land in Super-admin → Barcodes → "Proposed corrections" with the current value vs the proposed one: Approve makes it the verified answer every workspace's next scan sees, Reject drops it. Approval uses a server-side review token (super-admin only; never exposed to public users), so a public instance can both capture proposals and host the review without handing out write access.
- Scanned items now get a cleaner catalog photo. Image options are ranked by catalog quality: a retailer or brand product page (clean studio shot on white) floats to the top, while recipe blogs, Pinterest, and styled/social photos sink, so the good photo is the first one you see instead of buried at #6. The barcode lookup's own auto-chosen image uses the same ranking. And when you pick a better photo for a scanned product, that choice is fed back to the shared barcode database as an image correction, so the next scan of that product (in any workspace) gets your clean image, beating whatever the providers had.
- Bambu LAN, as a setting on the printer. Open a Bambu printer in digifab → LAN access → enter its IP + access code, and Cobblr adds a LAN transport to the *same* printer (no second connection). Cloud keeps doing live telemetry; file-push (sending an arbitrary sliced file, which cloud can't do) and control now route through your on-site edge bridge over the printer's LAN. (Live camera over LAN is a follow-up; it needs an RTSP update in the bridge.)
- Printers connected through the edge bridge (Duet, Klipper/Moonraker, Prusa) now show live controls: Pause / Resume / Stop: in the printer modal, instead of "no live controls." Prusa printers also report their nozzle/bed temperatures now.
- Live camera over LAN. A Bambu printer with LAN access now shows its chamber camera right in the printer modal: a refreshing still, grabbed frame-by-frame over your bridge (cloud can't do this). Click it to zoom full-screen.
- New file library for digifab: upload your sliced .3mf / .gcode files and Cobblr pulls out the slicer's plate thumbnail for an at-a-glance grid (no slicer or render step: the preview is read straight from the file). Send any library file to a printer or a pool in one click. Multi-plate 3MFs show their plate count.
- A workspace can now run more than one edge bridge: a second site/VLAN, or LightBurn, which has to run its bridge on the LightBurn PC itself. Give the extra bridge an id in the connection setup; it gets its own tunnel channel and the machines you add are pinned to it. Your existing single bridge keeps working exactly as before (no id = the default channel).
- 3D-printer Print updates: post a photo + live status to Discord as a print runs: your rules. Add a Discord channel once, then make rules per printer (or all printers): post every N % / N minutes / N layers (whichever comes first, with an optional "no more than once every N min" cap), with a `{{param}}` message template you can preview, and the live camera photo attached.
- Click a printer in digifab (its name, or "Details & controls") to open a full printer modal: image, live telemetry (nozzle/bed/chamber temps, AMS filament colors + remaining, chamber light, firmware-update + alert badges), the controls (now inside the modal), a link-to-machine picker, and that printer's recent prints. Click any print: there or in the history list, for a detail card with a large cover you can click to zoom full-screen. The temperature controls show the live actual → target and pre-fill from the current target.
- Print queue gains priority and auto-reprint-on-fail. Mark a job High or Urgent and it jumps ahead of Normal jobs when a printer frees up; set an auto-retry count and a failed print re-queues and re-sends itself (pool jobs re-drip to any free printer, device jobs retry on the same one) instead of just stopping.
- Duplicate a print-update rule: a copy button on each rule clones it (cadence, message, hooks and all), so you can spin up a per-printer variant in seconds instead of rebuilding it.
- Print-update rules gain a "Test fire now" button (posts a real update from the printer's current live telemetry, so you can verify a rule end-to-end without waiting), plus pre/post hooks: run printer controls around the post (e.g. chamber light on → settle → shoot the photo → light off). Hooks run non-blocking, so the delays never hold up telemetry.
- Each Bambu printer now has a transport mode in its LAN-access panel: All cloud (status/control/history via Bambu's cloud), Prefer LAN (status + control + file-push over your bridge, cloud fills in print-history names), or LAN only (everything local, cloud off: no internet needed, but you lose cloud-only history names/covers). Cobblr pulls live telemetry over LAN for the LAN modes, so status keeps flowing even with the printer off the cloud.
- Scanning is now type-aware. Cobblr recognises what kind of code you scanned and routes it to the right place instead of running everything through the product-barcode databases: a UPC/EAN goes to the barcode chain, an ISBN is looked up as a book (Open Library: title, author, cover), a real Amazon ASIN tries the product page, an Amazon FNSKU (the `X00…` warehouse labels) is recognised as un-lookupable and sent straight to "name it" (no more endless "rate-limited, retrying"), and a URL goes to the maker-page resolver. The upshot: non-barcodes no longer waste a barcode lookup or get falsely stuck as rate-limited, and books/Amazon items identify better.
- When you scan a barcode with the camera, Cobblr now double-checks the result against your photo. After the barcode resolves to a product name, a vision model glances at the picture you captured and asks "does this actually look like that?", and if it clearly doesn't (a store-local or reused barcode resolving to an unrelated product), the item is flagged "⚠ this photo doesn't look like *X*, the barcode may be wrong" with its confidence dropped, so you catch the mismatch instead of filing the wrong thing. Runs in the background (your named item appears instantly) and only fires when there's a photo to compare. A fix you make then flows to the shared Barcode Intelligence DB so the next scan of that code is right.

### Improvements
- The live pump now also records a print it finds already finished when it connects, so a print that completed while the pump was offline (or before this tracking existed) still lands in Print history, not just prints it watched start-to-finish.
- Experimental cloud control for Bambu. From a Bambu's fleet card you can now toggle the chamber light or nudge Z over the cloud connection: no LAN setup. It rides the same MQTT the pump already holds (the broker the Bambu app uses). It's a safe, visible way to test whether the printer accepts our commands; if it does, full pause/resume/stop over cloud follows.
- Cobblr now captures the full Bambu data: the complete live MQTT report (temps, AMS, layers, fans, errors, the lot) and the cloud print-history (model name, cover image, weight, time), stored raw. The groundwork for richer print history and correct print names.
- Generic printer controls. Every printer's fleet card now has a Controls panel that shows exactly what *that* printer can do: declared by its driver. Bambu (cloud) gets pause/resume/stop, jog (X/Y/Z), chamber light, and nozzle/bed temperature; the declarative managers (OctoPrint/Klipper/Duet) get whatever their manifest declares, including custom commands. Only supported controls appear.
- Print history now shows Bambu prints by their real model name with a thumbnail: "Cherry Blossom Crossbody Bag" instead of the cryptic profile name, plus accurate weight and print time, pulled from Bambu's cloud history. Failed/successful prints, per-printer rollups, and filament totals all include them.
- Prints you start outside Cobblr now show up in Print history too. Cloud Bambu is monitor-only, so prints are kicked off from Bambu Studio: the pump now records each one when it finishes (completed/failed), and history merges them with the prints Cobblr sent.
- Tidied the Modules list. The built-in wasm demo modules (hello-world examples + a sandbox URL archiver) were only ever there to exercise the platform's sandbox feature. They no longer appear in real workspaces (they load only when you're developing Cobblr itself). And Maintenance is now an optional module you can switch off if you don't use it; it was previously pinned on with no way to disable it.
- Scanning the same barcode again no longer piles up duplicate rows in the Scan Inbox. It bumps the quantity on the existing pending entry (the way companion app does it), shown as a ×N badge on the card. Scan a thing five times → one row, ×5. (Scoped per scan session + area, and only while the entry is still pending, a re-scan after you've filed it starts fresh.) Plus, a phantom row with a spinner now appears at the top of the inbox the instant you scan, so you know it registered while the few-second lookup runs, then swaps in for the real row.
- You can now adjust a scanned item's quantity right in the inbox: the ×N badge on a card is a − / + stepper that updates the count in place (handy when a repeat-scan over-counted), keeping the item pending. No need to open the commit form to change a quantity.
- Discarding a scanned item from the inbox is now a single tap on the ✕: no confirmation dialog (it's not a big deal to remove a scan). And nothing's truly lost: a Recently deleted section at the bottom of the Scan Inbox lists what you discarded, each with a Restore button that puts it right back, enriched data and all.
- When you scan a maker's product QR (e.g. a Polar Filament spool), Cobblr fetches the maker's data and fills in the real product, and that vendor list is now data you can edit, not code. A new Scan Resolvers page in the operator console lets you add a maker by describing how to read its URL (match → fetch → map fields), test it against a real URL, and edit or disable built-ins, no deploy. Adding the next maker is a list entry, not a code change. (Replaces the old single-vendor maker-scan module; the built-in Polar resolver behaves identically.)

### Fixes
- Uninstalling a bundle now cleans up after itself properly. The tables a bundle added (e.g. a Yarn, Hooks, or Designs list) are removed, and any module it switched on is switched back off: but only when nothing else still needs it. If another installed bundle shares that module, or you turned it on yourself, it stays. The Remove dialog now tells you exactly what will go ("deletes Yarn (12 items), Hooks and turns off Inventory") before you confirm. Previously the bundle's lists and modules were left behind, lingering in your sidebar.
- Groundwork for the premium Discord-bot print-updates tier: print-update channels can now be a managed-bot destination (delivered by Cobblr's hosted layer), alongside the free incoming-webhook kind.
- The edge-bridge setup now names the bridge it's reporting on ("the garage bridge is online ✓" vs a vague "your bridge"), since you can run several. The Bridge-id field is clearer too: blank = your main bridge (installed without a BRIDGE_ID), and any additional bridge must be named so it gets its own channel.
- The LAN printer camera now shows the last frame instantly when you open a printer, instead of a few seconds of "connecting": the live feed swaps in as soon as a fresh frame loads.
- Two digifab fixes: a Bambu cloud connection no longer reads "monitor only": cloud control (chamber light, pause/resume/stop) works, so it now shows "control" with a tooltip on what still needs LAN. And the "link to machine" picker now finds machines across all instances (your printers under "3D Printers", lasers under "Laser cutters", …) instead of only the default collection, no more spurious "no matches".
- Printer-modal cleanup: the machine link is now a compact "Linked to X · change" line (no more full-width dropdown for a one-and-done), the empty photo placeholder is dropped when there's no machine image, the AMS chip hides a meaningless "0%" external-spool reading, duplicated print sub-labels are suppressed, and the "needs LAN control" note flips to "Commands route over your LAN bridge" once LAN is on.
- Print-history sub-labels (the slicer profile, e.g. "成年人版 0.2mm 层高, 3 层墙, 15% 填充") now render in English: "Adult 0.2mm layer, 3 walls, 15% infill". Bambu only translates the model title, so Cobblr maps the common Chinese slicer terms itself.
- Modals no longer close when you click outside them: a stray outside-click can no longer discard a half-filled form (Esc, the ✕, and Cancel still close). And when you start a 3D-printer and jump to "Install the edge connector," the name you typed now carries into the connector setup.
- Filing a scanned item into a category whose module isn't set up yet no longer dead-ends on a cryptic "Target create returned 409". Committing a scan now enables the target module automatically (e.g. Inventory) and files the item: the capture-first "scan first, structure later" promise. And if a commit can't go through, you now see the real reason (e.g. "Enable Inventory in Configuration → Modules") instead of a bare status code.
- Hands-free barcode scanning now works with Bluetooth scanners. On the Scan Inbox page a HID scanner types the code "behind the scenes" and auto-ingests it, but the detector judged a scan by the gap between individual keystrokes, which is too strict for Bluetooth's jitter (USB dongles are steadier), so BT scans were silently dropped unless you first opened the UPC field. It now judges the whole code's average speed instead, tolerant of Bluetooth's variable timing, still ignores human typing, so hands-free scanning works the same whether your scanner is USB or Bluetooth.

## 2026-06-20

### Features
- Added a guided edge bridge setup in Print Manager → Add a connection → "Edge adapter (your bridge)": it shows the one command to run the bridge on a box at your site, watches live for it to dial in ("Waiting…" → "Bridge online ✓"), and creates the tunnel connection once it's connected. The way to reach a LAN machine from a hosted Cobblr.
- Reach a LAN machine from a hosted Cobblr. A hosted instance (strict egress) can't dial your home network directly, so now an on-site edge bridge dials OUT to Cobblr and holds a tunnel open; Cobblr routes printer commands down it. Create an `edge_adapter` connection with a `cobblr-edge://` address and the bridge handles the rest. No inbound firewall hole, no SSRF surface (the cloud never fetches a private IP). The tunnel now powers connection-test and device-listing too, not just job send/poll.
- Read QR labels printed by another app. Configuration → External QR rules lets you teach the scanner a foreign label format (a companion app URL, a bare Homebox number, anything) and map it to the matching item here, so labels you already printed keep working without a reprint. Scan one and it opens the item exactly like a native Cobblr label; paste a sample into the built-in tester to check a rule before you trust it. Fully opt-in: with no rules, the scanner behaves exactly as before.

### Improvements
- Bambu connections now show which way they work: a "monitor only" / "control" badge with the reason (hover it), and when you add a Bambu you choose up front (Cloud account (live status, no remote control) Bambu blocks it) or LAN via your edge bridge (full control: send / pause / cancel).
- Cloud Bambu printers now show real-time status on the fleet: live nozzle/bed temps, print state, and the printer's own progress (layer + time left), streamed from Bambu's cloud MQTT instead of the slow HTTP poll. Works even for a print you started straight from Bambu Studio.
- When you add a Bambu printer and pick which one it is, the printers now show as a clickable card stack (name, model, online/offline) instead of a dropdown, and a single printer is pre-selected. Matches the look of the connect wizard.
- Adding another Bambu printer no longer asks you to sign in again. When you pick Bambu Lab, Cobblr reuses the Bambu account(s) you've already connected and lists the printers on them you haven't added yet: pick one and it links. You can connect several Bambu accounts at once (a connection is one account); when you have more than one, a small Bambu account picker lets you choose which, and + Connect another Bambu account adds a new login. Signing into the same account again refreshes that connection in place (no duplicate) so its machine links and fleet view stay intact.
- Adding a machine to a bridge you already run is now obvious: if your edge bridge is online, the setup skips the install steps and goes straight to "add a machine", so you can plug a Bambu (LAN), Prusa, Klipper, etc. into your existing bridge without reinstalling. Also: in the pool machine picker, the printer name now gets priority over the connection it's on (no more "Thum…").
- Known machines now get the right product photo. Cobblr checks a curated image catalog (the `CobblrHQ/printer-images` manifest) before its live web-image search, so a Prusa Mini stops showing up as an MK4. Fix or add a model's photo in that repo and it updates within minutes: no Cobblr redeploy.
- Hooking up a non-Bambu printer is now as slick as the Bambu wizard. Pick RepRapFirmware (Duet), Klipper, Prusa, or Marlin/OctoPrint when adding a 3D printer and a Connect directly option lets you enter the printer's address (and API key if its firmware needs one) right in the dialog. Cobblr installs the right driver, creates the connection, tests it, and links the printer. No trip to the Print Manager, no pasted JSON. The Print Manager → Drivers dialog also gained a one-click catalog for installing those drivers on their own.
- A Print history section on Digital Fabrication: at-a-glance stats (prints, success rate, filament used, print time) over a 7 / 30 / 90-day window, a per-printer rollup, and your recent prints: all from the jobs Cobblr already tracked, no new setup.
- A print-manager connection's printers now expand inline as a children list under the connection row. Click the chevron (or the row) instead of opening a separate modal. Fewer clicks, nothing pops over the page; the link-to-machine pickers are right there in place.
- Pause / resume / cancel a running print now works on OctoPrint, Klipper (Moonraker), and Duet: the live-control buttons that used to be greyed out for those managers now act. Support is declared per driver (a manifest's `commands`), so any declarative manager can add control without a code change.
- The Machines page now remembers whether you were on Items or Fleet across a refresh (a new persisted-view helper), and the fleet's live "time left" reads as 13h 2m instead of 782m.
- The edge bridge is now install-once, add-machines-anytime. Name it and Install the bridge with just a token: the box dials in and stays open. Then add machines to it whenever (in the setup dialog or later); each rides the tunnel and the running bridge configures it on the fly, no restart, no regenerated command. Naming the bridge is enough to install it; machines are step two, exactly as you'd expect.
- Installing the edge connector is a smooth in-flow step now: no trip to another page. Add a printer that's on your network from a hosted Cobblr, pick "Via an edge bridge", and the dialog: asks what it's bridging (Klipper / Prusa / Duet / Bambu-LAN / LightBurn, pre-selected from the printer kind, with the right fields), generates a least-privilege token for you (new `devices:edge` scope that can *only* run the bridge), and bakes both into a ready-to-run command. It then watches for the bridge to dial in and lets you pick the machine.
- The edge-bridge install step now offers a docker compose snippet alongside the `docker run` command: toggle between them and copy whichever fits how you run things.
- One edge bridge can now front all the machines on your site over the tunnel. The cloud routes each call to the right machine by its instance id (`/voron/devices`, `/mk4/status/…`), so a single bridge with several configured machines: Klipper here, a Prusa there, a Duet too, all reach a hosted Cobblr through one dial-out connection. Add a machine = add an instance to the bridge config + a connection in Cobblr.
- The edge-bridge setup now configures one bridge for all the machines at your site. It clearly frames the first machine, and + Add another machine lets you put several on the same bridge (each with its own driver + IP). It generates one run command and creates a connection per machine. Plus: the multi-step connect modals now have a clear ← Back to the previous step, and Cancel exits the whole dialog (it used to quietly go back, which was confusing).
- Share machines on your edge bridge with another Cobblr workspace: pick which printers, choose read-only (watch) or read + write (control), and send a one-time link. Their workspace never receives your printer's credentials, and you can revoke access any time.
- External QR rules got simpler to set up and now cover every kind of label. Define a foreign system once: its base URL plus a list of `/segment/ → kind` children (e.g. `/printers/ → Machines`, `/parts/ → Parts`), and one rule handles a whole host: the segment picks the kind, the trailing id resolves the item. Works for machines, parts, assets, and locations alike, and the seeding backfill now covers all four.
- Groundwork for reading QR labels printed by other apps. The scanner can now be taught (per workspace) how to map a foreign QR code (a companion app URL, a bare Homebox number, …) to the matching Cobblr item, so labels you already printed keep working without a reprint. When a rule matches, the scan behaves exactly like a native one (opens the item). Opt-in and invisible until you set up a rule. The point-and-click setup lands next; today it's configurable via the API.
- The live Fleet view (machine status, temps, progress, in-flight jobs) is now a tab right inside your 3D Printers section. No more hunting for it under Configuration → Print Manager. It appears whenever the Print Manager is enabled, alongside the printer list. A linked printer's detail view also shows a live status chip (linked / connected / idle / printing % / needs-clearing / offline) so you can see state at a glance.
- The "that's a LAN address" warning in the direct-connect printer flow now only shows on a hosted Cobblr (cobblr.me / managed), which can't reach a device on your network. A self-hosted Cobblr on the same LAN no longer shows it: direct connection just works. Driven by a new `hosted` flag on `/auth/config`.
- A printer's photo is now editable from its detail modal. Hover the image and click Change to upload your own, Auto to re-fetch a product photo, or Remove. No more being stuck with whatever auto-loaded.
- The auto-fetched printer photo now appears live in the open detail modal: no need to close or refresh. When you open a printer with no image, Cobblr finds its product photo in the background and the thumbnail updates in place as soon as it lands.

### Fixes
- The Bambu live pump now derives the cloud MQTT username from the account token when the stored one is blank, so real-time telemetry starts even for connections saved before this, plus clearer pump logging.
- The Digital Fabrication entry in the navbar now opens its page instead of bouncing back to the workspace home: the page is routed at `/digifab` to match the nav link.
- Fixed an edge-bridge printer getting stuck on "loading…" with a dead Link button. The machine's host/key wasn't reaching the bridge (a config-shape bug), so a PrusaLink/Duet printer never appeared. Also: a direct driver (PrusaLink, Duet, LAN Bambu) IS the one printer, so its detail no longer shows a pointless "which printer" dropdown. It links directly. The dropdown now only appears for a true multi-printer farm manager (FDM Monster / OctoPrint).
- Connecting a printer by IP no longer creates a broken connection. Typing a bare address like `192.168.1.50` (no `http://`) now gets normalized to a proper URL instead of being saved schemeless, which previously surfaced later as "unreachable, invalid URL" in the fleet. An address that still can't be parsed is rejected up front with a clear message.

## 2026-06-19

### Features
- Connected printers now get a product photo automatically: you do nothing. When you add or open a 3D printer, Cobblr searches the web for its model (e.g. "Bambu Lab A1 3D printer"), fetches the best match into your file store, and sets it as the printer's image. Reuses the same image pipeline that gives a scanned product its photo; the image lives in your workspace, and any photo you upload yourself always wins.
- You can now connect a 3D printer to its print manager right from the printer flow: the "New 3D printer" modal has a Print Manager section (turn it on with one tap if it's off, then link to FDM Monster, OctoPrint, or Bambu), and the printer's own page has the connect panel. Workspaces that installed a machine bundle before this feature shipped get Print Manager turned on automatically: no reinstall.
- Connect a Bambu Lab printer by signing in with your Bambu account: no IP, no access code to copy. Add a connection → Bambu Lab, log in (region + email + password, with 2FA if you use it), and Cobblr discovers the printers on your account and pulls each one's details automatically. Cloud mode gives you live status in the fleet cockpit; the wizard tells you up front what each mode can and can't do (remote control needs LAN + Developer Mode, coming next).
- A machine type with its own tab (3D Printers, Laser Cutters, CNC Machines) now opens the full machine page. Set every field, edit, attach files, and link the machine straight to its print manager (FDM Monster/OctoPrint) from its own page, instead of a name-only stub. Installing one of those bundles also offers a checked-by-default "Connect to your machines" option that brings the Print Manager along.
- Setting up a tracker is smoother. The "what do you want to keep track of?" search now tolerates typos: *"pprint"* still finds 3D Printers, *"machne"* finds Machines. And picking a ready-made tracker no longer pops a second *"enable this module?"* dialog; Cobblr enables what the tracker needs and installs it in one step.
- Forward receipts by email. Every workspace you're in now has its own `receipts+…@` address (shown on the Scan page. Tap Copy). Forward a receipt there and its attachment runs through the same parser as an upload, landing as line items in that workspace's scan inbox, no app, no typing. The address itself names the workspace (signed, so it can't be typo'd or forged into someone else's), so multi-workspace folks get one address each and they all arrive in one inbox on our side. Operator-configured (needs the mail Worker set up, like reply-by-email); when it isn't, the address just doesn't show.
- Receipts now parse without AI whenever they can. Upload a CSV export or a text PDF with a line-item table and Cobblr pulls out the items deterministically: header-mapped columns for CSV, table extraction for PDFs, no model call, works even with no AI provider configured. A messy PDF falls back to a single AI text read, and a photo of a receipt still uses an AI vision read. Same one-row-per-line-item inbox triage either way; the inbox remembers how each line was read.
- A scanned or forwarded receipt can now become one purchase order instead of a pile of unrelated parts. With the Purchases module on, a receipt in your scan inbox shows a "Confirm as purchase order" banner: one tap creates a purchase order (vendor, date, total) and files every line as an order item linked to its part. You can still fix or discard individual lines first; it rolls up whatever's left. Without Purchases, the lines just confirm into parts as before.
- Public surfaces (wall panels) now have a per-surface auto-refresh setting and an E-paper mode. Pick how often a published screen re-fetches (every 30s up to hourly, or Off for a static panel) and flip on E-paper mode to force a light, high-contrast (black-on-white) layout with auto-refresh off, so a reflective panel in a cabinet doesn't ghost on a timer. Works for view, board, and whole-App surfaces.

### Improvements
- Connecting a Bambu printer now lives right in the New 3D printer flow: pick what kind of printer it is (Bambu, Klipper, Prusa, …) and how to connect: Directly through Bambu API or through a manager (FDM Monster / OctoPrint). Choosing Bambu-direct signs you into your Bambu account once, discovers every printer on it, and lets you pick which one this is, with the name and model pre-filled and the printer linked the moment you create it. One login covers your whole fleet.
- After a one-click bundle update, the dashboard row keeps a "See details" link (and shows the version you updated to), so you can still open the bundle and read what changed. The 3D Printers / Laser Cutters / CNC Machines bundles now carry that "what's new" note too.
- Creating a location while filling out a form no longer pops a second modal: "+ new location" now expands as an accordion right inside the form you're in. Kind is a two-button pick (Container / Area) instead of a dropdown, and a container you name "Bin" (or "Shelf", "Drawer") gets a globally-unique number appended automatically, "Bin 17", so every container has a unique label/scan number.
- The machine / 3D-printer detail modal opens without scrolling: a tighter header (photo + actions + Kind share the top row) and small add-pills: Tag · File · Link · Note. That only grow into full fields once you use them, instead of a wall of empty attachment boxes. Known printer models also show a product photo in place of the name-initial placeholder (drop images in `web/public/printers/`).
- A 3D printer's detail form now shows only the fields its kind needs. A Bambu (or Prusa) (a closed product where the vendor owns the hardware) hides the DIY build-detail block (hotend, extruder, mainboard, firmware, bed size, local IP); a Klipper / RepRap / Marlin printer you configured yourself still shows them. Quantity is gone from individual machines too (a printer is one unit, not a stock count). Existing Bambu printers clean up automatically; a Kind selector lets you adjust.
- The New 3D printer "what kind of printer is this?" picker now includes RepRapFirmware (Duet) alongside Bambu, Klipper, Prusa, and Marlin, so Duet owners pick their control ecosystem and connect through Duet Web Control.
- A machine's State is now a dropdown (functional / needs maintenance / broken / building / built / kit / rebuilding / loaned / sold / decommissioned) instead of a free-text box, with any existing value preserved. Also fixed the printer thumbnail: a broken image now falls back to the name-initial letter (no broken-image icon), and the auto-fetched product photo now lands correctly on printers that live in a named tab (e.g. 3D Printers).

## 2026-06-18

### Features
- Plug in a barcode scanner and just scan. USB or Bluetooth barcode scanners (1D laser or 2D imager) now work hands-free on the Scan page: point, pull the trigger, and the item lands in your inbox automatically. No need to open the UPC box or click into a field first. Typing into a real field still works normally, so the scanner never gets in the way.
- A brand-new workspace now starts with capture, not setup. Instead of a blank builder asking you to define modules and fields, the first screen asks "What do you want to keep track of?": just scan something or write something down ("3 skeins of blue worsted wool", "my Ender 3 printer"). Cobblr reads each thing against its ready-made trackers and, once a few cluster, offers to build the right one in a tap: "These look like yarn, make a Yarn tracker (3)" installs the tracker and files everything you captured into it, fields and all. No structure to design up front, do first, organise after.
- Track a consumable (a filament spool, a yarn cone, a roll of anything) properly. Set its capacity (the new 1kg), and Cobblr keeps a consumption ledger on the item: what drew it down and how much, each with its source, so you can see *"started at 1kg → these prints used this much → here's what's left."* A print that declares its filament writes a ledger row automatically (attributed to the print). The part page shows a remaining-vs-capacity gauge + the full history.
- Building a print farm from scratch? Digital Fabrication's new Add several button stands up a whole fleet at once: paste a list of printer URLs (one per line: `url`, or `name, url`, or `name, url, apikey`), pick a default firmware, and Cobblr makes one direct connection per printer, installs the drivers it needs, and (optionally) groups them all into a pool. Hit Detect firmware and it probes each URL to guess OctoPrint vs Klipper/Moonraker vs PrusaLink vs Duet for you. Tick test each to see which came up reachable. (Migrating an *existing* FDM Monster farm? Use Import farm instead.)
- The print farm got a real cockpit: live camera feeds, temperatures, and pause/resume. Each printer on the Fleet now shows its live nozzle and bed temperature, and you can give it a camera URL (your OctoPrint/Klipper webcam stream, or any MJPEG/HLS feed). Cobblr embeds the live feed right on the printer's card, so you can watch every machine from one screen. And a running print now has Pause and Resume controls (where the machine's manager supports it). Cobblr still never touches the camera or the hardware directly; it embeds the stream the manager already serves and asks the manager to pause; coordinate-not-control all the way down.
- A finished print no longer closes your task until you confirm it came out good. A printer reporting "completed" only means the gcode finished, not that a good part is on the bed (it could be spaghetti). So Cobblr now treats the after-effects by how much it hurts to get them wrong: your filament count and machine hours still update the moment a print finishes (cheap to undo), but closing the to-do that print was for now waits for your verdict. When you clear the bed, you tap Came out good: which closes the linked task, or Scrapped: which puts the filament and the machine usage back and leaves the task open. In the normal good case it's one tap you were already making; the one thing that used to silently go wrong on a failed print (a task marked done that wasn't) no longer can.
- Drive a printer at home from a Cobblr in the cloud: no port-forwarding. The print farm now supports the device tunnel: an edge agent on your network dials *out* to Cobblr and holds the connection open, so a hosted Cobblr can list, upload, print, pause, and track a printer behind home NAT exactly as if it were on the same LAN. Point a Digital Fabrication edge-adapter connection at `cobblr-edge://` and it routes through the tunnel instead of a direct URL. Two edge agents ship: the standalone edge-bridge (Docker/Pi) and a new OctoPrint plugin (`OctoPrint-Cobblr`) that also posts rich print updates, with a live webcam snapshot, straight to Discord. (It reuses the same proven dial-out path as Cobblr's local-AI relay. The cloud relay is hosted-only; self-hosted Cobblr on your LAN doesn't need any of this.)
- Print farm safety. The pool queue no longer auto-starts the next print on a printer that just finished: a finished (or failed) print now flags the printer "needs clearing" on the Fleet view, and the auto-assigner skips it until you tap Cleared, ready. No more crashing a fresh print onto a bed that still holds the last part. Two more hardening fixes ride along: a transient network blip while polling no longer flips a healthy in-progress print to "failed" (it retries a few times first), and a pool job whose send fails now shows up as failed with the reason instead of silently sitting "waiting" forever.
- Digital Fabrication now opens on a live Fleet view: every machine across all your connections on one floor, each with its current state (printing / idle / error / offline) and, when there's a Cobblr job on it, the file and a progress bar. A header counts the fleet at a glance ("12 machines · 4 printing · 1 error"). It refreshes itself, and an unreachable manager just flags its own row instead of blanking the rest. It reads what your machine managers (FDM Monster, OctoPrint, …) report. It never drives the hardware.
- Machines now accrue usage as your prints run. Link a print job to a machine, and each completed print bumps that machine's lifetime print count (and hours, when the manager reports a duration). It's the foundation for maintenance-by-usage: pair it with a maintenance threshold to get reminded "nozzle due at 500 prints" instead of guessing. Runs through a wire on print completion, so it's tunable like anything else.
- A print job can now declare the filament it consumes (a part + grams), and when the print finishes Cobblr deducts that material from your stock automatically. Your farm finally knows your filament. No more guessing whether you can start the next plate. (Set it on a new job under "Filament"; deduction runs through a wire on completion, so you can tune or disable it like any wire.)
- Digital Fabrication can now run a cross-machine queue. Make a pool (a set of machines, even across different connections) then queue jobs onto the pool instead of a single printer. Cobblr drips each job onto the next *free* machine as printers finish, so you can throw a batch at your farm and walk away. It's how you run a pile of individual printers (OctoPrint, Klipper, …) as one farm without FDM Monster in the middle. Pure coordinate-not-control: Cobblr only picks which printer gets the file.
- Live print updates in Discord, with the webcam snapshot, the ETA, and one post instead of two plugins. As a job runs, the farm posts to Discord (or in-app, or email): started → 25 / 50 / 75 % → finished / failed, as a rich, colour-coded card with Progress, Remaining, Elapsed, and Filament, the live webcam snapshot embedded right in the post, and a Live view link to the feed, the OctoEverywhere experience, in one message. Set it up once at Me → Notification channels: add `digifab.print.*` (or `*`) on the Discord channel with your server's webhook URL. The snapshot works wherever Cobblr can reach the camera (a self-hosted instance on the same network as the printer); everything else (the card, ETA, links) works everywhere. A failed print comes through as a red, high-priority alert.
- The print farm scales to a real fleet: searchable pickers + bulk actions. Every New-Job dropdown (connection, filament spool, pool, machine, printer) and the printer→machine link picker is now a type-to-search box instead of a scroll-through list, so picking the right spool out of a full inventory, or the right printer out of fifty, is a few keystrokes. The print queue got checkboxes: select a batch (or select-all) and Send / Cancel / Remove them in one click instead of confirming fifty times, a mixed selection only acts on the rows each action applies to. And building a pool is no longer one-dropdown-click-per-machine: + add machines… opens a searchable checklist so you add the whole set at once.
- Print queue: live, cancellable, and no longer capped. The queue now updates on its own while anything is running (no more stale snapshot: pool auto-assignment and print progress refresh live). Every job gets Cancel (tells the printer to stop where the manager supports it; otherwise marks it cancelled so Cobblr stops tracking it) and Remove from queue (a queued/finished job, an active print must be cancelled first so you can't lose track of it). And the queue no longer silently hides everything past 200 jobs: it pages, with a Load older jobs button and a status filter behind the API.
- Stuck print jobs can be re-pointed at a printer: no delete-and-recreate. When a job's target matches no printer (or several), it lands in *awaiting assignment* instead of guessing. Now each such job in the queue has a Pick printer button: choose one from a searchable list and it's sent straight there, reusing the file Cobblr already uploaded, so there's no re-upload and no rebuilding the job from scratch. Picking a printer that isn't on that connection is refused, so a stray pick can't misroute the print.
- See your printer's webcam from anywhere: opt-in snapshot relay (off by default). A printer's camera is usually a LAN-only address, so it only shows in the Fleet when you're on the same network. Turn on Relay snapshots to cloud (the camera control on a Fleet card) and the edge agent pushes a frame every few seconds up to Cobblr, which serves the latest back, so you get a near-live thumbnail of the printer from your phone, anywhere, without exposing your camera or streaming full video. It's off until you switch it on (it uses upload bandwidth), and the tunnel itself still carries only stats, not video. This is the deliberate, cheap middle ground for remote viewing. Paired with the OctoPrint plugin's matching "relay snapshots" toggle.
- Migrating off FDM Monster? Digital Fabrication now has an Import farm button. Point it at your FDM Monster and bring its printers in two ways, your choice: connect to each printer directly: Cobblr recreates every printer as its own connection of the *matching* type (OctoPrint, Klipper/Moonraker, PrusaLink, FluidNC: per printer, not all-OctoPrint), installs the drivers it needs, pools them, and drops FDM Monster from the path; or keep FDM Monster and mirror its printers into a Cobblr pool. Either way the Fleet reads the imported machines as one farm.
- Upload a receipt and Cobblr reads it for you. On the Scan page, hit Receipt, pick a receipt PDF or a photo of one, and AI pulls out the vendor, date, and every line item. Each lands in your scan inbox as its own row, ready to confirm into a part the same way a barcode or photo scan does. No more typing a receipt in by hand.
- Spoolman users: Cobblr now talks to it. Connect your Spoolman (Inventory → Spoolman) and Sync: each spool comes in as an item with its measured remaining weight, and Cobblr treats Spoolman as the source of truth: it mirrors the number and won't deduct that spool itself (so a print counted by Moonraker → Spoolman isn't double-counted). Don't use Spoolman? Nothing changes. Cobblr's own consumption ledger keeps tracking. The right tracker for your setup, automatically.

### Improvements
- Capture-first onboarding now works without AI. When a workspace has no AI provider (free tier, self-host, or offline), the matcher falls back to a deterministic keyword + field-overlap pass, so writing "3 skeins of blue worsted merino wool" still gets identified as Yarn (fiber Wool, weight Worsted, qty 3) and offers to build the tracker, at zero AI cost. Connecting an AI provider only sharpens the identification + field-fill; capture-first never goes dark.
- The Configuration page is no longer a wall. It now opens to the ~11 settings you actually use day-to-day, grouped and fully visible; the other two dozen (tokens, integrations, diagnostics, …) tuck behind a single Show advanced settings button. Search still finds every setting regardless. And Simple mode now does something visible: turning it on drops you straight onto your clean workspace home, and if you come back to Configuration it shows a calm one-card state instead of the control room, with one click to turn it back off.
- The print-farm cockpit now shows chamber temperature and the current job stage ("Preheating bed", "Calibrating extrusion", …) on each printer card, so you can see *why* a printer isn't printing yet, not just that it's busy. Surfaced by the edge-bridge's Bambu and new Klipper/Moonraker drivers.
- The Fleet floor now groups machines by pool: so a pool of individual printers (one OctoPrint here, a Klipper there) reads as one farm, with a header and a machine count, even across different connections. Machines that aren't in a pool still show under their connection. The payoff of running your printers as a Cobblr farm instead of through FDM Monster.
- Opening Cobblr on a new device (or any time there's no remembered workspace) now lands you on the top of your workspace switcher (your first owned workspace) instead of your oldest membership, which could be a shared workspace sitting low in the list. (A pin-your-own default workspace is a planned follow-up.)
- Managed apps (like Cobblr for Yarn) now show their tabs in a curated order (the headline tracker first) instead of alphabetically.
- The + New thing dialog now explains its two choices far more clearly. Each option leads with the key idea: *"Stands on its own"* vs *"Lives inside an existing thing"*, followed by when to pick it and a concrete example, so it's obvious whether you want a brand-new standalone list or a sub-category nested inside something you already track.
- The new-workspace start screen is now one page with no dead-ends: a single search box at the top: type "yarn", "3d printer", or just describe what you have ("blue worsted wool, 3 skeins"), that both filters the ready-made trackers below it AND lets you add freeform text directly (Cobblr finds the right tracker). The full tracker gallery shows inline (no more "Start from a template" that whisked you to a separate view with no way back), with the marketplace one link away.
- A photo you scan that can't be auto-identified (a bare photo on a workspace with no vision AI) no longer sits spinning forever. It now shows a one-field "What is this?" box; type a couple of words ("blue worsted yarn") and Cobblr routes it to the right table and fills the fields it recognizes, just like a written note. Works in both the Scan Inbox and the capture-first onboarding.

### Fixes
- The Fleet view stays fast and gentle on a big farm. The live floor used to ask every connected machine manager for its printer list on every refresh, all at once: fine for a couple of printers, rough at fifty, where one slow manager could stall the whole view. Now each connection's printer list is briefly cached (a rapid refresh or a second screen reuses it), each fetch is time-boxed (a slow or unreachable manager falls back to its last-known state instead of freezing everyone), and the fetches run in bounded batches rather than a fifty-wide burst. Your in-flight jobs, links, and bed-clear flags are still read live every refresh, only the printer *state* can be a few seconds old.
- Print jobs honour printer tags on every farm, and won't land on the wrong machine. A job targeted at a tag (e.g. your "PLA" group) now resolves to a real printer inside Cobblr before it's sent, so it works the same on every connected farm, including FDM Monster's classic submit, which used to ignore the tag and let the farm route the file wherever it liked. And if a job points at a printer that isn't on its connection (a stale or mixed-up id), the send is refused with a clear error and the job is left in the queue to re-target, instead of silently printing on whatever machine happened to share that id.

## 2026-06-17

### Features
- Backups can now go somewhere automatically. Add a *destination*: a server path / NAS folder, or an S3-compatible bucket (AWS S3, Cloudflare R2, Backblaze B2, MinIO), choose a daily or weekly schedule and how many copies to keep, and Cobblr pushes a backup on its own. Or hit Back up now to send one immediately. Credentials are encrypted at rest and never shown back.
- You can now download a blueprint of your workspace's *setup*: the modules, custom fields, wires, shared views and bundles you've configured, with no data, to share or reuse, and a full backup (that setup plus every row and uploaded file) as a single `.zip` to keep wherever you like. Restore a backup into a fresh workspace to bring everything back exactly, with the links between things intact. Find it under Configuration → Backup & blueprints.
- New visual form builder (Configuration → Form builder): pick what you're tracking and drag its fields into the order you want, grouping them under named section headings like "Specs" or "Purchase info". The layout you arrange then shows on every add/edit form for that thing.
- A managed app now has a Settings page: a locked app like "Cobblr for Yarn" hides the whole platform, but you still need to tailor it, so the account menu gains a Settings item (at `/me/app-settings`, inside the lock-down) with a friendly show/hide list of every menu surface. Don't want the Designs table, or want to turn the scanner off? Hide it. Nothing is deleted, hiding a table just takes it off the menu and your items stay, ready to reappear when you show it again.
- The notifications bell can now open as a full-height right sidebar (the same shape as the AI chat) not just the compact dropdown. Toggle between the two from the panel icon in the inbox header, and it remembers whichever you prefer.
- Platform operators can now "View as" a workspace member: a read-only, time-boxed, audited support session that renders their workspace exactly as they see it, with an unmissable banner + full-window border. Editing is off by default and takes a deliberate "Enable editing" toggle; every session is logged and leaves a trace in the workspace's own activity feed.
- Colour swatches in the scan inbox: when a scan parses a colour (e.g. a Polar filament spool → "Royal Blue"), the Source-data box and the "From the label" strip now show a real colour swatch next to the value. Vendors give us the colour *name*, not a hex, so the swatch resolves standard web colour names (Royal Blue → a royal-blue dot); a maker-specific name we can't resolve ("Galaxy Black") just stays as text.

### Improvements
- Updating a bundle from your workspace home no longer makes you open the changelog modal and confirm a second time. When an update is conflict-free, the home strip now offers Update now (it applies right there and the line flips to "Update complete") plus a See details link if you want the changelog. The modal only steps in when an update touches a field you'd customized, then the line reads "Resolve conflict to update".
- The Workspace configuration page now has a search bar at the top. Type any part of a setting's name or description ("tokens", "units", "AI", "printer") and the page filters down to the matching tiles across every section, expanding them automatically, so you no longer have to remember which collapsed group a setting lives under. Clearing the search restores the normal grouped view.
- When we resolve your feedback, the in-app notification and Discord message now lead with what you reported before the fix (matching the email) so the reply has context instead of being a wall of fix text. The Discord message keeps a greeting; the in-app bell drops it to stay short.
- Modal and dialog titles now display in their natural case (e.g. "Send feedback", "New order", "Members") instead of being force-lowercased, so they read like proper titles.
