{
  "name": "@cobblr/thermal-print",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Home-agnostic Bluetooth thermal-label core: pure ESC/POS raster protocol (Phomemo M-series), a Web Bluetooth transport, an escalating printer self-test sequence, and a harvestable printer-profile registry. Consumed by the standalone printer self-test site today and by in-app label printing later \u2014 so both drive identical hardware behaviour.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./profiles": {
      "types": "./src/profiles.ts",
      "import": "./src/profiles.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
