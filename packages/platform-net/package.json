{
  "name": "@cobblr/platform-net",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-only outbound-fetch primitives shared across the platform: the one redirect-following, IP-pinning fetch loop every SSRF-guarded path uses.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "undici": "^6.27.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
